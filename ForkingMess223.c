		irq_set_handler_data(port->irq, port);
/*		irq_set_handler_data(port->irq, port);
 *  linux/kernel/fork.c		irq_set_handler_data(port->irq, port);
 *		irq_set_handler_data(port->irq, port);
 *  Copyright (C) 1991, 1992  Linus Torvalds		irq_set_handler_data(port->irq, port);
 */		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/*		irq_set_handler_data(port->irq, port);
 *  'fork.c' contains the help-routines for the 'fork' system call		irq_set_handler_data(port->irq, port);
 * (see also entry.S and others).		irq_set_handler_data(port->irq, port);
 * Fork is rather simple, once you get the hang of it, but the memory		irq_set_handler_data(port->irq, port);
 * management can be a bitch. See 'mm/memory.c': 'copy_page_range()'		irq_set_handler_data(port->irq, port);
 */		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
#include <linux/slab.h>		irq_set_handler_data(port->irq, port);
#include <linux/init.h>		irq_set_handler_data(port->irq, port);
#include <linux/unistd.h>		irq_set_handler_data(port->irq, port);
#include <linux/module.h>		irq_set_handler_data(port->irq, port);
#include <linux/vmalloc.h>		irq_set_handler_data(port->irq, port);
#include <linux/completion.h>		irq_set_handler_data(port->irq, port);
#include <linux/personality.h>		irq_set_handler_data(port->irq, port);
#include <linux/mempolicy.h>		irq_set_handler_data(port->irq, port);
#include <linux/sem.h>		irq_set_handler_data(port->irq, port);
#include <linux/file.h>		irq_set_handler_data(port->irq, port);
#include <linux/fdtable.h>		irq_set_handler_data(port->irq, port);
#include <linux/iocontext.h>		irq_set_handler_data(port->irq, port);
#include <linux/key.h>		irq_set_handler_data(port->irq, port);
#include <linux/binfmts.h>		irq_set_handler_data(port->irq, port);
#include <linux/mman.h>		irq_set_handler_data(port->irq, port);
#include <linux/mmu_notifier.h>		irq_set_handler_data(port->irq, port);
#include <linux/fs.h>		irq_set_handler_data(port->irq, port);
#include <linux/nsproxy.h>		irq_set_handler_data(port->irq, port);
#include <linux/capability.h>		irq_set_handler_data(port->irq, port);
#include <linux/cpu.h>		irq_set_handler_data(port->irq, port);
#include <linux/cgroup.h>		irq_set_handler_data(port->irq, port);
#include <linux/security.h>		irq_set_handler_data(port->irq, port);
#include <linux/hugetlb.h>		irq_set_handler_data(port->irq, port);
#include <linux/seccomp.h>		irq_set_handler_data(port->irq, port);
#include <linux/swap.h>		irq_set_handler_data(port->irq, port);
#include <linux/syscalls.h>		irq_set_handler_data(port->irq, port);
#include <linux/jiffies.h>		irq_set_handler_data(port->irq, port);
#include <linux/futex.h>		irq_set_handler_data(port->irq, port);
#include <linux/compat.h>		irq_set_handler_data(port->irq, port);
#include <linux/kthread.h>		irq_set_handler_data(port->irq, port);
#include <linux/task_io_accounting_ops.h>		irq_set_handler_data(port->irq, port);
#include <linux/rcupdate.h>		irq_set_handler_data(port->irq, port);
#include <linux/ptrace.h>		irq_set_handler_data(port->irq, port);
#include <linux/mount.h>		irq_set_handler_data(port->irq, port);
#include <linux/audit.h>		irq_set_handler_data(port->irq, port);
#include <linux/memcontrol.h>		irq_set_handler_data(port->irq, port);
#include <linux/ftrace.h>		irq_set_handler_data(port->irq, port);
#include <linux/proc_fs.h>		irq_set_handler_data(port->irq, port);
#include <linux/profile.h>		irq_set_handler_data(port->irq, port);
#include <linux/rmap.h>		irq_set_handler_data(port->irq, port);
#include <linux/ksm.h>		irq_set_handler_data(port->irq, port);
#include <linux/acct.h>		irq_set_handler_data(port->irq, port);
#include <linux/tsacct_kern.h>		irq_set_handler_data(port->irq, port);
#include <linux/cn_proc.h>		irq_set_handler_data(port->irq, port);
#include <linux/freezer.h>		irq_set_handler_data(port->irq, port);
#include <linux/delayacct.h>		irq_set_handler_data(port->irq, port);
#include <linux/taskstats_kern.h>		irq_set_handler_data(port->irq, port);
#include <linux/random.h>		irq_set_handler_data(port->irq, port);
#include <linux/tty.h>		irq_set_handler_data(port->irq, port);
#include <linux/blkdev.h>		irq_set_handler_data(port->irq, port);
#include <linux/fs_struct.h>		irq_set_handler_data(port->irq, port);
#include <linux/magic.h>		irq_set_handler_data(port->irq, port);
#include <linux/perf_event.h>		irq_set_handler_data(port->irq, port);
#include <linux/posix-timers.h>		irq_set_handler_data(port->irq, port);
#include <linux/user-return-notifier.h>		irq_set_handler_data(port->irq, port);
#include <linux/oom.h>		irq_set_handler_data(port->irq, port);
#include <linux/khugepaged.h>		irq_set_handler_data(port->irq, port);
#include <linux/signalfd.h>		irq_set_handler_data(port->irq, port);
#include <linux/uprobes.h>		irq_set_handler_data(port->irq, port);
#include <linux/aio.h>		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
#include <asm/pgtable.h>		irq_set_handler_data(port->irq, port);
#include <asm/pgalloc.h>		irq_set_handler_data(port->irq, port);
#include <asm/uaccess.h>		irq_set_handler_data(port->irq, port);
#include <asm/mmu_context.h>		irq_set_handler_data(port->irq, port);
#include <asm/cacheflush.h>		irq_set_handler_data(port->irq, port);
#include <asm/tlbflush.h>		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
#include <trace/events/sched.h>		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
#define CREATE_TRACE_POINTS		irq_set_handler_data(port->irq, port);
#include <trace/events/task.h>		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/*		irq_set_handler_data(port->irq, port);
 * Protected counters by write_lock_irq(&tasklist_lock)		irq_set_handler_data(port->irq, port);
 */		irq_set_handler_data(port->irq, port);
unsigned long total_forks;	/* Handle normal Linux uptimes. */		irq_set_handler_data(port->irq, port);
int nr_threads;			/* The idle threads do not count.. */		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
int max_threads;		/* tunable limit on nr_threads */		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
DEFINE_PER_CPU(unsigned long, process_counts) = 0;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
__cacheline_aligned DEFINE_RWLOCK(tasklist_lock);  /* outer */		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_PROVE_RCU		irq_set_handler_data(port->irq, port);
int lockdep_tasklist_lock_is_held(void)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	return lockdep_is_held(&tasklist_lock);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
EXPORT_SYMBOL_GPL(lockdep_tasklist_lock_is_held);		irq_set_handler_data(port->irq, port);
#endif /* #ifdef CONFIG_PROVE_RCU */		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
int nr_processes(void)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	int cpu;		irq_set_handler_data(port->irq, port);
	int total = 0;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	for_each_possible_cpu(cpu)		irq_set_handler_data(port->irq, port);
		total += per_cpu(process_counts, cpu);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	return total;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
void __weak arch_release_task_struct(struct task_struct *tsk)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
#ifndef CONFIG_ARCH_TASK_STRUCT_ALLOCATOR		irq_set_handler_data(port->irq, port);
static struct kmem_cache *task_struct_cachep;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static inline struct task_struct *alloc_task_struct_node(int node)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	return kmem_cache_alloc_node(task_struct_cachep, GFP_KERNEL, node);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static inline void free_task_struct(struct task_struct *tsk)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	kmem_cache_free(task_struct_cachep, tsk);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
void __weak arch_release_thread_info(struct thread_info *ti)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
#ifndef CONFIG_ARCH_THREAD_INFO_ALLOCATOR		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/*		irq_set_handler_data(port->irq, port);
 * Allocate pages if THREAD_SIZE is >= PAGE_SIZE, otherwise use a		irq_set_handler_data(port->irq, port);
 * kmemcache based allocator.		irq_set_handler_data(port->irq, port);
 */		irq_set_handler_data(port->irq, port);
# if THREAD_SIZE >= PAGE_SIZE		irq_set_handler_data(port->irq, port);
static struct thread_info *alloc_thread_info_node(struct task_struct *tsk,		irq_set_handler_data(port->irq, port);
						  int node)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	struct page *page = alloc_pages_node(node, THREADINFO_GFP_ACCOUNTED,		irq_set_handler_data(port->irq, port);
					     THREAD_SIZE_ORDER);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	return page ? page_address(page) : NULL;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static inline void free_thread_info(struct thread_info *ti)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	free_memcg_kmem_pages((unsigned long)ti, THREAD_SIZE_ORDER);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
# else		irq_set_handler_data(port->irq, port);
static struct kmem_cache *thread_info_cache;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static struct thread_info *alloc_thread_info_node(struct task_struct *tsk,		irq_set_handler_data(port->irq, port);
						  int node)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	return kmem_cache_alloc_node(thread_info_cache, THREADINFO_GFP, node);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static void free_thread_info(struct thread_info *ti)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	kmem_cache_free(thread_info_cache, ti);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
void thread_info_cache_init(void)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	thread_info_cache = kmem_cache_create("thread_info", THREAD_SIZE,		irq_set_handler_data(port->irq, port);
					      THREAD_SIZE, 0, NULL);		irq_set_handler_data(port->irq, port);
	BUG_ON(thread_info_cache == NULL);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
# endif		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/* SLAB cache for signal_struct structures (tsk->signal) */		irq_set_handler_data(port->irq, port);
static struct kmem_cache *signal_cachep;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/* SLAB cache for sighand_struct structures (tsk->sighand) */		irq_set_handler_data(port->irq, port);
struct kmem_cache *sighand_cachep;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/* SLAB cache for files_struct structures (tsk->files) */		irq_set_handler_data(port->irq, port);
struct kmem_cache *files_cachep;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/* SLAB cache for fs_struct structures (tsk->fs) */		irq_set_handler_data(port->irq, port);
struct kmem_cache *fs_cachep;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/* SLAB cache for vm_area_struct structures */		irq_set_handler_data(port->irq, port);
struct kmem_cache *vm_area_cachep;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/* SLAB cache for mm_struct structures (tsk->mm) */		irq_set_handler_data(port->irq, port);
static struct kmem_cache *mm_cachep;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static void account_kernel_stack(struct thread_info *ti, int account)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	struct zone *zone = page_zone(virt_to_page(ti));		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	mod_zone_page_state(zone, NR_KERNEL_STACK, account);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
void free_task(struct task_struct *tsk)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	account_kernel_stack(tsk->stack, -1);		irq_set_handler_data(port->irq, port);
	arch_release_thread_info(tsk->stack);		irq_set_handler_data(port->irq, port);
	free_thread_info(tsk->stack);		irq_set_handler_data(port->irq, port);
	rt_mutex_debug_task_free(tsk);		irq_set_handler_data(port->irq, port);
	ftrace_graph_exit_task(tsk);		irq_set_handler_data(port->irq, port);
	put_seccomp_filter(tsk);		irq_set_handler_data(port->irq, port);
	arch_release_task_struct(tsk);		irq_set_handler_data(port->irq, port);
	free_task_struct(tsk);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
EXPORT_SYMBOL(free_task);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static inline void free_signal_struct(struct signal_struct *sig)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	taskstats_tgid_free(sig);		irq_set_handler_data(port->irq, port);
	sched_autogroup_exit(sig);		irq_set_handler_data(port->irq, port);
	kmem_cache_free(signal_cachep, sig);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static inline void put_signal_struct(struct signal_struct *sig)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	if (atomic_dec_and_test(&sig->sigcnt))		irq_set_handler_data(port->irq, port);
		free_signal_struct(sig);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
void __put_task_struct(struct task_struct *tsk)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	WARN_ON(!tsk->exit_state);		irq_set_handler_data(port->irq, port);
	WARN_ON(atomic_read(&tsk->usage));		irq_set_handler_data(port->irq, port);
	WARN_ON(tsk == current);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	task_numa_free(tsk);		irq_set_handler_data(port->irq, port);
	security_task_free(tsk);		irq_set_handler_data(port->irq, port);
	exit_creds(tsk);		irq_set_handler_data(port->irq, port);
	delayacct_tsk_free(tsk);		irq_set_handler_data(port->irq, port);
	put_signal_struct(tsk->signal);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	if (!profile_handoff_task(tsk))		irq_set_handler_data(port->irq, port);
		free_task(tsk);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
EXPORT_SYMBOL_GPL(__put_task_struct);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
void __init __weak arch_task_cache_init(void) { }		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
void __init fork_init(unsigned long mempages)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
#ifndef CONFIG_ARCH_TASK_STRUCT_ALLOCATOR		irq_set_handler_data(port->irq, port);
#ifndef ARCH_MIN_TASKALIGN		irq_set_handler_data(port->irq, port);
#define ARCH_MIN_TASKALIGN	L1_CACHE_BYTES		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
	/* create a slab on which task_structs can be allocated */		irq_set_handler_data(port->irq, port);
	task_struct_cachep =		irq_set_handler_data(port->irq, port);
		kmem_cache_create("task_struct", sizeof(struct task_struct),		irq_set_handler_data(port->irq, port);
			ARCH_MIN_TASKALIGN, SLAB_PANIC | SLAB_NOTRACK, NULL);		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/* do the arch specific task caches init */		irq_set_handler_data(port->irq, port);
	arch_task_cache_init();		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * The default maximum number of threads is set to a safe		irq_set_handler_data(port->irq, port);
	 * value: the thread structures can take up at most half		irq_set_handler_data(port->irq, port);
	 * of memory.		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	max_threads = mempages / (8 * THREAD_SIZE / PAGE_SIZE);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * we need to allow at least 20 threads to boot a system		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	if (max_threads < 20)		irq_set_handler_data(port->irq, port);
		max_threads = 20;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	init_task.signal->rlim[RLIMIT_NPROC].rlim_cur = max_threads/2;		irq_set_handler_data(port->irq, port);
	init_task.signal->rlim[RLIMIT_NPROC].rlim_max = max_threads/2;		irq_set_handler_data(port->irq, port);
	init_task.signal->rlim[RLIMIT_SIGPENDING] =		irq_set_handler_data(port->irq, port);
		init_task.signal->rlim[RLIMIT_NPROC];		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
int __attribute__((weak)) arch_dup_task_struct(struct task_struct *dst,		irq_set_handler_data(port->irq, port);
					       struct task_struct *src)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	*dst = *src;		irq_set_handler_data(port->irq, port);
	return 0;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static struct task_struct *dup_task_struct(struct task_struct *orig)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	struct task_struct *tsk;		irq_set_handler_data(port->irq, port);
	struct thread_info *ti;		irq_set_handler_data(port->irq, port);
	unsigned long *stackend;		irq_set_handler_data(port->irq, port);
	int node = tsk_fork_get_node(orig);		irq_set_handler_data(port->irq, port);
	int err;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	tsk = alloc_task_struct_node(node);		irq_set_handler_data(port->irq, port);
	if (!tsk)		irq_set_handler_data(port->irq, port);
		return NULL;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	ti = alloc_thread_info_node(tsk, node);		irq_set_handler_data(port->irq, port);
	if (!ti)		irq_set_handler_data(port->irq, port);
		goto free_tsk;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	err = arch_dup_task_struct(tsk, orig);		irq_set_handler_data(port->irq, port);
	if (err)		irq_set_handler_data(port->irq, port);
		goto free_ti;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	tsk->stack = ti;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	setup_thread_stack(tsk, orig);		irq_set_handler_data(port->irq, port);
	clear_user_return_notifier(tsk);		irq_set_handler_data(port->irq, port);
	clear_tsk_need_resched(tsk);		irq_set_handler_data(port->irq, port);
	stackend = end_of_stack(tsk);		irq_set_handler_data(port->irq, port);
	*stackend = STACK_END_MAGIC;	/* for overflow detection */		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_CC_STACKPROTECTOR		irq_set_handler_data(port->irq, port);
	tsk->stack_canary = get_random_int();		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * One for us, one for whoever does the "release_task()" (usually		irq_set_handler_data(port->irq, port);
	 * parent)		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	atomic_set(&tsk->usage, 2);		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_BLK_DEV_IO_TRACE		irq_set_handler_data(port->irq, port);
	tsk->btrace_seq = 0;		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
	tsk->splice_pipe = NULL;		irq_set_handler_data(port->irq, port);
	tsk->task_frag.page = NULL;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	account_kernel_stack(ti, 1);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	return tsk;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
free_ti:		irq_set_handler_data(port->irq, port);
	free_thread_info(ti);		irq_set_handler_data(port->irq, port);
free_tsk:		irq_set_handler_data(port->irq, port);
	free_task_struct(tsk);		irq_set_handler_data(port->irq, port);
	return NULL;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_MMU		irq_set_handler_data(port->irq, port);
static int dup_mmap(struct mm_struct *mm, struct mm_struct *oldmm)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	struct vm_area_struct *mpnt, *tmp, *prev, **pprev;		irq_set_handler_data(port->irq, port);
	struct rb_node **rb_link, *rb_parent;		irq_set_handler_data(port->irq, port);
	int retval;		irq_set_handler_data(port->irq, port);
	unsigned long charge;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	uprobe_start_dup_mmap();		irq_set_handler_data(port->irq, port);
	down_write(&oldmm->mmap_sem);		irq_set_handler_data(port->irq, port);
	flush_cache_dup_mm(oldmm);		irq_set_handler_data(port->irq, port);
	uprobe_dup_mmap(oldmm, mm);		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * Not linked in yet - no deadlock potential:		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	down_write_nested(&mm->mmap_sem, SINGLE_DEPTH_NESTING);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	mm->locked_vm = 0;		irq_set_handler_data(port->irq, port);
	mm->mmap = NULL;		irq_set_handler_data(port->irq, port);
	mm->mmap_cache = NULL;		irq_set_handler_data(port->irq, port);
	mm->map_count = 0;		irq_set_handler_data(port->irq, port);
	cpumask_clear(mm_cpumask(mm));		irq_set_handler_data(port->irq, port);
	mm->mm_rb = RB_ROOT;		irq_set_handler_data(port->irq, port);
	rb_link = &mm->mm_rb.rb_node;		irq_set_handler_data(port->irq, port);
	rb_parent = NULL;		irq_set_handler_data(port->irq, port);
	pprev = &mm->mmap;		irq_set_handler_data(port->irq, port);
	retval = ksm_fork(mm, oldmm);		irq_set_handler_data(port->irq, port);
	if (retval)		irq_set_handler_data(port->irq, port);
		goto out;		irq_set_handler_data(port->irq, port);
	retval = khugepaged_fork(mm, oldmm);		irq_set_handler_data(port->irq, port);
	if (retval)		irq_set_handler_data(port->irq, port);
		goto out;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	prev = NULL;		irq_set_handler_data(port->irq, port);
	for (mpnt = oldmm->mmap; mpnt; mpnt = mpnt->vm_next) {		irq_set_handler_data(port->irq, port);
		struct file *file;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		if (mpnt->vm_flags & VM_DONTCOPY) {		irq_set_handler_data(port->irq, port);
			vm_stat_account(mm, mpnt->vm_flags, mpnt->vm_file,		irq_set_handler_data(port->irq, port);
							-vma_pages(mpnt));		irq_set_handler_data(port->irq, port);
			continue;		irq_set_handler_data(port->irq, port);
		}		irq_set_handler_data(port->irq, port);
		charge = 0;		irq_set_handler_data(port->irq, port);
		if (mpnt->vm_flags & VM_ACCOUNT) {		irq_set_handler_data(port->irq, port);
			unsigned long len = vma_pages(mpnt);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
			if (security_vm_enough_memory_mm(oldmm, len)) /* sic */		irq_set_handler_data(port->irq, port);
				goto fail_nomem;		irq_set_handler_data(port->irq, port);
			charge = len;		irq_set_handler_data(port->irq, port);
		}		irq_set_handler_data(port->irq, port);
		tmp = kmem_cache_alloc(vm_area_cachep, GFP_KERNEL);		irq_set_handler_data(port->irq, port);
		if (!tmp)		irq_set_handler_data(port->irq, port);
			goto fail_nomem;		irq_set_handler_data(port->irq, port);
		*tmp = *mpnt;		irq_set_handler_data(port->irq, port);
		INIT_LIST_HEAD(&tmp->anon_vma_chain);		irq_set_handler_data(port->irq, port);
		retval = vma_dup_policy(mpnt, tmp);		irq_set_handler_data(port->irq, port);
		if (retval)		irq_set_handler_data(port->irq, port);
			goto fail_nomem_policy;		irq_set_handler_data(port->irq, port);
		tmp->vm_mm = mm;		irq_set_handler_data(port->irq, port);
		if (anon_vma_fork(tmp, mpnt))		irq_set_handler_data(port->irq, port);
			goto fail_nomem_anon_vma_fork;		irq_set_handler_data(port->irq, port);
		tmp->vm_flags &= ~VM_LOCKED;		irq_set_handler_data(port->irq, port);
		tmp->vm_next = tmp->vm_prev = NULL;		irq_set_handler_data(port->irq, port);
		file = tmp->vm_file;		irq_set_handler_data(port->irq, port);
		if (file) {		irq_set_handler_data(port->irq, port);
			struct inode *inode = file_inode(file);		irq_set_handler_data(port->irq, port);
			struct address_space *mapping = file->f_mapping;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
			get_file(file);		irq_set_handler_data(port->irq, port);
			if (tmp->vm_flags & VM_DENYWRITE)		irq_set_handler_data(port->irq, port);
				atomic_dec(&inode->i_writecount);		irq_set_handler_data(port->irq, port);
			mutex_lock(&mapping->i_mmap_mutex);		irq_set_handler_data(port->irq, port);
			if (tmp->vm_flags & VM_SHARED)		irq_set_handler_data(port->irq, port);
				mapping->i_mmap_writable++;		irq_set_handler_data(port->irq, port);
			flush_dcache_mmap_lock(mapping);		irq_set_handler_data(port->irq, port);
			/* insert tmp into the share list, just after mpnt */		irq_set_handler_data(port->irq, port);
			if (unlikely(tmp->vm_flags & VM_NONLINEAR))		irq_set_handler_data(port->irq, port);
				vma_nonlinear_insert(tmp,		irq_set_handler_data(port->irq, port);
						&mapping->i_mmap_nonlinear);		irq_set_handler_data(port->irq, port);
			else		irq_set_handler_data(port->irq, port);
				vma_interval_tree_insert_after(tmp, mpnt,		irq_set_handler_data(port->irq, port);
							&mapping->i_mmap);		irq_set_handler_data(port->irq, port);
			flush_dcache_mmap_unlock(mapping);		irq_set_handler_data(port->irq, port);
			mutex_unlock(&mapping->i_mmap_mutex);		irq_set_handler_data(port->irq, port);
		}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		/*		irq_set_handler_data(port->irq, port);
		 * Clear hugetlb-related page reserves for children. This only		irq_set_handler_data(port->irq, port);
		 * affects MAP_PRIVATE mappings. Faults generated by the child		irq_set_handler_data(port->irq, port);
		 * are not guaranteed to succeed, even if read-only		irq_set_handler_data(port->irq, port);
		 */		irq_set_handler_data(port->irq, port);
		if (is_vm_hugetlb_page(tmp))		irq_set_handler_data(port->irq, port);
			reset_vma_resv_huge_pages(tmp);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		/*		irq_set_handler_data(port->irq, port);
		 * Link in the new vma and copy the page table entries.		irq_set_handler_data(port->irq, port);
		 */		irq_set_handler_data(port->irq, port);
		*pprev = tmp;		irq_set_handler_data(port->irq, port);
		pprev = &tmp->vm_next;		irq_set_handler_data(port->irq, port);
		tmp->vm_prev = prev;		irq_set_handler_data(port->irq, port);
		prev = tmp;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		__vma_link_rb(mm, tmp, rb_link, rb_parent);		irq_set_handler_data(port->irq, port);
		rb_link = &tmp->vm_rb.rb_right;		irq_set_handler_data(port->irq, port);
		rb_parent = &tmp->vm_rb;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		mm->map_count++;		irq_set_handler_data(port->irq, port);
		retval = copy_page_range(mm, oldmm, mpnt);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		if (tmp->vm_ops && tmp->vm_ops->open)		irq_set_handler_data(port->irq, port);
			tmp->vm_ops->open(tmp);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		if (retval)		irq_set_handler_data(port->irq, port);
			goto out;		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
	/* a new mm has just been created */		irq_set_handler_data(port->irq, port);
	arch_dup_mmap(oldmm, mm);		irq_set_handler_data(port->irq, port);
	retval = 0;		irq_set_handler_data(port->irq, port);
out:		irq_set_handler_data(port->irq, port);
	up_write(&mm->mmap_sem);		irq_set_handler_data(port->irq, port);
	flush_tlb_mm(oldmm);		irq_set_handler_data(port->irq, port);
	up_write(&oldmm->mmap_sem);		irq_set_handler_data(port->irq, port);
	uprobe_end_dup_mmap();		irq_set_handler_data(port->irq, port);
	return retval;		irq_set_handler_data(port->irq, port);
fail_nomem_anon_vma_fork:		irq_set_handler_data(port->irq, port);
	mpol_put(vma_policy(tmp));		irq_set_handler_data(port->irq, port);
fail_nomem_policy:		irq_set_handler_data(port->irq, port);
	kmem_cache_free(vm_area_cachep, tmp);		irq_set_handler_data(port->irq, port);
fail_nomem:		irq_set_handler_data(port->irq, port);
	retval = -ENOMEM;		irq_set_handler_data(port->irq, port);
	vm_unacct_memory(charge);		irq_set_handler_data(port->irq, port);
	goto out;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static inline int mm_alloc_pgd(struct mm_struct *mm)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	mm->pgd = pgd_alloc(mm);		irq_set_handler_data(port->irq, port);
	if (unlikely(!mm->pgd))		irq_set_handler_data(port->irq, port);
		return -ENOMEM;		irq_set_handler_data(port->irq, port);
	return 0;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static inline void mm_free_pgd(struct mm_struct *mm)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	pgd_free(mm, mm->pgd);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
#else		irq_set_handler_data(port->irq, port);
#define dup_mmap(mm, oldmm)	(0)		irq_set_handler_data(port->irq, port);
#define mm_alloc_pgd(mm)	(0)		irq_set_handler_data(port->irq, port);
#define mm_free_pgd(mm)		irq_set_handler_data(port->irq, port);
#endif /* CONFIG_MMU */		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
__cacheline_aligned_in_smp DEFINE_SPINLOCK(mmlist_lock);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
#define allocate_mm()	(kmem_cache_alloc(mm_cachep, GFP_KERNEL))		irq_set_handler_data(port->irq, port);
#define free_mm(mm)	(kmem_cache_free(mm_cachep, (mm)))		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static unsigned long default_dump_filter = MMF_DUMP_FILTER_DEFAULT;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static int __init coredump_filter_setup(char *s)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	default_dump_filter =		irq_set_handler_data(port->irq, port);
		(simple_strtoul(s, NULL, 0) << MMF_DUMP_FILTER_SHIFT) &		irq_set_handler_data(port->irq, port);
		MMF_DUMP_FILTER_MASK;		irq_set_handler_data(port->irq, port);
	return 1;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
__setup("coredump_filter=", coredump_filter_setup);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
#include <linux/init_task.h>		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static void mm_init_aio(struct mm_struct *mm)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_AIO		irq_set_handler_data(port->irq, port);
	spin_lock_init(&mm->ioctx_lock);		irq_set_handler_data(port->irq, port);
	mm->ioctx_table = NULL;		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static struct mm_struct *mm_init(struct mm_struct *mm, struct task_struct *p)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	atomic_set(&mm->mm_users, 1);		irq_set_handler_data(port->irq, port);
	atomic_set(&mm->mm_count, 1);		irq_set_handler_data(port->irq, port);
	init_rwsem(&mm->mmap_sem);		irq_set_handler_data(port->irq, port);
	INIT_LIST_HEAD(&mm->mmlist);		irq_set_handler_data(port->irq, port);
	mm->flags = (current->mm) ?		irq_set_handler_data(port->irq, port);
		(current->mm->flags & MMF_INIT_MASK) : default_dump_filter;		irq_set_handler_data(port->irq, port);
	mm->core_state = NULL;		irq_set_handler_data(port->irq, port);
	atomic_long_set(&mm->nr_ptes, 0);		irq_set_handler_data(port->irq, port);
	memset(&mm->rss_stat, 0, sizeof(mm->rss_stat));		irq_set_handler_data(port->irq, port);
	spin_lock_init(&mm->page_table_lock);		irq_set_handler_data(port->irq, port);
	mm_init_aio(mm);		irq_set_handler_data(port->irq, port);
	mm_init_owner(mm, p);		irq_set_handler_data(port->irq, port);
	clear_tlb_flush_pending(mm);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	if (likely(!mm_alloc_pgd(mm))) {		irq_set_handler_data(port->irq, port);
		mm->def_flags = 0;		irq_set_handler_data(port->irq, port);
		mmu_notifier_mm_init(mm);		irq_set_handler_data(port->irq, port);
		return mm;		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	free_mm(mm);		irq_set_handler_data(port->irq, port);
	return NULL;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static void check_mm(struct mm_struct *mm)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	int i;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	for (i = 0; i < NR_MM_COUNTERS; i++) {		irq_set_handler_data(port->irq, port);
		long x = atomic_long_read(&mm->rss_stat.count[i]);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		if (unlikely(x))		irq_set_handler_data(port->irq, port);
			printk(KERN_ALERT "BUG: Bad rss-counter state "		irq_set_handler_data(port->irq, port);
					  "mm:%p idx:%d val:%ld\n", mm, i, x);		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
#if defined(CONFIG_TRANSPARENT_HUGEPAGE) && !USE_SPLIT_PMD_PTLOCKS		irq_set_handler_data(port->irq, port);
	VM_BUG_ON(mm->pmd_huge_pte);		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/*		irq_set_handler_data(port->irq, port);
 * Allocate and initialize an mm_struct.		irq_set_handler_data(port->irq, port);
 */		irq_set_handler_data(port->irq, port);
struct mm_struct *mm_alloc(void)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	struct mm_struct *mm;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	mm = allocate_mm();		irq_set_handler_data(port->irq, port);
	if (!mm)		irq_set_handler_data(port->irq, port);
		return NULL;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	memset(mm, 0, sizeof(*mm));		irq_set_handler_data(port->irq, port);
	mm_init_cpumask(mm);		irq_set_handler_data(port->irq, port);
	return mm_init(mm, current);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/*		irq_set_handler_data(port->irq, port);
 * Called when the last reference to the mm		irq_set_handler_data(port->irq, port);
 * is dropped: either by a lazy thread or by		irq_set_handler_data(port->irq, port);
 * mmput. Free the page directory and the mm.		irq_set_handler_data(port->irq, port);
 */		irq_set_handler_data(port->irq, port);
void __mmdrop(struct mm_struct *mm)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	BUG_ON(mm == &init_mm);		irq_set_handler_data(port->irq, port);
	mm_free_pgd(mm);		irq_set_handler_data(port->irq, port);
	destroy_context(mm);		irq_set_handler_data(port->irq, port);
	mmu_notifier_mm_destroy(mm);		irq_set_handler_data(port->irq, port);
	check_mm(mm);		irq_set_handler_data(port->irq, port);
	free_mm(mm);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
EXPORT_SYMBOL_GPL(__mmdrop);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/*		irq_set_handler_data(port->irq, port);
 * Decrement the use count and release all resources for an mm.		irq_set_handler_data(port->irq, port);
 */		irq_set_handler_data(port->irq, port);
void mmput(struct mm_struct *mm)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	might_sleep();		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	if (atomic_dec_and_test(&mm->mm_users)) {		irq_set_handler_data(port->irq, port);
		uprobe_clear_state(mm);		irq_set_handler_data(port->irq, port);
		exit_aio(mm);		irq_set_handler_data(port->irq, port);
		ksm_exit(mm);		irq_set_handler_data(port->irq, port);
		khugepaged_exit(mm); /* must run before exit_mmap */		irq_set_handler_data(port->irq, port);
		exit_mmap(mm);		irq_set_handler_data(port->irq, port);
		set_mm_exe_file(mm, NULL);		irq_set_handler_data(port->irq, port);
		if (!list_empty(&mm->mmlist)) {		irq_set_handler_data(port->irq, port);
			spin_lock(&mmlist_lock);		irq_set_handler_data(port->irq, port);
			list_del(&mm->mmlist);		irq_set_handler_data(port->irq, port);
			spin_unlock(&mmlist_lock);		irq_set_handler_data(port->irq, port);
		}		irq_set_handler_data(port->irq, port);
		if (mm->binfmt)		irq_set_handler_data(port->irq, port);
			module_put(mm->binfmt->module);		irq_set_handler_data(port->irq, port);
		mmdrop(mm);		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
EXPORT_SYMBOL_GPL(mmput);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
void set_mm_exe_file(struct mm_struct *mm, struct file *new_exe_file)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	if (new_exe_file)		irq_set_handler_data(port->irq, port);
		get_file(new_exe_file);		irq_set_handler_data(port->irq, port);
	if (mm->exe_file)		irq_set_handler_data(port->irq, port);
		fput(mm->exe_file);		irq_set_handler_data(port->irq, port);
	mm->exe_file = new_exe_file;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
struct file *get_mm_exe_file(struct mm_struct *mm)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	struct file *exe_file;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/* We need mmap_sem to protect against races with removal of exe_file */		irq_set_handler_data(port->irq, port);
	down_read(&mm->mmap_sem);		irq_set_handler_data(port->irq, port);
	exe_file = mm->exe_file;		irq_set_handler_data(port->irq, port);
	if (exe_file)		irq_set_handler_data(port->irq, port);
		get_file(exe_file);		irq_set_handler_data(port->irq, port);
	up_read(&mm->mmap_sem);		irq_set_handler_data(port->irq, port);
	return exe_file;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static void dup_mm_exe_file(struct mm_struct *oldmm, struct mm_struct *newmm)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	/* It's safe to write the exe_file pointer without exe_file_lock because		irq_set_handler_data(port->irq, port);
	 * this is called during fork when the task is not yet in /proc */		irq_set_handler_data(port->irq, port);
	newmm->exe_file = get_mm_exe_file(oldmm);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/**		irq_set_handler_data(port->irq, port);
 * get_task_mm - acquire a reference to the task's mm		irq_set_handler_data(port->irq, port);
 *		irq_set_handler_data(port->irq, port);
 * Returns %NULL if the task has no mm.  Checks PF_KTHREAD (meaning		irq_set_handler_data(port->irq, port);
 * this kernel workthread has transiently adopted a user mm with use_mm,		irq_set_handler_data(port->irq, port);
 * to do its AIO) is not set and if so returns a reference to it, after		irq_set_handler_data(port->irq, port);
 * bumping up the use count.  User must release the mm via mmput()		irq_set_handler_data(port->irq, port);
 * after use.  Typically used by /proc and ptrace.		irq_set_handler_data(port->irq, port);
 */		irq_set_handler_data(port->irq, port);
struct mm_struct *get_task_mm(struct task_struct *task)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	struct mm_struct *mm;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	task_lock(task);		irq_set_handler_data(port->irq, port);
	mm = task->mm;		irq_set_handler_data(port->irq, port);
	if (mm) {		irq_set_handler_data(port->irq, port);
		if (task->flags & PF_KTHREAD)		irq_set_handler_data(port->irq, port);
			mm = NULL;		irq_set_handler_data(port->irq, port);
		else		irq_set_handler_data(port->irq, port);
			atomic_inc(&mm->mm_users);		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
	task_unlock(task);		irq_set_handler_data(port->irq, port);
	return mm;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
EXPORT_SYMBOL_GPL(get_task_mm);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
struct mm_struct *mm_access(struct task_struct *task, unsigned int mode)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	struct mm_struct *mm;		irq_set_handler_data(port->irq, port);
	int err;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	err =  mutex_lock_killable(&task->signal->cred_guard_mutex);		irq_set_handler_data(port->irq, port);
	if (err)		irq_set_handler_data(port->irq, port);
		return ERR_PTR(err);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	mm = get_task_mm(task);		irq_set_handler_data(port->irq, port);
	if (mm && mm != current->mm &&		irq_set_handler_data(port->irq, port);
			!ptrace_may_access(task, mode)) {		irq_set_handler_data(port->irq, port);
		mmput(mm);		irq_set_handler_data(port->irq, port);
		mm = ERR_PTR(-EACCES);		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
	mutex_unlock(&task->signal->cred_guard_mutex);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	return mm;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static void complete_vfork_done(struct task_struct *tsk)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	struct completion *vfork;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	task_lock(tsk);		irq_set_handler_data(port->irq, port);
	vfork = tsk->vfork_done;		irq_set_handler_data(port->irq, port);
	if (likely(vfork)) {		irq_set_handler_data(port->irq, port);
		tsk->vfork_done = NULL;		irq_set_handler_data(port->irq, port);
		complete(vfork);		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
	task_unlock(tsk);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static int wait_for_vfork_done(struct task_struct *child,		irq_set_handler_data(port->irq, port);
				struct completion *vfork)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	int killed;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	freezer_do_not_count();		irq_set_handler_data(port->irq, port);
	killed = wait_for_completion_killable(vfork);		irq_set_handler_data(port->irq, port);
	freezer_count();		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	if (killed) {		irq_set_handler_data(port->irq, port);
		task_lock(child);		irq_set_handler_data(port->irq, port);
		child->vfork_done = NULL;		irq_set_handler_data(port->irq, port);
		task_unlock(child);		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	put_task_struct(child);		irq_set_handler_data(port->irq, port);
	return killed;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/* Please note the differences between mmput and mm_release.		irq_set_handler_data(port->irq, port);
 * mmput is called whenever we stop holding onto a mm_struct,		irq_set_handler_data(port->irq, port);
 * error success whatever.		irq_set_handler_data(port->irq, port);
 *		irq_set_handler_data(port->irq, port);
 * mm_release is called after a mm_struct has been removed		irq_set_handler_data(port->irq, port);
 * from the current process.		irq_set_handler_data(port->irq, port);
 *		irq_set_handler_data(port->irq, port);
 * This difference is important for error handling, when we		irq_set_handler_data(port->irq, port);
 * only half set up a mm_struct for a new process and need to restore		irq_set_handler_data(port->irq, port);
 * the old one.  Because we mmput the new mm_struct before		irq_set_handler_data(port->irq, port);
 * restoring the old one. . .		irq_set_handler_data(port->irq, port);
 * Eric Biederman 10 January 1998		irq_set_handler_data(port->irq, port);
 */		irq_set_handler_data(port->irq, port);
void mm_release(struct task_struct *tsk, struct mm_struct *mm)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	/* Get rid of any futexes when releasing the mm */		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_FUTEX		irq_set_handler_data(port->irq, port);
	if (unlikely(tsk->robust_list)) {		irq_set_handler_data(port->irq, port);
		exit_robust_list(tsk);		irq_set_handler_data(port->irq, port);
		tsk->robust_list = NULL;		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_COMPAT		irq_set_handler_data(port->irq, port);
	if (unlikely(tsk->compat_robust_list)) {		irq_set_handler_data(port->irq, port);
		compat_exit_robust_list(tsk);		irq_set_handler_data(port->irq, port);
		tsk->compat_robust_list = NULL;		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
	if (unlikely(!list_empty(&tsk->pi_state_list)))		irq_set_handler_data(port->irq, port);
		exit_pi_state_list(tsk);		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	uprobe_free_utask(tsk);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/* Get rid of any cached register state */		irq_set_handler_data(port->irq, port);
	deactivate_mm(tsk, mm);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * If we're exiting normally, clear a user-space tid field if		irq_set_handler_data(port->irq, port);
	 * requested.  We leave this alone when dying by signal, to leave		irq_set_handler_data(port->irq, port);
	 * the value intact in a core dump, and to save the unnecessary		irq_set_handler_data(port->irq, port);
	 * trouble, say, a killed vfork parent shouldn't touch this mm.		irq_set_handler_data(port->irq, port);
	 * Userland only wants this done for a sys_exit.		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	if (tsk->clear_child_tid) {		irq_set_handler_data(port->irq, port);
		if (!(tsk->flags & PF_SIGNALED) &&		irq_set_handler_data(port->irq, port);
		    atomic_read(&mm->mm_users) > 1) {		irq_set_handler_data(port->irq, port);
			/*		irq_set_handler_data(port->irq, port);
			 * We don't check the error code - if userspace has		irq_set_handler_data(port->irq, port);
			 * not set up a proper pointer then tough luck.		irq_set_handler_data(port->irq, port);
			 */		irq_set_handler_data(port->irq, port);
			put_user(0, tsk->clear_child_tid);		irq_set_handler_data(port->irq, port);
			sys_futex(tsk->clear_child_tid, FUTEX_WAKE,		irq_set_handler_data(port->irq, port);
					1, NULL, NULL, 0);		irq_set_handler_data(port->irq, port);
		}		irq_set_handler_data(port->irq, port);
		tsk->clear_child_tid = NULL;		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * All done, finally we can wake up parent and return this mm to him.		irq_set_handler_data(port->irq, port);
	 * Also kthread_stop() uses this completion for synchronization.		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	if (tsk->vfork_done)		irq_set_handler_data(port->irq, port);
		complete_vfork_done(tsk);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/*		irq_set_handler_data(port->irq, port);
 * Allocate a new mm structure and copy contents from the		irq_set_handler_data(port->irq, port);
 * mm structure of the passed in task structure.		irq_set_handler_data(port->irq, port);
 */		irq_set_handler_data(port->irq, port);
static struct mm_struct *dup_mm(struct task_struct *tsk)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	struct mm_struct *mm, *oldmm = current->mm;		irq_set_handler_data(port->irq, port);
	int err;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	mm = allocate_mm();		irq_set_handler_data(port->irq, port);
	if (!mm)		irq_set_handler_data(port->irq, port);
		goto fail_nomem;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	memcpy(mm, oldmm, sizeof(*mm));		irq_set_handler_data(port->irq, port);
	mm_init_cpumask(mm);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
#if defined(CONFIG_TRANSPARENT_HUGEPAGE) && !USE_SPLIT_PMD_PTLOCKS		irq_set_handler_data(port->irq, port);
	mm->pmd_huge_pte = NULL;		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
	if (!mm_init(mm, tsk))		irq_set_handler_data(port->irq, port);
		goto fail_nomem;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	if (init_new_context(tsk, mm))		irq_set_handler_data(port->irq, port);
		goto fail_nocontext;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	dup_mm_exe_file(oldmm, mm);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	err = dup_mmap(mm, oldmm);		irq_set_handler_data(port->irq, port);
	if (err)		irq_set_handler_data(port->irq, port);
		goto free_pt;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	mm->hiwater_rss = get_mm_rss(mm);		irq_set_handler_data(port->irq, port);
	mm->hiwater_vm = mm->total_vm;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	if (mm->binfmt && !try_module_get(mm->binfmt->module))		irq_set_handler_data(port->irq, port);
		goto free_pt;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	return mm;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
free_pt:		irq_set_handler_data(port->irq, port);
	/* don't put binfmt in mmput, we haven't got module yet */		irq_set_handler_data(port->irq, port);
	mm->binfmt = NULL;		irq_set_handler_data(port->irq, port);
	mmput(mm);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
fail_nomem:		irq_set_handler_data(port->irq, port);
	return NULL;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
fail_nocontext:		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * If init_new_context() failed, we cannot use mmput() to free the mm		irq_set_handler_data(port->irq, port);
	 * because it calls destroy_context()		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	mm_free_pgd(mm);		irq_set_handler_data(port->irq, port);
	free_mm(mm);		irq_set_handler_data(port->irq, port);
	return NULL;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static int copy_mm(unsigned long clone_flags, struct task_struct *tsk)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	struct mm_struct *mm, *oldmm;		irq_set_handler_data(port->irq, port);
	int retval;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	tsk->min_flt = tsk->maj_flt = 0;		irq_set_handler_data(port->irq, port);
	tsk->nvcsw = tsk->nivcsw = 0;		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_DETECT_HUNG_TASK		irq_set_handler_data(port->irq, port);
	tsk->last_switch_count = tsk->nvcsw + tsk->nivcsw;		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	tsk->mm = NULL;		irq_set_handler_data(port->irq, port);
	tsk->active_mm = NULL;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * Are we cloning a kernel thread?		irq_set_handler_data(port->irq, port);
	 *		irq_set_handler_data(port->irq, port);
	 * We need to steal a active VM for that..		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	oldmm = current->mm;		irq_set_handler_data(port->irq, port);
	if (!oldmm)		irq_set_handler_data(port->irq, port);
		return 0;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	if (clone_flags & CLONE_VM) {		irq_set_handler_data(port->irq, port);
		atomic_inc(&oldmm->mm_users);		irq_set_handler_data(port->irq, port);
		mm = oldmm;		irq_set_handler_data(port->irq, port);
		goto good_mm;		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	retval = -ENOMEM;		irq_set_handler_data(port->irq, port);
	mm = dup_mm(tsk);		irq_set_handler_data(port->irq, port);
	if (!mm)		irq_set_handler_data(port->irq, port);
		goto fail_nomem;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
good_mm:		irq_set_handler_data(port->irq, port);
	tsk->mm = mm;		irq_set_handler_data(port->irq, port);
	tsk->active_mm = mm;		irq_set_handler_data(port->irq, port);
	return 0;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
fail_nomem:		irq_set_handler_data(port->irq, port);
	return retval;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static int copy_fs(unsigned long clone_flags, struct task_struct *tsk)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	struct fs_struct *fs = current->fs;		irq_set_handler_data(port->irq, port);
	if (clone_flags & CLONE_FS) {		irq_set_handler_data(port->irq, port);
		/* tsk->fs is already what we want */		irq_set_handler_data(port->irq, port);
		spin_lock(&fs->lock);		irq_set_handler_data(port->irq, port);
		if (fs->in_exec) {		irq_set_handler_data(port->irq, port);
			spin_unlock(&fs->lock);		irq_set_handler_data(port->irq, port);
			return -EAGAIN;		irq_set_handler_data(port->irq, port);
		}		irq_set_handler_data(port->irq, port);
		fs->users++;		irq_set_handler_data(port->irq, port);
		spin_unlock(&fs->lock);		irq_set_handler_data(port->irq, port);
		return 0;		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
	tsk->fs = copy_fs_struct(fs);		irq_set_handler_data(port->irq, port);
	if (!tsk->fs)		irq_set_handler_data(port->irq, port);
		return -ENOMEM;		irq_set_handler_data(port->irq, port);
	return 0;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static int copy_files(unsigned long clone_flags, struct task_struct *tsk)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	struct files_struct *oldf, *newf;		irq_set_handler_data(port->irq, port);
	int error = 0;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * A background process may not have any files ...		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	oldf = current->files;		irq_set_handler_data(port->irq, port);
	if (!oldf)		irq_set_handler_data(port->irq, port);
		goto out;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	if (clone_flags & CLONE_FILES) {		irq_set_handler_data(port->irq, port);
		atomic_inc(&oldf->count);		irq_set_handler_data(port->irq, port);
		goto out;		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	newf = dup_fd(oldf, &error);		irq_set_handler_data(port->irq, port);
	if (!newf)		irq_set_handler_data(port->irq, port);
		goto out;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	tsk->files = newf;		irq_set_handler_data(port->irq, port);
	error = 0;		irq_set_handler_data(port->irq, port);
out:		irq_set_handler_data(port->irq, port);
	return error;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static int copy_io(unsigned long clone_flags, struct task_struct *tsk)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_BLOCK		irq_set_handler_data(port->irq, port);
	struct io_context *ioc = current->io_context;		irq_set_handler_data(port->irq, port);
	struct io_context *new_ioc;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	if (!ioc)		irq_set_handler_data(port->irq, port);
		return 0;		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * Share io context with parent, if CLONE_IO is set		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	if (clone_flags & CLONE_IO) {		irq_set_handler_data(port->irq, port);
		ioc_task_link(ioc);		irq_set_handler_data(port->irq, port);
		tsk->io_context = ioc;		irq_set_handler_data(port->irq, port);
	} else if (ioprio_valid(ioc->ioprio)) {		irq_set_handler_data(port->irq, port);
		new_ioc = get_task_io_context(tsk, GFP_KERNEL, NUMA_NO_NODE);		irq_set_handler_data(port->irq, port);
		if (unlikely(!new_ioc))		irq_set_handler_data(port->irq, port);
			return -ENOMEM;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		new_ioc->ioprio = ioc->ioprio;		irq_set_handler_data(port->irq, port);
		put_io_context(new_ioc);		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
	return 0;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static int copy_sighand(unsigned long clone_flags, struct task_struct *tsk)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	struct sighand_struct *sig;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	if (clone_flags & CLONE_SIGHAND) {		irq_set_handler_data(port->irq, port);
		atomic_inc(&current->sighand->count);		irq_set_handler_data(port->irq, port);
		return 0;		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
	sig = kmem_cache_alloc(sighand_cachep, GFP_KERNEL);		irq_set_handler_data(port->irq, port);
	rcu_assign_pointer(tsk->sighand, sig);		irq_set_handler_data(port->irq, port);
	if (!sig)		irq_set_handler_data(port->irq, port);
		return -ENOMEM;		irq_set_handler_data(port->irq, port);
	atomic_set(&sig->count, 1);		irq_set_handler_data(port->irq, port);
	memcpy(sig->action, current->sighand->action, sizeof(sig->action));		irq_set_handler_data(port->irq, port);
	return 0;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
void __cleanup_sighand(struct sighand_struct *sighand)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	if (atomic_dec_and_test(&sighand->count)) {		irq_set_handler_data(port->irq, port);
		signalfd_cleanup(sighand);		irq_set_handler_data(port->irq, port);
		kmem_cache_free(sighand_cachep, sighand);		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/*		irq_set_handler_data(port->irq, port);
 * Initialize POSIX timer handling for a thread group.		irq_set_handler_data(port->irq, port);
 */		irq_set_handler_data(port->irq, port);
static void posix_cpu_timers_init_group(struct signal_struct *sig)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	unsigned long cpu_limit;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/* Thread group counters. */		irq_set_handler_data(port->irq, port);
	thread_group_cputime_init(sig);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	cpu_limit = ACCESS_ONCE(sig->rlim[RLIMIT_CPU].rlim_cur);		irq_set_handler_data(port->irq, port);
	if (cpu_limit != RLIM_INFINITY) {		irq_set_handler_data(port->irq, port);
		sig->cputime_expires.prof_exp = secs_to_cputime(cpu_limit);		irq_set_handler_data(port->irq, port);
		sig->cputimer.running = 1;		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/* The timer lists. */		irq_set_handler_data(port->irq, port);
	INIT_LIST_HEAD(&sig->cpu_timers[0]);		irq_set_handler_data(port->irq, port);
	INIT_LIST_HEAD(&sig->cpu_timers[1]);		irq_set_handler_data(port->irq, port);
	INIT_LIST_HEAD(&sig->cpu_timers[2]);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static int copy_signal(unsigned long clone_flags, struct task_struct *tsk)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	struct signal_struct *sig;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	if (clone_flags & CLONE_THREAD)		irq_set_handler_data(port->irq, port);
		return 0;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	sig = kmem_cache_zalloc(signal_cachep, GFP_KERNEL);		irq_set_handler_data(port->irq, port);
	tsk->signal = sig;		irq_set_handler_data(port->irq, port);
	if (!sig)		irq_set_handler_data(port->irq, port);
		return -ENOMEM;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	sig->nr_threads = 1;		irq_set_handler_data(port->irq, port);
	atomic_set(&sig->live, 1);		irq_set_handler_data(port->irq, port);
	atomic_set(&sig->sigcnt, 1);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/* list_add(thread_node, thread_head) without INIT_LIST_HEAD() */		irq_set_handler_data(port->irq, port);
	sig->thread_head = (struct list_head)LIST_HEAD_INIT(tsk->thread_node);		irq_set_handler_data(port->irq, port);
	tsk->thread_node = (struct list_head)LIST_HEAD_INIT(sig->thread_head);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	init_waitqueue_head(&sig->wait_chldexit);		irq_set_handler_data(port->irq, port);
	sig->curr_target = tsk;		irq_set_handler_data(port->irq, port);
	init_sigpending(&sig->shared_pending);		irq_set_handler_data(port->irq, port);
	INIT_LIST_HEAD(&sig->posix_timers);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	hrtimer_init(&sig->real_timer, CLOCK_MONOTONIC, HRTIMER_MODE_REL);		irq_set_handler_data(port->irq, port);
	sig->real_timer.function = it_real_fn;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	task_lock(current->group_leader);		irq_set_handler_data(port->irq, port);
	memcpy(sig->rlim, current->signal->rlim, sizeof sig->rlim);		irq_set_handler_data(port->irq, port);
	task_unlock(current->group_leader);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	posix_cpu_timers_init_group(sig);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	tty_audit_fork(sig);		irq_set_handler_data(port->irq, port);
	sched_autogroup_fork(sig);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_CGROUPS		irq_set_handler_data(port->irq, port);
	init_rwsem(&sig->group_rwsem);		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	sig->oom_score_adj = current->signal->oom_score_adj;		irq_set_handler_data(port->irq, port);
	sig->oom_score_adj_min = current->signal->oom_score_adj_min;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	sig->has_child_subreaper = current->signal->has_child_subreaper ||		irq_set_handler_data(port->irq, port);
				   current->signal->is_child_subreaper;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	mutex_init(&sig->cred_guard_mutex);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	return 0;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static void copy_flags(unsigned long clone_flags, struct task_struct *p)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	unsigned long new_flags = p->flags;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	new_flags &= ~(PF_SUPERPRIV | PF_WQ_WORKER);		irq_set_handler_data(port->irq, port);
	new_flags |= PF_FORKNOEXEC;		irq_set_handler_data(port->irq, port);
	p->flags = new_flags;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
SYSCALL_DEFINE1(set_tid_address, int __user *, tidptr)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	current->clear_child_tid = tidptr;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	return task_pid_vnr(current);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static void rt_mutex_init_task(struct task_struct *p)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	raw_spin_lock_init(&p->pi_lock);		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_RT_MUTEXES		irq_set_handler_data(port->irq, port);
	p->pi_waiters = RB_ROOT;		irq_set_handler_data(port->irq, port);
	p->pi_waiters_leftmost = NULL;		irq_set_handler_data(port->irq, port);
	p->pi_blocked_on = NULL;		irq_set_handler_data(port->irq, port);
	p->pi_top_task = NULL;		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_MM_OWNER		irq_set_handler_data(port->irq, port);
void mm_init_owner(struct mm_struct *mm, struct task_struct *p)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	mm->owner = p;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
#endif /* CONFIG_MM_OWNER */		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/*		irq_set_handler_data(port->irq, port);
 * Initialize POSIX timer handling for a single task.		irq_set_handler_data(port->irq, port);
 */		irq_set_handler_data(port->irq, port);
static void posix_cpu_timers_init(struct task_struct *tsk)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	tsk->cputime_expires.prof_exp = 0;		irq_set_handler_data(port->irq, port);
	tsk->cputime_expires.virt_exp = 0;		irq_set_handler_data(port->irq, port);
	tsk->cputime_expires.sched_exp = 0;		irq_set_handler_data(port->irq, port);
	INIT_LIST_HEAD(&tsk->cpu_timers[0]);		irq_set_handler_data(port->irq, port);
	INIT_LIST_HEAD(&tsk->cpu_timers[1]);		irq_set_handler_data(port->irq, port);
	INIT_LIST_HEAD(&tsk->cpu_timers[2]);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static inline void		irq_set_handler_data(port->irq, port);
init_task_pid(struct task_struct *task, enum pid_type type, struct pid *pid)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	 task->pids[type].pid = pid;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/*		irq_set_handler_data(port->irq, port);
 * This creates a new process as a copy of the old one,		irq_set_handler_data(port->irq, port);
 * but does not actually start it yet.		irq_set_handler_data(port->irq, port);
 *		irq_set_handler_data(port->irq, port);
 * It copies the registers, and all the appropriate		irq_set_handler_data(port->irq, port);
 * parts of the process environment (as per the clone		irq_set_handler_data(port->irq, port);
 * flags). The actual kick-off is left to the caller.		irq_set_handler_data(port->irq, port);
 */		irq_set_handler_data(port->irq, port);
static struct task_struct *copy_process(unsigned long clone_flags,		irq_set_handler_data(port->irq, port);
					unsigned long stack_start,		irq_set_handler_data(port->irq, port);
					unsigned long stack_size,		irq_set_handler_data(port->irq, port);
					int __user *child_tidptr,		irq_set_handler_data(port->irq, port);
					struct pid *pid,		irq_set_handler_data(port->irq, port);
					int trace)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	int retval;		irq_set_handler_data(port->irq, port);
	struct task_struct *p;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	if ((clone_flags & (CLONE_NEWNS|CLONE_FS)) == (CLONE_NEWNS|CLONE_FS))		irq_set_handler_data(port->irq, port);
		return ERR_PTR(-EINVAL);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	if ((clone_flags & (CLONE_NEWUSER|CLONE_FS)) == (CLONE_NEWUSER|CLONE_FS))		irq_set_handler_data(port->irq, port);
		return ERR_PTR(-EINVAL);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * Thread groups must share signals as well, and detached threads		irq_set_handler_data(port->irq, port);
	 * can only be started up within the thread group.		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	if ((clone_flags & CLONE_THREAD) && !(clone_flags & CLONE_SIGHAND))		irq_set_handler_data(port->irq, port);
		return ERR_PTR(-EINVAL);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * Shared signal handlers imply shared VM. By way of the above,		irq_set_handler_data(port->irq, port);
	 * thread groups also imply shared VM. Blocking this case allows		irq_set_handler_data(port->irq, port);
	 * for various simplifications in other code.		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	if ((clone_flags & CLONE_SIGHAND) && !(clone_flags & CLONE_VM))		irq_set_handler_data(port->irq, port);
		return ERR_PTR(-EINVAL);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * Siblings of global init remain as zombies on exit since they are		irq_set_handler_data(port->irq, port);
	 * not reaped by their parent (swapper). To solve this and to avoid		irq_set_handler_data(port->irq, port);
	 * multi-rooted process trees, prevent global and container-inits		irq_set_handler_data(port->irq, port);
	 * from creating siblings.		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	if ((clone_flags & CLONE_PARENT) &&		irq_set_handler_data(port->irq, port);
				current->signal->flags & SIGNAL_UNKILLABLE)		irq_set_handler_data(port->irq, port);
		return ERR_PTR(-EINVAL);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * If the new process will be in a different pid or user namespace		irq_set_handler_data(port->irq, port);
	 * do not allow it to share a thread group or signal handlers or		irq_set_handler_data(port->irq, port);
	 * parent with the forking task.		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	if (clone_flags & CLONE_SIGHAND) {		irq_set_handler_data(port->irq, port);
		if ((clone_flags & (CLONE_NEWUSER | CLONE_NEWPID)) ||		irq_set_handler_data(port->irq, port);
		    (task_active_pid_ns(current) !=		irq_set_handler_data(port->irq, port);
				current->nsproxy->pid_ns_for_children))		irq_set_handler_data(port->irq, port);
			return ERR_PTR(-EINVAL);		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	retval = security_task_create(clone_flags);		irq_set_handler_data(port->irq, port);
	if (retval)		irq_set_handler_data(port->irq, port);
		goto fork_out;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	retval = -ENOMEM;		irq_set_handler_data(port->irq, port);
	p = dup_task_struct(current);		irq_set_handler_data(port->irq, port);
	if (!p)		irq_set_handler_data(port->irq, port);
		goto fork_out;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	ftrace_graph_init_task(p);		irq_set_handler_data(port->irq, port);
	get_seccomp_filter(p);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	rt_mutex_init_task(p);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_PROVE_LOCKING		irq_set_handler_data(port->irq, port);
	DEBUG_LOCKS_WARN_ON(!p->hardirqs_enabled);		irq_set_handler_data(port->irq, port);
	DEBUG_LOCKS_WARN_ON(!p->softirqs_enabled);		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
	retval = -EAGAIN;		irq_set_handler_data(port->irq, port);
	if (atomic_read(&p->real_cred->user->processes) >=		irq_set_handler_data(port->irq, port);
			task_rlimit(p, RLIMIT_NPROC)) {		irq_set_handler_data(port->irq, port);
		if (p->real_cred->user != INIT_USER &&		irq_set_handler_data(port->irq, port);
		    !capable(CAP_SYS_RESOURCE) && !capable(CAP_SYS_ADMIN))		irq_set_handler_data(port->irq, port);
			goto bad_fork_free;		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
	current->flags &= ~PF_NPROC_EXCEEDED;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	retval = copy_creds(p, clone_flags);		irq_set_handler_data(port->irq, port);
	if (retval < 0)		irq_set_handler_data(port->irq, port);
		goto bad_fork_free;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * If multiple threads are within copy_process(), then this check		irq_set_handler_data(port->irq, port);
	 * triggers too late. This doesn't hurt, the check is only there		irq_set_handler_data(port->irq, port);
	 * to stop root fork bombs.		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	retval = -EAGAIN;		irq_set_handler_data(port->irq, port);
	if (nr_threads >= max_threads)		irq_set_handler_data(port->irq, port);
		goto bad_fork_cleanup_count;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	if (!try_module_get(task_thread_info(p)->exec_domain->module))		irq_set_handler_data(port->irq, port);
		goto bad_fork_cleanup_count;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	delayacct_tsk_init(p);	/* Must remain after dup_task_struct() */		irq_set_handler_data(port->irq, port);
	copy_flags(clone_flags, p);		irq_set_handler_data(port->irq, port);
	INIT_LIST_HEAD(&p->children);		irq_set_handler_data(port->irq, port);
	INIT_LIST_HEAD(&p->sibling);		irq_set_handler_data(port->irq, port);
	rcu_copy_process(p);		irq_set_handler_data(port->irq, port);
	p->vfork_done = NULL;		irq_set_handler_data(port->irq, port);
	spin_lock_init(&p->alloc_lock);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	init_sigpending(&p->pending);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	p->utime = p->stime = p->gtime = 0;		irq_set_handler_data(port->irq, port);
	p->utimescaled = p->stimescaled = 0;		irq_set_handler_data(port->irq, port);
#ifndef CONFIG_VIRT_CPU_ACCOUNTING_NATIVE		irq_set_handler_data(port->irq, port);
	p->prev_cputime.utime = p->prev_cputime.stime = 0;		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_VIRT_CPU_ACCOUNTING_GEN		irq_set_handler_data(port->irq, port);
	seqlock_init(&p->vtime_seqlock);		irq_set_handler_data(port->irq, port);
	p->vtime_snap = 0;		irq_set_handler_data(port->irq, port);
	p->vtime_snap_whence = VTIME_SLEEPING;		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
#if defined(SPLIT_RSS_COUNTING)		irq_set_handler_data(port->irq, port);
	memset(&p->rss_stat, 0, sizeof(p->rss_stat));		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	p->default_timer_slack_ns = current->timer_slack_ns;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	task_io_accounting_init(&p->ioac);		irq_set_handler_data(port->irq, port);
	acct_clear_integrals(p);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	posix_cpu_timers_init(p);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	do_posix_clock_monotonic_gettime(&p->start_time);		irq_set_handler_data(port->irq, port);
	p->real_start_time = p->start_time;		irq_set_handler_data(port->irq, port);
	monotonic_to_bootbased(&p->real_start_time);		irq_set_handler_data(port->irq, port);
	p->io_context = NULL;		irq_set_handler_data(port->irq, port);
	p->audit_context = NULL;		irq_set_handler_data(port->irq, port);
	if (clone_flags & CLONE_THREAD)		irq_set_handler_data(port->irq, port);
		threadgroup_change_begin(current);		irq_set_handler_data(port->irq, port);
	cgroup_fork(p);		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_NUMA		irq_set_handler_data(port->irq, port);
	p->mempolicy = mpol_dup(p->mempolicy);		irq_set_handler_data(port->irq, port);
	if (IS_ERR(p->mempolicy)) {		irq_set_handler_data(port->irq, port);
		retval = PTR_ERR(p->mempolicy);		irq_set_handler_data(port->irq, port);
		p->mempolicy = NULL;		irq_set_handler_data(port->irq, port);
		goto bad_fork_cleanup_cgroup;		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
	mpol_fix_fork_child_flag(p);		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_CPUSETS		irq_set_handler_data(port->irq, port);
	p->cpuset_mem_spread_rotor = NUMA_NO_NODE;		irq_set_handler_data(port->irq, port);
	p->cpuset_slab_spread_rotor = NUMA_NO_NODE;		irq_set_handler_data(port->irq, port);
	seqcount_init(&p->mems_allowed_seq);		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_TRACE_IRQFLAGS		irq_set_handler_data(port->irq, port);
	p->irq_events = 0;		irq_set_handler_data(port->irq, port);
	p->hardirqs_enabled = 0;		irq_set_handler_data(port->irq, port);
	p->hardirq_enable_ip = 0;		irq_set_handler_data(port->irq, port);
	p->hardirq_enable_event = 0;		irq_set_handler_data(port->irq, port);
	p->hardirq_disable_ip = _THIS_IP_;		irq_set_handler_data(port->irq, port);
	p->hardirq_disable_event = 0;		irq_set_handler_data(port->irq, port);
	p->softirqs_enabled = 1;		irq_set_handler_data(port->irq, port);
	p->softirq_enable_ip = _THIS_IP_;		irq_set_handler_data(port->irq, port);
	p->softirq_enable_event = 0;		irq_set_handler_data(port->irq, port);
	p->softirq_disable_ip = 0;		irq_set_handler_data(port->irq, port);
	p->softirq_disable_event = 0;		irq_set_handler_data(port->irq, port);
	p->hardirq_context = 0;		irq_set_handler_data(port->irq, port);
	p->softirq_context = 0;		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_LOCKDEP		irq_set_handler_data(port->irq, port);
	p->lockdep_depth = 0; /* no locks held yet */		irq_set_handler_data(port->irq, port);
	p->curr_chain_key = 0;		irq_set_handler_data(port->irq, port);
	p->lockdep_recursion = 0;		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_DEBUG_MUTEXES		irq_set_handler_data(port->irq, port);
	p->blocked_on = NULL; /* not blocked yet */		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_MEMCG		irq_set_handler_data(port->irq, port);
	p->memcg_batch.do_batch = 0;		irq_set_handler_data(port->irq, port);
	p->memcg_batch.memcg = NULL;		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_BCACHE		irq_set_handler_data(port->irq, port);
	p->sequential_io	= 0;		irq_set_handler_data(port->irq, port);
	p->sequential_io_avg	= 0;		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/* Perform scheduler related setup. Assign this task to a CPU. */		irq_set_handler_data(port->irq, port);
	retval = sched_fork(clone_flags, p);		irq_set_handler_data(port->irq, port);
	if (retval)		irq_set_handler_data(port->irq, port);
		goto bad_fork_cleanup_policy;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	retval = perf_event_init_task(p);		irq_set_handler_data(port->irq, port);
	if (retval)		irq_set_handler_data(port->irq, port);
		goto bad_fork_cleanup_policy;		irq_set_handler_data(port->irq, port);
	retval = audit_alloc(p);		irq_set_handler_data(port->irq, port);
	if (retval)		irq_set_handler_data(port->irq, port);
		goto bad_fork_cleanup_policy;		irq_set_handler_data(port->irq, port);
	/* copy all the process information */		irq_set_handler_data(port->irq, port);
	retval = copy_semundo(clone_flags, p);		irq_set_handler_data(port->irq, port);
	if (retval)		irq_set_handler_data(port->irq, port);
		goto bad_fork_cleanup_audit;		irq_set_handler_data(port->irq, port);
	retval = copy_files(clone_flags, p);		irq_set_handler_data(port->irq, port);
	if (retval)		irq_set_handler_data(port->irq, port);
		goto bad_fork_cleanup_semundo;		irq_set_handler_data(port->irq, port);
	retval = copy_fs(clone_flags, p);		irq_set_handler_data(port->irq, port);
	if (retval)		irq_set_handler_data(port->irq, port);
		goto bad_fork_cleanup_files;		irq_set_handler_data(port->irq, port);
	retval = copy_sighand(clone_flags, p);		irq_set_handler_data(port->irq, port);
	if (retval)		irq_set_handler_data(port->irq, port);
		goto bad_fork_cleanup_fs;		irq_set_handler_data(port->irq, port);
	retval = copy_signal(clone_flags, p);		irq_set_handler_data(port->irq, port);
	if (retval)		irq_set_handler_data(port->irq, port);
		goto bad_fork_cleanup_sighand;		irq_set_handler_data(port->irq, port);
	retval = copy_mm(clone_flags, p);		irq_set_handler_data(port->irq, port);
	if (retval)		irq_set_handler_data(port->irq, port);
		goto bad_fork_cleanup_signal;		irq_set_handler_data(port->irq, port);
	retval = copy_namespaces(clone_flags, p);		irq_set_handler_data(port->irq, port);
	if (retval)		irq_set_handler_data(port->irq, port);
		goto bad_fork_cleanup_mm;		irq_set_handler_data(port->irq, port);
	retval = copy_io(clone_flags, p);		irq_set_handler_data(port->irq, port);
	if (retval)		irq_set_handler_data(port->irq, port);
		goto bad_fork_cleanup_namespaces;		irq_set_handler_data(port->irq, port);
	retval = copy_thread(clone_flags, stack_start, stack_size, p);		irq_set_handler_data(port->irq, port);
	if (retval)		irq_set_handler_data(port->irq, port);
		goto bad_fork_cleanup_io;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	if (pid != &init_struct_pid) {		irq_set_handler_data(port->irq, port);
		retval = -ENOMEM;		irq_set_handler_data(port->irq, port);
		pid = alloc_pid(p->nsproxy->pid_ns_for_children);		irq_set_handler_data(port->irq, port);
		if (!pid)		irq_set_handler_data(port->irq, port);
			goto bad_fork_cleanup_io;		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	p->set_child_tid = (clone_flags & CLONE_CHILD_SETTID) ? child_tidptr : NULL;		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * Clear TID on mm_release()?		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	p->clear_child_tid = (clone_flags & CLONE_CHILD_CLEARTID) ? child_tidptr : NULL;		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_BLOCK		irq_set_handler_data(port->irq, port);
	p->plug = NULL;		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_FUTEX		irq_set_handler_data(port->irq, port);
	p->robust_list = NULL;		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_COMPAT		irq_set_handler_data(port->irq, port);
	p->compat_robust_list = NULL;		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
	INIT_LIST_HEAD(&p->pi_state_list);		irq_set_handler_data(port->irq, port);
	p->pi_state_cache = NULL;		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * sigaltstack should be cleared when sharing the same VM		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	if ((clone_flags & (CLONE_VM|CLONE_VFORK)) == CLONE_VM)		irq_set_handler_data(port->irq, port);
		p->sas_ss_sp = p->sas_ss_size = 0;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * Syscall tracing and stepping should be turned off in the		irq_set_handler_data(port->irq, port);
	 * child regardless of CLONE_PTRACE.		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	user_disable_single_step(p);		irq_set_handler_data(port->irq, port);
	clear_tsk_thread_flag(p, TIF_SYSCALL_TRACE);		irq_set_handler_data(port->irq, port);
#ifdef TIF_SYSCALL_EMU		irq_set_handler_data(port->irq, port);
	clear_tsk_thread_flag(p, TIF_SYSCALL_EMU);		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
	clear_all_latency_tracing(p);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/* ok, now we should be set up.. */		irq_set_handler_data(port->irq, port);
	p->pid = pid_nr(pid);		irq_set_handler_data(port->irq, port);
	if (clone_flags & CLONE_THREAD) {		irq_set_handler_data(port->irq, port);
		p->exit_signal = -1;		irq_set_handler_data(port->irq, port);
		p->group_leader = current->group_leader;		irq_set_handler_data(port->irq, port);
		p->tgid = current->tgid;		irq_set_handler_data(port->irq, port);
	} else {		irq_set_handler_data(port->irq, port);
		if (clone_flags & CLONE_PARENT)		irq_set_handler_data(port->irq, port);
			p->exit_signal = current->group_leader->exit_signal;		irq_set_handler_data(port->irq, port);
		else		irq_set_handler_data(port->irq, port);
			p->exit_signal = (clone_flags & CSIGNAL);		irq_set_handler_data(port->irq, port);
		p->group_leader = p;		irq_set_handler_data(port->irq, port);
		p->tgid = p->pid;		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	p->nr_dirtied = 0;		irq_set_handler_data(port->irq, port);
	p->nr_dirtied_pause = 128 >> (PAGE_SHIFT - 10);		irq_set_handler_data(port->irq, port);
	p->dirty_paused_when = 0;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	p->pdeath_signal = 0;		irq_set_handler_data(port->irq, port);
	INIT_LIST_HEAD(&p->thread_group);		irq_set_handler_data(port->irq, port);
	p->task_works = NULL;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * Make it visible to the rest of the system, but dont wake it up yet.		irq_set_handler_data(port->irq, port);
	 * Need tasklist lock for parent etc handling!		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	write_lock_irq(&tasklist_lock);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/* CLONE_PARENT re-uses the old parent */		irq_set_handler_data(port->irq, port);
	if (clone_flags & (CLONE_PARENT|CLONE_THREAD)) {		irq_set_handler_data(port->irq, port);
		p->real_parent = current->real_parent;		irq_set_handler_data(port->irq, port);
		p->parent_exec_id = current->parent_exec_id;		irq_set_handler_data(port->irq, port);
	} else {		irq_set_handler_data(port->irq, port);
		p->real_parent = current;		irq_set_handler_data(port->irq, port);
		p->parent_exec_id = current->self_exec_id;		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	spin_lock(&current->sighand->siglock);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * Process group and session signals need to be delivered to just the		irq_set_handler_data(port->irq, port);
	 * parent before the fork or both the parent and the child after the		irq_set_handler_data(port->irq, port);
	 * fork. Restart if a signal comes in before we add the new process to		irq_set_handler_data(port->irq, port);
	 * it's process group.		irq_set_handler_data(port->irq, port);
	 * A fatal signal pending means that current will exit, so the new		irq_set_handler_data(port->irq, port);
	 * thread can't slip out of an OOM kill (or normal SIGKILL).		irq_set_handler_data(port->irq, port);
	*/		irq_set_handler_data(port->irq, port);
	recalc_sigpending();		irq_set_handler_data(port->irq, port);
	if (signal_pending(current)) {		irq_set_handler_data(port->irq, port);
		spin_unlock(&current->sighand->siglock);		irq_set_handler_data(port->irq, port);
		write_unlock_irq(&tasklist_lock);		irq_set_handler_data(port->irq, port);
		retval = -ERESTARTNOINTR;		irq_set_handler_data(port->irq, port);
		goto bad_fork_free_pid;		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	if (likely(p->pid)) {		irq_set_handler_data(port->irq, port);
		ptrace_init_task(p, (clone_flags & CLONE_PTRACE) || trace);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		init_task_pid(p, PIDTYPE_PID, pid);		irq_set_handler_data(port->irq, port);
		if (thread_group_leader(p)) {		irq_set_handler_data(port->irq, port);
			init_task_pid(p, PIDTYPE_PGID, task_pgrp(current));		irq_set_handler_data(port->irq, port);
			init_task_pid(p, PIDTYPE_SID, task_session(current));		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
			if (is_child_reaper(pid)) {		irq_set_handler_data(port->irq, port);
				ns_of_pid(pid)->child_reaper = p;		irq_set_handler_data(port->irq, port);
				p->signal->flags |= SIGNAL_UNKILLABLE;		irq_set_handler_data(port->irq, port);
			}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
			p->signal->leader_pid = pid;		irq_set_handler_data(port->irq, port);
			p->signal->tty = tty_kref_get(current->signal->tty);		irq_set_handler_data(port->irq, port);
			list_add_tail(&p->sibling, &p->real_parent->children);		irq_set_handler_data(port->irq, port);
			list_add_tail_rcu(&p->tasks, &init_task.tasks);		irq_set_handler_data(port->irq, port);
			attach_pid(p, PIDTYPE_PGID);		irq_set_handler_data(port->irq, port);
			attach_pid(p, PIDTYPE_SID);		irq_set_handler_data(port->irq, port);
			__this_cpu_inc(process_counts);		irq_set_handler_data(port->irq, port);
		} else {		irq_set_handler_data(port->irq, port);
			current->signal->nr_threads++;		irq_set_handler_data(port->irq, port);
			atomic_inc(&current->signal->live);		irq_set_handler_data(port->irq, port);
			atomic_inc(&current->signal->sigcnt);		irq_set_handler_data(port->irq, port);
			list_add_tail_rcu(&p->thread_group,		irq_set_handler_data(port->irq, port);
					  &p->group_leader->thread_group);		irq_set_handler_data(port->irq, port);
			list_add_tail_rcu(&p->thread_node,		irq_set_handler_data(port->irq, port);
					  &p->signal->thread_head);		irq_set_handler_data(port->irq, port);
		}		irq_set_handler_data(port->irq, port);
		attach_pid(p, PIDTYPE_PID);		irq_set_handler_data(port->irq, port);
		nr_threads++;		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	total_forks++;		irq_set_handler_data(port->irq, port);
	spin_unlock(&current->sighand->siglock);		irq_set_handler_data(port->irq, port);
	write_unlock_irq(&tasklist_lock);		irq_set_handler_data(port->irq, port);
	proc_fork_connector(p);		irq_set_handler_data(port->irq, port);
	cgroup_post_fork(p);		irq_set_handler_data(port->irq, port);
	if (clone_flags & CLONE_THREAD)		irq_set_handler_data(port->irq, port);
		threadgroup_change_end(current);		irq_set_handler_data(port->irq, port);
	perf_event_fork(p);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	trace_task_newtask(p, clone_flags);		irq_set_handler_data(port->irq, port);
	uprobe_copy_process(p, clone_flags);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	return p;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
bad_fork_free_pid:		irq_set_handler_data(port->irq, port);
	if (pid != &init_struct_pid)		irq_set_handler_data(port->irq, port);
		free_pid(pid);		irq_set_handler_data(port->irq, port);
bad_fork_cleanup_io:		irq_set_handler_data(port->irq, port);
	if (p->io_context)		irq_set_handler_data(port->irq, port);
		exit_io_context(p);		irq_set_handler_data(port->irq, port);
bad_fork_cleanup_namespaces:		irq_set_handler_data(port->irq, port);
	exit_task_namespaces(p);		irq_set_handler_data(port->irq, port);
bad_fork_cleanup_mm:		irq_set_handler_data(port->irq, port);
	if (p->mm)		irq_set_handler_data(port->irq, port);
		mmput(p->mm);		irq_set_handler_data(port->irq, port);
bad_fork_cleanup_signal:		irq_set_handler_data(port->irq, port);
	if (!(clone_flags & CLONE_THREAD))		irq_set_handler_data(port->irq, port);
		free_signal_struct(p->signal);		irq_set_handler_data(port->irq, port);
bad_fork_cleanup_sighand:		irq_set_handler_data(port->irq, port);
	__cleanup_sighand(p->sighand);		irq_set_handler_data(port->irq, port);
bad_fork_cleanup_fs:		irq_set_handler_data(port->irq, port);
	exit_fs(p); /* blocking */		irq_set_handler_data(port->irq, port);
bad_fork_cleanup_files:		irq_set_handler_data(port->irq, port);
	exit_files(p); /* blocking */		irq_set_handler_data(port->irq, port);
bad_fork_cleanup_semundo:		irq_set_handler_data(port->irq, port);
	exit_sem(p);		irq_set_handler_data(port->irq, port);
bad_fork_cleanup_audit:		irq_set_handler_data(port->irq, port);
	audit_free(p);		irq_set_handler_data(port->irq, port);
bad_fork_cleanup_policy:		irq_set_handler_data(port->irq, port);
	perf_event_free_task(p);		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_NUMA		irq_set_handler_data(port->irq, port);
	mpol_put(p->mempolicy);		irq_set_handler_data(port->irq, port);
bad_fork_cleanup_cgroup:		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
	if (clone_flags & CLONE_THREAD)		irq_set_handler_data(port->irq, port);
		threadgroup_change_end(current);		irq_set_handler_data(port->irq, port);
	cgroup_exit(p, 0);		irq_set_handler_data(port->irq, port);
	delayacct_tsk_free(p);		irq_set_handler_data(port->irq, port);
	module_put(task_thread_info(p)->exec_domain->module);		irq_set_handler_data(port->irq, port);
bad_fork_cleanup_count:		irq_set_handler_data(port->irq, port);
	atomic_dec(&p->cred->user->processes);		irq_set_handler_data(port->irq, port);
	exit_creds(p);		irq_set_handler_data(port->irq, port);
bad_fork_free:		irq_set_handler_data(port->irq, port);
	free_task(p);		irq_set_handler_data(port->irq, port);
fork_out:		irq_set_handler_data(port->irq, port);
	return ERR_PTR(retval);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static inline void init_idle_pids(struct pid_link *links)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	enum pid_type type;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	for (type = PIDTYPE_PID; type < PIDTYPE_MAX; ++type) {		irq_set_handler_data(port->irq, port);
		INIT_HLIST_NODE(&links[type].node); /* not really needed */		irq_set_handler_data(port->irq, port);
		links[type].pid = &init_struct_pid;		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
struct task_struct *fork_idle(int cpu)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	struct task_struct *task;		irq_set_handler_data(port->irq, port);
	task = copy_process(CLONE_VM, 0, 0, NULL, &init_struct_pid, 0);		irq_set_handler_data(port->irq, port);
	if (!IS_ERR(task)) {		irq_set_handler_data(port->irq, port);
		init_idle_pids(task->pids);		irq_set_handler_data(port->irq, port);
		init_idle(task, cpu);		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	return task;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/*		irq_set_handler_data(port->irq, port);
 *  Ok, this is the main fork-routine.		irq_set_handler_data(port->irq, port);
 *		irq_set_handler_data(port->irq, port);
 * It copies the process, and if successful kick-starts		irq_set_handler_data(port->irq, port);
 * it and waits for it to finish using the VM if required.		irq_set_handler_data(port->irq, port);
 */		irq_set_handler_data(port->irq, port);
long do_fork(unsigned long clone_flags,		irq_set_handler_data(port->irq, port);
	      unsigned long stack_start,		irq_set_handler_data(port->irq, port);
	      unsigned long stack_size,		irq_set_handler_data(port->irq, port);
	      int __user *parent_tidptr,		irq_set_handler_data(port->irq, port);
	      int __user *child_tidptr)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	struct task_struct *p;		irq_set_handler_data(port->irq, port);
	int trace = 0;		irq_set_handler_data(port->irq, port);
	long nr;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * Determine whether and which event to report to ptracer.  When		irq_set_handler_data(port->irq, port);
	 * called from kernel_thread or CLONE_UNTRACED is explicitly		irq_set_handler_data(port->irq, port);
	 * requested, no event is reported; otherwise, report if the event		irq_set_handler_data(port->irq, port);
	 * for the type of forking is enabled.		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	if (!(clone_flags & CLONE_UNTRACED)) {		irq_set_handler_data(port->irq, port);
		if (clone_flags & CLONE_VFORK)		irq_set_handler_data(port->irq, port);
			trace = PTRACE_EVENT_VFORK;		irq_set_handler_data(port->irq, port);
		else if ((clone_flags & CSIGNAL) != SIGCHLD)		irq_set_handler_data(port->irq, port);
			trace = PTRACE_EVENT_CLONE;		irq_set_handler_data(port->irq, port);
		else		irq_set_handler_data(port->irq, port);
			trace = PTRACE_EVENT_FORK;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		if (likely(!ptrace_event_enabled(current, trace)))		irq_set_handler_data(port->irq, port);
			trace = 0;		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	p = copy_process(clone_flags, stack_start, stack_size,		irq_set_handler_data(port->irq, port);
			 child_tidptr, NULL, trace);		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * Do this prior waking up the new thread - the thread pointer		irq_set_handler_data(port->irq, port);
	 * might get invalid after that point, if the thread exits quickly.		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	if (!IS_ERR(p)) {		irq_set_handler_data(port->irq, port);
		struct completion vfork;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		trace_sched_process_fork(current, p);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		nr = task_pid_vnr(p);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		if (clone_flags & CLONE_PARENT_SETTID)		irq_set_handler_data(port->irq, port);
			put_user(nr, parent_tidptr);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		if (clone_flags & CLONE_VFORK) {		irq_set_handler_data(port->irq, port);
			p->vfork_done = &vfork;		irq_set_handler_data(port->irq, port);
			init_completion(&vfork);		irq_set_handler_data(port->irq, port);
			get_task_struct(p);		irq_set_handler_data(port->irq, port);
		}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		wake_up_new_task(p);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		/* forking complete and child started to run, tell ptracer */		irq_set_handler_data(port->irq, port);
		if (unlikely(trace))		irq_set_handler_data(port->irq, port);
			ptrace_event(trace, nr);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		if (clone_flags & CLONE_VFORK) {		irq_set_handler_data(port->irq, port);
			if (!wait_for_vfork_done(p, &vfork))		irq_set_handler_data(port->irq, port);
				ptrace_event(PTRACE_EVENT_VFORK_DONE, nr);		irq_set_handler_data(port->irq, port);
		}		irq_set_handler_data(port->irq, port);
	} else {		irq_set_handler_data(port->irq, port);
		nr = PTR_ERR(p);		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
	return nr;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/*		irq_set_handler_data(port->irq, port);
 * Create a kernel thread.		irq_set_handler_data(port->irq, port);
 */		irq_set_handler_data(port->irq, port);
pid_t kernel_thread(int (*fn)(void *), void *arg, unsigned long flags)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	return do_fork(flags|CLONE_VM|CLONE_UNTRACED, (unsigned long)fn,		irq_set_handler_data(port->irq, port);
		(unsigned long)arg, NULL, NULL);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
#ifdef __ARCH_WANT_SYS_FORK		irq_set_handler_data(port->irq, port);
SYSCALL_DEFINE0(fork)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_MMU		irq_set_handler_data(port->irq, port);
	return do_fork(SIGCHLD, 0, 0, NULL, NULL);		irq_set_handler_data(port->irq, port);
#else		irq_set_handler_data(port->irq, port);
	/* can not support in nommu mode */		irq_set_handler_data(port->irq, port);
	return -EINVAL;		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
#ifdef __ARCH_WANT_SYS_VFORK		irq_set_handler_data(port->irq, port);
SYSCALL_DEFINE0(vfork)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	return do_fork(CLONE_VFORK | CLONE_VM | SIGCHLD, 0,		irq_set_handler_data(port->irq, port);
			0, NULL, NULL);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
#ifdef __ARCH_WANT_SYS_CLONE		irq_set_handler_data(port->irq, port);
#ifdef CONFIG_CLONE_BACKWARDS		irq_set_handler_data(port->irq, port);
SYSCALL_DEFINE5(clone, unsigned long, clone_flags, unsigned long, newsp,		irq_set_handler_data(port->irq, port);
		 int __user *, parent_tidptr,		irq_set_handler_data(port->irq, port);
		 int, tls_val,		irq_set_handler_data(port->irq, port);
		 int __user *, child_tidptr)		irq_set_handler_data(port->irq, port);
#elif defined(CONFIG_CLONE_BACKWARDS2)		irq_set_handler_data(port->irq, port);
SYSCALL_DEFINE5(clone, unsigned long, newsp, unsigned long, clone_flags,		irq_set_handler_data(port->irq, port);
		 int __user *, parent_tidptr,		irq_set_handler_data(port->irq, port);
		 int __user *, child_tidptr,		irq_set_handler_data(port->irq, port);
		 int, tls_val)		irq_set_handler_data(port->irq, port);
#elif defined(CONFIG_CLONE_BACKWARDS3)		irq_set_handler_data(port->irq, port);
SYSCALL_DEFINE6(clone, unsigned long, clone_flags, unsigned long, newsp,		irq_set_handler_data(port->irq, port);
		int, stack_size,		irq_set_handler_data(port->irq, port);
		int __user *, parent_tidptr,		irq_set_handler_data(port->irq, port);
		int __user *, child_tidptr,		irq_set_handler_data(port->irq, port);
		int, tls_val)		irq_set_handler_data(port->irq, port);
#else		irq_set_handler_data(port->irq, port);
SYSCALL_DEFINE5(clone, unsigned long, clone_flags, unsigned long, newsp,		irq_set_handler_data(port->irq, port);
		 int __user *, parent_tidptr,		irq_set_handler_data(port->irq, port);
		 int __user *, child_tidptr,		irq_set_handler_data(port->irq, port);
		 int, tls_val)		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	return do_fork(clone_flags, newsp, 0, parent_tidptr, child_tidptr);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
#ifndef ARCH_MIN_MMSTRUCT_ALIGN		irq_set_handler_data(port->irq, port);
#define ARCH_MIN_MMSTRUCT_ALIGN 0		irq_set_handler_data(port->irq, port);
#endif		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
static void sighand_ctor(void *data)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	struct sighand_struct *sighand = data;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	spin_lock_init(&sighand->siglock);		irq_set_handler_data(port->irq, port);
	init_waitqueue_head(&sighand->signalfd_wqh);		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
void __init proc_caches_init(void)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	sighand_cachep = kmem_cache_create("sighand_cache",		irq_set_handler_data(port->irq, port);
			sizeof(struct sighand_struct), 0,		irq_set_handler_data(port->irq, port);
			SLAB_HWCACHE_ALIGN|SLAB_PANIC|SLAB_DESTROY_BY_RCU|		irq_set_handler_data(port->irq, port);
			SLAB_NOTRACK, sighand_ctor);		irq_set_handler_data(port->irq, port);
	signal_cachep = kmem_cache_create("signal_cache",		irq_set_handler_data(port->irq, port);
			sizeof(struct signal_struct), 0,		irq_set_handler_data(port->irq, port);
			SLAB_HWCACHE_ALIGN|SLAB_PANIC|SLAB_NOTRACK, NULL);		irq_set_handler_data(port->irq, port);
	files_cachep = kmem_cache_create("files_cache",		irq_set_handler_data(port->irq, port);
			sizeof(struct files_struct), 0,		irq_set_handler_data(port->irq, port);
			SLAB_HWCACHE_ALIGN|SLAB_PANIC|SLAB_NOTRACK, NULL);		irq_set_handler_data(port->irq, port);
	fs_cachep = kmem_cache_create("fs_cache",		irq_set_handler_data(port->irq, port);
			sizeof(struct fs_struct), 0,		irq_set_handler_data(port->irq, port);
			SLAB_HWCACHE_ALIGN|SLAB_PANIC|SLAB_NOTRACK, NULL);		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * FIXME! The "sizeof(struct mm_struct)" currently includes the		irq_set_handler_data(port->irq, port);
	 * whole struct cpumask for the OFFSTACK case. We could change		irq_set_handler_data(port->irq, port);
	 * this to *only* allocate as much of it as required by the		irq_set_handler_data(port->irq, port);
	 * maximum number of CPU's we can ever have.  The cpumask_allocation		irq_set_handler_data(port->irq, port);
	 * is at the end of the structure, exactly for that reason.		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	mm_cachep = kmem_cache_create("mm_struct",		irq_set_handler_data(port->irq, port);
			sizeof(struct mm_struct), ARCH_MIN_MMSTRUCT_ALIGN,		irq_set_handler_data(port->irq, port);
			SLAB_HWCACHE_ALIGN|SLAB_PANIC|SLAB_NOTRACK, NULL);		irq_set_handler_data(port->irq, port);
	vm_area_cachep = KMEM_CACHE(vm_area_struct, SLAB_PANIC);		irq_set_handler_data(port->irq, port);
	mmap_init();		irq_set_handler_data(port->irq, port);
	nsproxy_cache_init();		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/*		irq_set_handler_data(port->irq, port);
 * Check constraints on flags passed to the unshare system call.		irq_set_handler_data(port->irq, port);
 */		irq_set_handler_data(port->irq, port);
static int check_unshare_flags(unsigned long unshare_flags)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	if (unshare_flags & ~(CLONE_THREAD|CLONE_FS|CLONE_NEWNS|CLONE_SIGHAND|		irq_set_handler_data(port->irq, port);
				CLONE_VM|CLONE_FILES|CLONE_SYSVSEM|		irq_set_handler_data(port->irq, port);
				CLONE_NEWUTS|CLONE_NEWIPC|CLONE_NEWNET|		irq_set_handler_data(port->irq, port);
				CLONE_NEWUSER|CLONE_NEWPID))		irq_set_handler_data(port->irq, port);
		return -EINVAL;		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * Not implemented, but pretend it works if there is nothing to		irq_set_handler_data(port->irq, port);
	 * unshare. Note that unsharing CLONE_THREAD or CLONE_SIGHAND		irq_set_handler_data(port->irq, port);
	 * needs to unshare vm.		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	if (unshare_flags & (CLONE_THREAD | CLONE_SIGHAND | CLONE_VM)) {		irq_set_handler_data(port->irq, port);
		/* FIXME: get_task_mm() increments ->mm_users */		irq_set_handler_data(port->irq, port);
		if (atomic_read(&current->mm->mm_users) > 1)		irq_set_handler_data(port->irq, port);
			return -EINVAL;		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	return 0;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/*		irq_set_handler_data(port->irq, port);
 * Unshare the filesystem structure if it is being shared		irq_set_handler_data(port->irq, port);
 */		irq_set_handler_data(port->irq, port);
static int unshare_fs(unsigned long unshare_flags, struct fs_struct **new_fsp)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	struct fs_struct *fs = current->fs;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	if (!(unshare_flags & CLONE_FS) || !fs)		irq_set_handler_data(port->irq, port);
		return 0;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/* don't need lock here; in the worst case we'll do useless copy */		irq_set_handler_data(port->irq, port);
	if (fs->users == 1)		irq_set_handler_data(port->irq, port);
		return 0;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	*new_fsp = copy_fs_struct(fs);		irq_set_handler_data(port->irq, port);
	if (!*new_fsp)		irq_set_handler_data(port->irq, port);
		return -ENOMEM;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	return 0;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/*		irq_set_handler_data(port->irq, port);
 * Unshare file descriptor table if it is being shared		irq_set_handler_data(port->irq, port);
 */		irq_set_handler_data(port->irq, port);
static int unshare_fd(unsigned long unshare_flags, struct files_struct **new_fdp)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	struct files_struct *fd = current->files;		irq_set_handler_data(port->irq, port);
	int error = 0;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	if ((unshare_flags & CLONE_FILES) &&		irq_set_handler_data(port->irq, port);
	    (fd && atomic_read(&fd->count) > 1)) {		irq_set_handler_data(port->irq, port);
		*new_fdp = dup_fd(fd, &error);		irq_set_handler_data(port->irq, port);
		if (!*new_fdp)		irq_set_handler_data(port->irq, port);
			return error;		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	return 0;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/*		irq_set_handler_data(port->irq, port);
 * unshare allows a process to 'unshare' part of the process		irq_set_handler_data(port->irq, port);
 * context which was originally shared using clone.  copy_*		irq_set_handler_data(port->irq, port);
 * functions used by do_fork() cannot be used here directly		irq_set_handler_data(port->irq, port);
 * because they modify an inactive task_struct that is being		irq_set_handler_data(port->irq, port);
 * constructed. Here we are modifying the current, active,		irq_set_handler_data(port->irq, port);
 * task_struct.		irq_set_handler_data(port->irq, port);
 */		irq_set_handler_data(port->irq, port);
SYSCALL_DEFINE1(unshare, unsigned long, unshare_flags)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	struct fs_struct *fs, *new_fs = NULL;		irq_set_handler_data(port->irq, port);
	struct files_struct *fd, *new_fd = NULL;		irq_set_handler_data(port->irq, port);
	struct cred *new_cred = NULL;		irq_set_handler_data(port->irq, port);
	struct nsproxy *new_nsproxy = NULL;		irq_set_handler_data(port->irq, port);
	int do_sysvsem = 0;		irq_set_handler_data(port->irq, port);
	int err;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * If unsharing a user namespace must also unshare the thread.		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	if (unshare_flags & CLONE_NEWUSER)		irq_set_handler_data(port->irq, port);
		unshare_flags |= CLONE_THREAD | CLONE_FS;		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * If unsharing a thread from a thread group, must also unshare vm.		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	if (unshare_flags & CLONE_THREAD)		irq_set_handler_data(port->irq, port);
		unshare_flags |= CLONE_VM;		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * If unsharing vm, must also unshare signal handlers.		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	if (unshare_flags & CLONE_VM)		irq_set_handler_data(port->irq, port);
		unshare_flags |= CLONE_SIGHAND;		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * If unsharing namespace, must also unshare filesystem information.		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	if (unshare_flags & CLONE_NEWNS)		irq_set_handler_data(port->irq, port);
		unshare_flags |= CLONE_FS;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	err = check_unshare_flags(unshare_flags);		irq_set_handler_data(port->irq, port);
	if (err)		irq_set_handler_data(port->irq, port);
		goto bad_unshare_out;		irq_set_handler_data(port->irq, port);
	/*		irq_set_handler_data(port->irq, port);
	 * CLONE_NEWIPC must also detach from the undolist: after switching		irq_set_handler_data(port->irq, port);
	 * to a new ipc namespace, the semaphore arrays from the old		irq_set_handler_data(port->irq, port);
	 * namespace are unreachable.		irq_set_handler_data(port->irq, port);
	 */		irq_set_handler_data(port->irq, port);
	if (unshare_flags & (CLONE_NEWIPC|CLONE_SYSVSEM))		irq_set_handler_data(port->irq, port);
		do_sysvsem = 1;		irq_set_handler_data(port->irq, port);
	err = unshare_fs(unshare_flags, &new_fs);		irq_set_handler_data(port->irq, port);
	if (err)		irq_set_handler_data(port->irq, port);
		goto bad_unshare_out;		irq_set_handler_data(port->irq, port);
	err = unshare_fd(unshare_flags, &new_fd);		irq_set_handler_data(port->irq, port);
	if (err)		irq_set_handler_data(port->irq, port);
		goto bad_unshare_cleanup_fs;		irq_set_handler_data(port->irq, port);
	err = unshare_userns(unshare_flags, &new_cred);		irq_set_handler_data(port->irq, port);
	if (err)		irq_set_handler_data(port->irq, port);
		goto bad_unshare_cleanup_fd;		irq_set_handler_data(port->irq, port);
	err = unshare_nsproxy_namespaces(unshare_flags, &new_nsproxy,		irq_set_handler_data(port->irq, port);
					 new_cred, new_fs);		irq_set_handler_data(port->irq, port);
	if (err)		irq_set_handler_data(port->irq, port);
		goto bad_unshare_cleanup_cred;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	if (new_fs || new_fd || do_sysvsem || new_cred || new_nsproxy) {		irq_set_handler_data(port->irq, port);
		if (do_sysvsem) {		irq_set_handler_data(port->irq, port);
			/*		irq_set_handler_data(port->irq, port);
			 * CLONE_SYSVSEM is equivalent to sys_exit().		irq_set_handler_data(port->irq, port);
			 */		irq_set_handler_data(port->irq, port);
			exit_sem(current);		irq_set_handler_data(port->irq, port);
		}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		if (new_nsproxy)		irq_set_handler_data(port->irq, port);
			switch_task_namespaces(current, new_nsproxy);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		task_lock(current);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		if (new_fs) {		irq_set_handler_data(port->irq, port);
			fs = current->fs;		irq_set_handler_data(port->irq, port);
			spin_lock(&fs->lock);		irq_set_handler_data(port->irq, port);
			current->fs = new_fs;		irq_set_handler_data(port->irq, port);
			if (--fs->users)		irq_set_handler_data(port->irq, port);
				new_fs = NULL;		irq_set_handler_data(port->irq, port);
			else		irq_set_handler_data(port->irq, port);
				new_fs = fs;		irq_set_handler_data(port->irq, port);
			spin_unlock(&fs->lock);		irq_set_handler_data(port->irq, port);
		}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		if (new_fd) {		irq_set_handler_data(port->irq, port);
			fd = current->files;		irq_set_handler_data(port->irq, port);
			current->files = new_fd;		irq_set_handler_data(port->irq, port);
			new_fd = fd;		irq_set_handler_data(port->irq, port);
		}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		task_unlock(current);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
		if (new_cred) {		irq_set_handler_data(port->irq, port);
			/* Install the new user namespace */		irq_set_handler_data(port->irq, port);
			commit_creds(new_cred);		irq_set_handler_data(port->irq, port);
			new_cred = NULL;		irq_set_handler_data(port->irq, port);
		}		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
bad_unshare_cleanup_cred:		irq_set_handler_data(port->irq, port);
	if (new_cred)		irq_set_handler_data(port->irq, port);
		put_cred(new_cred);		irq_set_handler_data(port->irq, port);
bad_unshare_cleanup_fd:		irq_set_handler_data(port->irq, port);
	if (new_fd)		irq_set_handler_data(port->irq, port);
		put_files_struct(new_fd);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
bad_unshare_cleanup_fs:		irq_set_handler_data(port->irq, port);
	if (new_fs)		irq_set_handler_data(port->irq, port);
		free_fs_struct(new_fs);		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
bad_unshare_out:		irq_set_handler_data(port->irq, port);
	return err;		irq_set_handler_data(port->irq, port);
}		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
/*		irq_set_handler_data(port->irq, port);
 *	Helper to unshare the files of the current task.		irq_set_handler_data(port->irq, port);
 *	We don't want to expose copy_files internals to		irq_set_handler_data(port->irq, port);
 *	the exec layer of the kernel.		irq_set_handler_data(port->irq, port);
 */		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
int unshare_files(struct files_struct **displaced)		irq_set_handler_data(port->irq, port);
{		irq_set_handler_data(port->irq, port);
	struct task_struct *task = current;		irq_set_handler_data(port->irq, port);
	struct files_struct *copy = NULL;		irq_set_handler_data(port->irq, port);
	int error;		irq_set_handler_data(port->irq, port);
		irq_set_handler_data(port->irq, port);
	error = unshare_fd(CLONE_FILES, &copy);		irq_set_handler_data(port->irq, port);
	if (error || !copy) {		irq_set_handler_data(port->irq, port);
		*displaced = NULL;		irq_set_handler_data(port->irq, port);
		return error;		irq_set_handler_data(port->irq, port);
	}		irq_set_handler_data(port->irq, port);
	*displaced = task->files;		irq_set_handler_data(port->irq, port);
	task_lock(task);		irq_set_handler_data(port->irq, port);
	task->files = copy;		irq_set_handler_data(port->irq, port);
	task_unlock(task);		irq_set_handler_data(port->irq, port);
	return 0;		irq_set_handler_data(port->irq, port);
}