			       IRQ_NOREQUEST, 0);
/*			       IRQ_NOREQUEST, 0);
 *  linux/kernel/fork.c			       IRQ_NOREQUEST, 0);
 *			       IRQ_NOREQUEST, 0);
 *  Copyright (C) 1991, 1992  Linus Torvalds			       IRQ_NOREQUEST, 0);
 */			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/*			       IRQ_NOREQUEST, 0);
 *  'fork.c' contains the help-routines for the 'fork' system call			       IRQ_NOREQUEST, 0);
 * (see also entry.S and others).			       IRQ_NOREQUEST, 0);
 * Fork is rather simple, once you get the hang of it, but the memory			       IRQ_NOREQUEST, 0);
 * management can be a bitch. See 'mm/memory.c': 'copy_page_range()'			       IRQ_NOREQUEST, 0);
 */			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
#include <linux/slab.h>			       IRQ_NOREQUEST, 0);
#include <linux/init.h>			       IRQ_NOREQUEST, 0);
#include <linux/unistd.h>			       IRQ_NOREQUEST, 0);
#include <linux/module.h>			       IRQ_NOREQUEST, 0);
#include <linux/vmalloc.h>			       IRQ_NOREQUEST, 0);
#include <linux/completion.h>			       IRQ_NOREQUEST, 0);
#include <linux/personality.h>			       IRQ_NOREQUEST, 0);
#include <linux/mempolicy.h>			       IRQ_NOREQUEST, 0);
#include <linux/sem.h>			       IRQ_NOREQUEST, 0);
#include <linux/file.h>			       IRQ_NOREQUEST, 0);
#include <linux/fdtable.h>			       IRQ_NOREQUEST, 0);
#include <linux/iocontext.h>			       IRQ_NOREQUEST, 0);
#include <linux/key.h>			       IRQ_NOREQUEST, 0);
#include <linux/binfmts.h>			       IRQ_NOREQUEST, 0);
#include <linux/mman.h>			       IRQ_NOREQUEST, 0);
#include <linux/mmu_notifier.h>			       IRQ_NOREQUEST, 0);
#include <linux/fs.h>			       IRQ_NOREQUEST, 0);
#include <linux/nsproxy.h>			       IRQ_NOREQUEST, 0);
#include <linux/capability.h>			       IRQ_NOREQUEST, 0);
#include <linux/cpu.h>			       IRQ_NOREQUEST, 0);
#include <linux/cgroup.h>			       IRQ_NOREQUEST, 0);
#include <linux/security.h>			       IRQ_NOREQUEST, 0);
#include <linux/hugetlb.h>			       IRQ_NOREQUEST, 0);
#include <linux/seccomp.h>			       IRQ_NOREQUEST, 0);
#include <linux/swap.h>			       IRQ_NOREQUEST, 0);
#include <linux/syscalls.h>			       IRQ_NOREQUEST, 0);
#include <linux/jiffies.h>			       IRQ_NOREQUEST, 0);
#include <linux/futex.h>			       IRQ_NOREQUEST, 0);
#include <linux/compat.h>			       IRQ_NOREQUEST, 0);
#include <linux/kthread.h>			       IRQ_NOREQUEST, 0);
#include <linux/task_io_accounting_ops.h>			       IRQ_NOREQUEST, 0);
#include <linux/rcupdate.h>			       IRQ_NOREQUEST, 0);
#include <linux/ptrace.h>			       IRQ_NOREQUEST, 0);
#include <linux/mount.h>			       IRQ_NOREQUEST, 0);
#include <linux/audit.h>			       IRQ_NOREQUEST, 0);
#include <linux/memcontrol.h>			       IRQ_NOREQUEST, 0);
#include <linux/ftrace.h>			       IRQ_NOREQUEST, 0);
#include <linux/proc_fs.h>			       IRQ_NOREQUEST, 0);
#include <linux/profile.h>			       IRQ_NOREQUEST, 0);
#include <linux/rmap.h>			       IRQ_NOREQUEST, 0);
#include <linux/ksm.h>			       IRQ_NOREQUEST, 0);
#include <linux/acct.h>			       IRQ_NOREQUEST, 0);
#include <linux/tsacct_kern.h>			       IRQ_NOREQUEST, 0);
#include <linux/cn_proc.h>			       IRQ_NOREQUEST, 0);
#include <linux/freezer.h>			       IRQ_NOREQUEST, 0);
#include <linux/delayacct.h>			       IRQ_NOREQUEST, 0);
#include <linux/taskstats_kern.h>			       IRQ_NOREQUEST, 0);
#include <linux/random.h>			       IRQ_NOREQUEST, 0);
#include <linux/tty.h>			       IRQ_NOREQUEST, 0);
#include <linux/blkdev.h>			       IRQ_NOREQUEST, 0);
#include <linux/fs_struct.h>			       IRQ_NOREQUEST, 0);
#include <linux/magic.h>			       IRQ_NOREQUEST, 0);
#include <linux/perf_event.h>			       IRQ_NOREQUEST, 0);
#include <linux/posix-timers.h>			       IRQ_NOREQUEST, 0);
#include <linux/user-return-notifier.h>			       IRQ_NOREQUEST, 0);
#include <linux/oom.h>			       IRQ_NOREQUEST, 0);
#include <linux/khugepaged.h>			       IRQ_NOREQUEST, 0);
#include <linux/signalfd.h>			       IRQ_NOREQUEST, 0);
#include <linux/uprobes.h>			       IRQ_NOREQUEST, 0);
#include <linux/aio.h>			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
#include <asm/pgtable.h>			       IRQ_NOREQUEST, 0);
#include <asm/pgalloc.h>			       IRQ_NOREQUEST, 0);
#include <asm/uaccess.h>			       IRQ_NOREQUEST, 0);
#include <asm/mmu_context.h>			       IRQ_NOREQUEST, 0);
#include <asm/cacheflush.h>			       IRQ_NOREQUEST, 0);
#include <asm/tlbflush.h>			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
#include <trace/events/sched.h>			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
#define CREATE_TRACE_POINTS			       IRQ_NOREQUEST, 0);
#include <trace/events/task.h>			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/*			       IRQ_NOREQUEST, 0);
 * Protected counters by write_lock_irq(&tasklist_lock)			       IRQ_NOREQUEST, 0);
 */			       IRQ_NOREQUEST, 0);
unsigned long total_forks;	/* Handle normal Linux uptimes. */			       IRQ_NOREQUEST, 0);
int nr_threads;			/* The idle threads do not count.. */			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
int max_threads;		/* tunable limit on nr_threads */			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
DEFINE_PER_CPU(unsigned long, process_counts) = 0;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
__cacheline_aligned DEFINE_RWLOCK(tasklist_lock);  /* outer */			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_PROVE_RCU			       IRQ_NOREQUEST, 0);
int lockdep_tasklist_lock_is_held(void)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	return lockdep_is_held(&tasklist_lock);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
EXPORT_SYMBOL_GPL(lockdep_tasklist_lock_is_held);			       IRQ_NOREQUEST, 0);
#endif /* #ifdef CONFIG_PROVE_RCU */			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
int nr_processes(void)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	int cpu;			       IRQ_NOREQUEST, 0);
	int total = 0;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	for_each_possible_cpu(cpu)			       IRQ_NOREQUEST, 0);
		total += per_cpu(process_counts, cpu);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	return total;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
void __weak arch_release_task_struct(struct task_struct *tsk)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
#ifndef CONFIG_ARCH_TASK_STRUCT_ALLOCATOR			       IRQ_NOREQUEST, 0);
static struct kmem_cache *task_struct_cachep;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static inline struct task_struct *alloc_task_struct_node(int node)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	return kmem_cache_alloc_node(task_struct_cachep, GFP_KERNEL, node);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static inline void free_task_struct(struct task_struct *tsk)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	kmem_cache_free(task_struct_cachep, tsk);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
void __weak arch_release_thread_info(struct thread_info *ti)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
#ifndef CONFIG_ARCH_THREAD_INFO_ALLOCATOR			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/*			       IRQ_NOREQUEST, 0);
 * Allocate pages if THREAD_SIZE is >= PAGE_SIZE, otherwise use a			       IRQ_NOREQUEST, 0);
 * kmemcache based allocator.			       IRQ_NOREQUEST, 0);
 */			       IRQ_NOREQUEST, 0);
# if THREAD_SIZE >= PAGE_SIZE			       IRQ_NOREQUEST, 0);
static struct thread_info *alloc_thread_info_node(struct task_struct *tsk,			       IRQ_NOREQUEST, 0);
						  int node)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	struct page *page = alloc_pages_node(node, THREADINFO_GFP_ACCOUNTED,			       IRQ_NOREQUEST, 0);
					     THREAD_SIZE_ORDER);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	return page ? page_address(page) : NULL;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static inline void free_thread_info(struct thread_info *ti)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	free_memcg_kmem_pages((unsigned long)ti, THREAD_SIZE_ORDER);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
# else			       IRQ_NOREQUEST, 0);
static struct kmem_cache *thread_info_cache;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static struct thread_info *alloc_thread_info_node(struct task_struct *tsk,			       IRQ_NOREQUEST, 0);
						  int node)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	return kmem_cache_alloc_node(thread_info_cache, THREADINFO_GFP, node);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static void free_thread_info(struct thread_info *ti)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	kmem_cache_free(thread_info_cache, ti);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
void thread_info_cache_init(void)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	thread_info_cache = kmem_cache_create("thread_info", THREAD_SIZE,			       IRQ_NOREQUEST, 0);
					      THREAD_SIZE, 0, NULL);			       IRQ_NOREQUEST, 0);
	BUG_ON(thread_info_cache == NULL);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
# endif			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/* SLAB cache for signal_struct structures (tsk->signal) */			       IRQ_NOREQUEST, 0);
static struct kmem_cache *signal_cachep;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/* SLAB cache for sighand_struct structures (tsk->sighand) */			       IRQ_NOREQUEST, 0);
struct kmem_cache *sighand_cachep;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/* SLAB cache for files_struct structures (tsk->files) */			       IRQ_NOREQUEST, 0);
struct kmem_cache *files_cachep;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/* SLAB cache for fs_struct structures (tsk->fs) */			       IRQ_NOREQUEST, 0);
struct kmem_cache *fs_cachep;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/* SLAB cache for vm_area_struct structures */			       IRQ_NOREQUEST, 0);
struct kmem_cache *vm_area_cachep;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/* SLAB cache for mm_struct structures (tsk->mm) */			       IRQ_NOREQUEST, 0);
static struct kmem_cache *mm_cachep;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static void account_kernel_stack(struct thread_info *ti, int account)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	struct zone *zone = page_zone(virt_to_page(ti));			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	mod_zone_page_state(zone, NR_KERNEL_STACK, account);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
void free_task(struct task_struct *tsk)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	account_kernel_stack(tsk->stack, -1);			       IRQ_NOREQUEST, 0);
	arch_release_thread_info(tsk->stack);			       IRQ_NOREQUEST, 0);
	free_thread_info(tsk->stack);			       IRQ_NOREQUEST, 0);
	rt_mutex_debug_task_free(tsk);			       IRQ_NOREQUEST, 0);
	ftrace_graph_exit_task(tsk);			       IRQ_NOREQUEST, 0);
	put_seccomp_filter(tsk);			       IRQ_NOREQUEST, 0);
	arch_release_task_struct(tsk);			       IRQ_NOREQUEST, 0);
	free_task_struct(tsk);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
EXPORT_SYMBOL(free_task);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static inline void free_signal_struct(struct signal_struct *sig)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	taskstats_tgid_free(sig);			       IRQ_NOREQUEST, 0);
	sched_autogroup_exit(sig);			       IRQ_NOREQUEST, 0);
	kmem_cache_free(signal_cachep, sig);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static inline void put_signal_struct(struct signal_struct *sig)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	if (atomic_dec_and_test(&sig->sigcnt))			       IRQ_NOREQUEST, 0);
		free_signal_struct(sig);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
void __put_task_struct(struct task_struct *tsk)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	WARN_ON(!tsk->exit_state);			       IRQ_NOREQUEST, 0);
	WARN_ON(atomic_read(&tsk->usage));			       IRQ_NOREQUEST, 0);
	WARN_ON(tsk == current);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	task_numa_free(tsk);			       IRQ_NOREQUEST, 0);
	security_task_free(tsk);			       IRQ_NOREQUEST, 0);
	exit_creds(tsk);			       IRQ_NOREQUEST, 0);
	delayacct_tsk_free(tsk);			       IRQ_NOREQUEST, 0);
	put_signal_struct(tsk->signal);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	if (!profile_handoff_task(tsk))			       IRQ_NOREQUEST, 0);
		free_task(tsk);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
EXPORT_SYMBOL_GPL(__put_task_struct);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
void __init __weak arch_task_cache_init(void) { }			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
void __init fork_init(unsigned long mempages)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
#ifndef CONFIG_ARCH_TASK_STRUCT_ALLOCATOR			       IRQ_NOREQUEST, 0);
#ifndef ARCH_MIN_TASKALIGN			       IRQ_NOREQUEST, 0);
#define ARCH_MIN_TASKALIGN	L1_CACHE_BYTES			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
	/* create a slab on which task_structs can be allocated */			       IRQ_NOREQUEST, 0);
	task_struct_cachep =			       IRQ_NOREQUEST, 0);
		kmem_cache_create("task_struct", sizeof(struct task_struct),			       IRQ_NOREQUEST, 0);
			ARCH_MIN_TASKALIGN, SLAB_PANIC | SLAB_NOTRACK, NULL);			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/* do the arch specific task caches init */			       IRQ_NOREQUEST, 0);
	arch_task_cache_init();			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * The default maximum number of threads is set to a safe			       IRQ_NOREQUEST, 0);
	 * value: the thread structures can take up at most half			       IRQ_NOREQUEST, 0);
	 * of memory.			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	max_threads = mempages / (8 * THREAD_SIZE / PAGE_SIZE);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * we need to allow at least 20 threads to boot a system			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	if (max_threads < 20)			       IRQ_NOREQUEST, 0);
		max_threads = 20;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	init_task.signal->rlim[RLIMIT_NPROC].rlim_cur = max_threads/2;			       IRQ_NOREQUEST, 0);
	init_task.signal->rlim[RLIMIT_NPROC].rlim_max = max_threads/2;			       IRQ_NOREQUEST, 0);
	init_task.signal->rlim[RLIMIT_SIGPENDING] =			       IRQ_NOREQUEST, 0);
		init_task.signal->rlim[RLIMIT_NPROC];			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
int __attribute__((weak)) arch_dup_task_struct(struct task_struct *dst,			       IRQ_NOREQUEST, 0);
					       struct task_struct *src)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	*dst = *src;			       IRQ_NOREQUEST, 0);
	return 0;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static struct task_struct *dup_task_struct(struct task_struct *orig)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	struct task_struct *tsk;			       IRQ_NOREQUEST, 0);
	struct thread_info *ti;			       IRQ_NOREQUEST, 0);
	unsigned long *stackend;			       IRQ_NOREQUEST, 0);
	int node = tsk_fork_get_node(orig);			       IRQ_NOREQUEST, 0);
	int err;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	tsk = alloc_task_struct_node(node);			       IRQ_NOREQUEST, 0);
	if (!tsk)			       IRQ_NOREQUEST, 0);
		return NULL;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	ti = alloc_thread_info_node(tsk, node);			       IRQ_NOREQUEST, 0);
	if (!ti)			       IRQ_NOREQUEST, 0);
		goto free_tsk;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	err = arch_dup_task_struct(tsk, orig);			       IRQ_NOREQUEST, 0);
	if (err)			       IRQ_NOREQUEST, 0);
		goto free_ti;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	tsk->stack = ti;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	setup_thread_stack(tsk, orig);			       IRQ_NOREQUEST, 0);
	clear_user_return_notifier(tsk);			       IRQ_NOREQUEST, 0);
	clear_tsk_need_resched(tsk);			       IRQ_NOREQUEST, 0);
	stackend = end_of_stack(tsk);			       IRQ_NOREQUEST, 0);
	*stackend = STACK_END_MAGIC;	/* for overflow detection */			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_CC_STACKPROTECTOR			       IRQ_NOREQUEST, 0);
	tsk->stack_canary = get_random_int();			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * One for us, one for whoever does the "release_task()" (usually			       IRQ_NOREQUEST, 0);
	 * parent)			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	atomic_set(&tsk->usage, 2);			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_BLK_DEV_IO_TRACE			       IRQ_NOREQUEST, 0);
	tsk->btrace_seq = 0;			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
	tsk->splice_pipe = NULL;			       IRQ_NOREQUEST, 0);
	tsk->task_frag.page = NULL;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	account_kernel_stack(ti, 1);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	return tsk;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
free_ti:			       IRQ_NOREQUEST, 0);
	free_thread_info(ti);			       IRQ_NOREQUEST, 0);
free_tsk:			       IRQ_NOREQUEST, 0);
	free_task_struct(tsk);			       IRQ_NOREQUEST, 0);
	return NULL;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_MMU			       IRQ_NOREQUEST, 0);
static int dup_mmap(struct mm_struct *mm, struct mm_struct *oldmm)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	struct vm_area_struct *mpnt, *tmp, *prev, **pprev;			       IRQ_NOREQUEST, 0);
	struct rb_node **rb_link, *rb_parent;			       IRQ_NOREQUEST, 0);
	int retval;			       IRQ_NOREQUEST, 0);
	unsigned long charge;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	uprobe_start_dup_mmap();			       IRQ_NOREQUEST, 0);
	down_write(&oldmm->mmap_sem);			       IRQ_NOREQUEST, 0);
	flush_cache_dup_mm(oldmm);			       IRQ_NOREQUEST, 0);
	uprobe_dup_mmap(oldmm, mm);			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * Not linked in yet - no deadlock potential:			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	down_write_nested(&mm->mmap_sem, SINGLE_DEPTH_NESTING);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	mm->locked_vm = 0;			       IRQ_NOREQUEST, 0);
	mm->mmap = NULL;			       IRQ_NOREQUEST, 0);
	mm->mmap_cache = NULL;			       IRQ_NOREQUEST, 0);
	mm->map_count = 0;			       IRQ_NOREQUEST, 0);
	cpumask_clear(mm_cpumask(mm));			       IRQ_NOREQUEST, 0);
	mm->mm_rb = RB_ROOT;			       IRQ_NOREQUEST, 0);
	rb_link = &mm->mm_rb.rb_node;			       IRQ_NOREQUEST, 0);
	rb_parent = NULL;			       IRQ_NOREQUEST, 0);
	pprev = &mm->mmap;			       IRQ_NOREQUEST, 0);
	retval = ksm_fork(mm, oldmm);			       IRQ_NOREQUEST, 0);
	if (retval)			       IRQ_NOREQUEST, 0);
		goto out;			       IRQ_NOREQUEST, 0);
	retval = khugepaged_fork(mm, oldmm);			       IRQ_NOREQUEST, 0);
	if (retval)			       IRQ_NOREQUEST, 0);
		goto out;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	prev = NULL;			       IRQ_NOREQUEST, 0);
	for (mpnt = oldmm->mmap; mpnt; mpnt = mpnt->vm_next) {			       IRQ_NOREQUEST, 0);
		struct file *file;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		if (mpnt->vm_flags & VM_DONTCOPY) {			       IRQ_NOREQUEST, 0);
			vm_stat_account(mm, mpnt->vm_flags, mpnt->vm_file,			       IRQ_NOREQUEST, 0);
							-vma_pages(mpnt));			       IRQ_NOREQUEST, 0);
			continue;			       IRQ_NOREQUEST, 0);
		}			       IRQ_NOREQUEST, 0);
		charge = 0;			       IRQ_NOREQUEST, 0);
		if (mpnt->vm_flags & VM_ACCOUNT) {			       IRQ_NOREQUEST, 0);
			unsigned long len = vma_pages(mpnt);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
			if (security_vm_enough_memory_mm(oldmm, len)) /* sic */			       IRQ_NOREQUEST, 0);
				goto fail_nomem;			       IRQ_NOREQUEST, 0);
			charge = len;			       IRQ_NOREQUEST, 0);
		}			       IRQ_NOREQUEST, 0);
		tmp = kmem_cache_alloc(vm_area_cachep, GFP_KERNEL);			       IRQ_NOREQUEST, 0);
		if (!tmp)			       IRQ_NOREQUEST, 0);
			goto fail_nomem;			       IRQ_NOREQUEST, 0);
		*tmp = *mpnt;			       IRQ_NOREQUEST, 0);
		INIT_LIST_HEAD(&tmp->anon_vma_chain);			       IRQ_NOREQUEST, 0);
		retval = vma_dup_policy(mpnt, tmp);			       IRQ_NOREQUEST, 0);
		if (retval)			       IRQ_NOREQUEST, 0);
			goto fail_nomem_policy;			       IRQ_NOREQUEST, 0);
		tmp->vm_mm = mm;			       IRQ_NOREQUEST, 0);
		if (anon_vma_fork(tmp, mpnt))			       IRQ_NOREQUEST, 0);
			goto fail_nomem_anon_vma_fork;			       IRQ_NOREQUEST, 0);
		tmp->vm_flags &= ~VM_LOCKED;			       IRQ_NOREQUEST, 0);
		tmp->vm_next = tmp->vm_prev = NULL;			       IRQ_NOREQUEST, 0);
		file = tmp->vm_file;			       IRQ_NOREQUEST, 0);
		if (file) {			       IRQ_NOREQUEST, 0);
			struct inode *inode = file_inode(file);			       IRQ_NOREQUEST, 0);
			struct address_space *mapping = file->f_mapping;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
			get_file(file);			       IRQ_NOREQUEST, 0);
			if (tmp->vm_flags & VM_DENYWRITE)			       IRQ_NOREQUEST, 0);
				atomic_dec(&inode->i_writecount);			       IRQ_NOREQUEST, 0);
			mutex_lock(&mapping->i_mmap_mutex);			       IRQ_NOREQUEST, 0);
			if (tmp->vm_flags & VM_SHARED)			       IRQ_NOREQUEST, 0);
				mapping->i_mmap_writable++;			       IRQ_NOREQUEST, 0);
			flush_dcache_mmap_lock(mapping);			       IRQ_NOREQUEST, 0);
			/* insert tmp into the share list, just after mpnt */			       IRQ_NOREQUEST, 0);
			if (unlikely(tmp->vm_flags & VM_NONLINEAR))			       IRQ_NOREQUEST, 0);
				vma_nonlinear_insert(tmp,			       IRQ_NOREQUEST, 0);
						&mapping->i_mmap_nonlinear);			       IRQ_NOREQUEST, 0);
			else			       IRQ_NOREQUEST, 0);
				vma_interval_tree_insert_after(tmp, mpnt,			       IRQ_NOREQUEST, 0);
							&mapping->i_mmap);			       IRQ_NOREQUEST, 0);
			flush_dcache_mmap_unlock(mapping);			       IRQ_NOREQUEST, 0);
			mutex_unlock(&mapping->i_mmap_mutex);			       IRQ_NOREQUEST, 0);
		}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		/*			       IRQ_NOREQUEST, 0);
		 * Clear hugetlb-related page reserves for children. This only			       IRQ_NOREQUEST, 0);
		 * affects MAP_PRIVATE mappings. Faults generated by the child			       IRQ_NOREQUEST, 0);
		 * are not guaranteed to succeed, even if read-only			       IRQ_NOREQUEST, 0);
		 */			       IRQ_NOREQUEST, 0);
		if (is_vm_hugetlb_page(tmp))			       IRQ_NOREQUEST, 0);
			reset_vma_resv_huge_pages(tmp);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		/*			       IRQ_NOREQUEST, 0);
		 * Link in the new vma and copy the page table entries.			       IRQ_NOREQUEST, 0);
		 */			       IRQ_NOREQUEST, 0);
		*pprev = tmp;			       IRQ_NOREQUEST, 0);
		pprev = &tmp->vm_next;			       IRQ_NOREQUEST, 0);
		tmp->vm_prev = prev;			       IRQ_NOREQUEST, 0);
		prev = tmp;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		__vma_link_rb(mm, tmp, rb_link, rb_parent);			       IRQ_NOREQUEST, 0);
		rb_link = &tmp->vm_rb.rb_right;			       IRQ_NOREQUEST, 0);
		rb_parent = &tmp->vm_rb;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		mm->map_count++;			       IRQ_NOREQUEST, 0);
		retval = copy_page_range(mm, oldmm, mpnt);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		if (tmp->vm_ops && tmp->vm_ops->open)			       IRQ_NOREQUEST, 0);
			tmp->vm_ops->open(tmp);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		if (retval)			       IRQ_NOREQUEST, 0);
			goto out;			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
	/* a new mm has just been created */			       IRQ_NOREQUEST, 0);
	arch_dup_mmap(oldmm, mm);			       IRQ_NOREQUEST, 0);
	retval = 0;			       IRQ_NOREQUEST, 0);
out:			       IRQ_NOREQUEST, 0);
	up_write(&mm->mmap_sem);			       IRQ_NOREQUEST, 0);
	flush_tlb_mm(oldmm);			       IRQ_NOREQUEST, 0);
	up_write(&oldmm->mmap_sem);			       IRQ_NOREQUEST, 0);
	uprobe_end_dup_mmap();			       IRQ_NOREQUEST, 0);
	return retval;			       IRQ_NOREQUEST, 0);
fail_nomem_anon_vma_fork:			       IRQ_NOREQUEST, 0);
	mpol_put(vma_policy(tmp));			       IRQ_NOREQUEST, 0);
fail_nomem_policy:			       IRQ_NOREQUEST, 0);
	kmem_cache_free(vm_area_cachep, tmp);			       IRQ_NOREQUEST, 0);
fail_nomem:			       IRQ_NOREQUEST, 0);
	retval = -ENOMEM;			       IRQ_NOREQUEST, 0);
	vm_unacct_memory(charge);			       IRQ_NOREQUEST, 0);
	goto out;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static inline int mm_alloc_pgd(struct mm_struct *mm)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	mm->pgd = pgd_alloc(mm);			       IRQ_NOREQUEST, 0);
	if (unlikely(!mm->pgd))			       IRQ_NOREQUEST, 0);
		return -ENOMEM;			       IRQ_NOREQUEST, 0);
	return 0;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static inline void mm_free_pgd(struct mm_struct *mm)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	pgd_free(mm, mm->pgd);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
#else			       IRQ_NOREQUEST, 0);
#define dup_mmap(mm, oldmm)	(0)			       IRQ_NOREQUEST, 0);
#define mm_alloc_pgd(mm)	(0)			       IRQ_NOREQUEST, 0);
#define mm_free_pgd(mm)			       IRQ_NOREQUEST, 0);
#endif /* CONFIG_MMU */			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
__cacheline_aligned_in_smp DEFINE_SPINLOCK(mmlist_lock);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
#define allocate_mm()	(kmem_cache_alloc(mm_cachep, GFP_KERNEL))			       IRQ_NOREQUEST, 0);
#define free_mm(mm)	(kmem_cache_free(mm_cachep, (mm)))			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static unsigned long default_dump_filter = MMF_DUMP_FILTER_DEFAULT;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static int __init coredump_filter_setup(char *s)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	default_dump_filter =			       IRQ_NOREQUEST, 0);
		(simple_strtoul(s, NULL, 0) << MMF_DUMP_FILTER_SHIFT) &			       IRQ_NOREQUEST, 0);
		MMF_DUMP_FILTER_MASK;			       IRQ_NOREQUEST, 0);
	return 1;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
__setup("coredump_filter=", coredump_filter_setup);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
#include <linux/init_task.h>			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static void mm_init_aio(struct mm_struct *mm)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_AIO			       IRQ_NOREQUEST, 0);
	spin_lock_init(&mm->ioctx_lock);			       IRQ_NOREQUEST, 0);
	mm->ioctx_table = NULL;			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static struct mm_struct *mm_init(struct mm_struct *mm, struct task_struct *p)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	atomic_set(&mm->mm_users, 1);			       IRQ_NOREQUEST, 0);
	atomic_set(&mm->mm_count, 1);			       IRQ_NOREQUEST, 0);
	init_rwsem(&mm->mmap_sem);			       IRQ_NOREQUEST, 0);
	INIT_LIST_HEAD(&mm->mmlist);			       IRQ_NOREQUEST, 0);
	mm->flags = (current->mm) ?			       IRQ_NOREQUEST, 0);
		(current->mm->flags & MMF_INIT_MASK) : default_dump_filter;			       IRQ_NOREQUEST, 0);
	mm->core_state = NULL;			       IRQ_NOREQUEST, 0);
	atomic_long_set(&mm->nr_ptes, 0);			       IRQ_NOREQUEST, 0);
	memset(&mm->rss_stat, 0, sizeof(mm->rss_stat));			       IRQ_NOREQUEST, 0);
	spin_lock_init(&mm->page_table_lock);			       IRQ_NOREQUEST, 0);
	mm_init_aio(mm);			       IRQ_NOREQUEST, 0);
	mm_init_owner(mm, p);			       IRQ_NOREQUEST, 0);
	clear_tlb_flush_pending(mm);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	if (likely(!mm_alloc_pgd(mm))) {			       IRQ_NOREQUEST, 0);
		mm->def_flags = 0;			       IRQ_NOREQUEST, 0);
		mmu_notifier_mm_init(mm);			       IRQ_NOREQUEST, 0);
		return mm;			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	free_mm(mm);			       IRQ_NOREQUEST, 0);
	return NULL;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static void check_mm(struct mm_struct *mm)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	int i;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	for (i = 0; i < NR_MM_COUNTERS; i++) {			       IRQ_NOREQUEST, 0);
		long x = atomic_long_read(&mm->rss_stat.count[i]);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		if (unlikely(x))			       IRQ_NOREQUEST, 0);
			printk(KERN_ALERT "BUG: Bad rss-counter state "			       IRQ_NOREQUEST, 0);
					  "mm:%p idx:%d val:%ld\n", mm, i, x);			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
#if defined(CONFIG_TRANSPARENT_HUGEPAGE) && !USE_SPLIT_PMD_PTLOCKS			       IRQ_NOREQUEST, 0);
	VM_BUG_ON(mm->pmd_huge_pte);			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/*			       IRQ_NOREQUEST, 0);
 * Allocate and initialize an mm_struct.			       IRQ_NOREQUEST, 0);
 */			       IRQ_NOREQUEST, 0);
struct mm_struct *mm_alloc(void)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	struct mm_struct *mm;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	mm = allocate_mm();			       IRQ_NOREQUEST, 0);
	if (!mm)			       IRQ_NOREQUEST, 0);
		return NULL;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	memset(mm, 0, sizeof(*mm));			       IRQ_NOREQUEST, 0);
	mm_init_cpumask(mm);			       IRQ_NOREQUEST, 0);
	return mm_init(mm, current);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/*			       IRQ_NOREQUEST, 0);
 * Called when the last reference to the mm			       IRQ_NOREQUEST, 0);
 * is dropped: either by a lazy thread or by			       IRQ_NOREQUEST, 0);
 * mmput. Free the page directory and the mm.			       IRQ_NOREQUEST, 0);
 */			       IRQ_NOREQUEST, 0);
void __mmdrop(struct mm_struct *mm)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	BUG_ON(mm == &init_mm);			       IRQ_NOREQUEST, 0);
	mm_free_pgd(mm);			       IRQ_NOREQUEST, 0);
	destroy_context(mm);			       IRQ_NOREQUEST, 0);
	mmu_notifier_mm_destroy(mm);			       IRQ_NOREQUEST, 0);
	check_mm(mm);			       IRQ_NOREQUEST, 0);
	free_mm(mm);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
EXPORT_SYMBOL_GPL(__mmdrop);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/*			       IRQ_NOREQUEST, 0);
 * Decrement the use count and release all resources for an mm.			       IRQ_NOREQUEST, 0);
 */			       IRQ_NOREQUEST, 0);
void mmput(struct mm_struct *mm)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	might_sleep();			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	if (atomic_dec_and_test(&mm->mm_users)) {			       IRQ_NOREQUEST, 0);
		uprobe_clear_state(mm);			       IRQ_NOREQUEST, 0);
		exit_aio(mm);			       IRQ_NOREQUEST, 0);
		ksm_exit(mm);			       IRQ_NOREQUEST, 0);
		khugepaged_exit(mm); /* must run before exit_mmap */			       IRQ_NOREQUEST, 0);
		exit_mmap(mm);			       IRQ_NOREQUEST, 0);
		set_mm_exe_file(mm, NULL);			       IRQ_NOREQUEST, 0);
		if (!list_empty(&mm->mmlist)) {			       IRQ_NOREQUEST, 0);
			spin_lock(&mmlist_lock);			       IRQ_NOREQUEST, 0);
			list_del(&mm->mmlist);			       IRQ_NOREQUEST, 0);
			spin_unlock(&mmlist_lock);			       IRQ_NOREQUEST, 0);
		}			       IRQ_NOREQUEST, 0);
		if (mm->binfmt)			       IRQ_NOREQUEST, 0);
			module_put(mm->binfmt->module);			       IRQ_NOREQUEST, 0);
		mmdrop(mm);			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
EXPORT_SYMBOL_GPL(mmput);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
void set_mm_exe_file(struct mm_struct *mm, struct file *new_exe_file)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	if (new_exe_file)			       IRQ_NOREQUEST, 0);
		get_file(new_exe_file);			       IRQ_NOREQUEST, 0);
	if (mm->exe_file)			       IRQ_NOREQUEST, 0);
		fput(mm->exe_file);			       IRQ_NOREQUEST, 0);
	mm->exe_file = new_exe_file;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
struct file *get_mm_exe_file(struct mm_struct *mm)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	struct file *exe_file;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/* We need mmap_sem to protect against races with removal of exe_file */			       IRQ_NOREQUEST, 0);
	down_read(&mm->mmap_sem);			       IRQ_NOREQUEST, 0);
	exe_file = mm->exe_file;			       IRQ_NOREQUEST, 0);
	if (exe_file)			       IRQ_NOREQUEST, 0);
		get_file(exe_file);			       IRQ_NOREQUEST, 0);
	up_read(&mm->mmap_sem);			       IRQ_NOREQUEST, 0);
	return exe_file;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static void dup_mm_exe_file(struct mm_struct *oldmm, struct mm_struct *newmm)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	/* It's safe to write the exe_file pointer without exe_file_lock because			       IRQ_NOREQUEST, 0);
	 * this is called during fork when the task is not yet in /proc */			       IRQ_NOREQUEST, 0);
	newmm->exe_file = get_mm_exe_file(oldmm);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/**			       IRQ_NOREQUEST, 0);
 * get_task_mm - acquire a reference to the task's mm			       IRQ_NOREQUEST, 0);
 *			       IRQ_NOREQUEST, 0);
 * Returns %NULL if the task has no mm.  Checks PF_KTHREAD (meaning			       IRQ_NOREQUEST, 0);
 * this kernel workthread has transiently adopted a user mm with use_mm,			       IRQ_NOREQUEST, 0);
 * to do its AIO) is not set and if so returns a reference to it, after			       IRQ_NOREQUEST, 0);
 * bumping up the use count.  User must release the mm via mmput()			       IRQ_NOREQUEST, 0);
 * after use.  Typically used by /proc and ptrace.			       IRQ_NOREQUEST, 0);
 */			       IRQ_NOREQUEST, 0);
struct mm_struct *get_task_mm(struct task_struct *task)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	struct mm_struct *mm;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	task_lock(task);			       IRQ_NOREQUEST, 0);
	mm = task->mm;			       IRQ_NOREQUEST, 0);
	if (mm) {			       IRQ_NOREQUEST, 0);
		if (task->flags & PF_KTHREAD)			       IRQ_NOREQUEST, 0);
			mm = NULL;			       IRQ_NOREQUEST, 0);
		else			       IRQ_NOREQUEST, 0);
			atomic_inc(&mm->mm_users);			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
	task_unlock(task);			       IRQ_NOREQUEST, 0);
	return mm;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
EXPORT_SYMBOL_GPL(get_task_mm);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
struct mm_struct *mm_access(struct task_struct *task, unsigned int mode)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	struct mm_struct *mm;			       IRQ_NOREQUEST, 0);
	int err;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	err =  mutex_lock_killable(&task->signal->cred_guard_mutex);			       IRQ_NOREQUEST, 0);
	if (err)			       IRQ_NOREQUEST, 0);
		return ERR_PTR(err);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	mm = get_task_mm(task);			       IRQ_NOREQUEST, 0);
	if (mm && mm != current->mm &&			       IRQ_NOREQUEST, 0);
			!ptrace_may_access(task, mode)) {			       IRQ_NOREQUEST, 0);
		mmput(mm);			       IRQ_NOREQUEST, 0);
		mm = ERR_PTR(-EACCES);			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
	mutex_unlock(&task->signal->cred_guard_mutex);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	return mm;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static void complete_vfork_done(struct task_struct *tsk)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	struct completion *vfork;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	task_lock(tsk);			       IRQ_NOREQUEST, 0);
	vfork = tsk->vfork_done;			       IRQ_NOREQUEST, 0);
	if (likely(vfork)) {			       IRQ_NOREQUEST, 0);
		tsk->vfork_done = NULL;			       IRQ_NOREQUEST, 0);
		complete(vfork);			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
	task_unlock(tsk);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static int wait_for_vfork_done(struct task_struct *child,			       IRQ_NOREQUEST, 0);
				struct completion *vfork)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	int killed;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	freezer_do_not_count();			       IRQ_NOREQUEST, 0);
	killed = wait_for_completion_killable(vfork);			       IRQ_NOREQUEST, 0);
	freezer_count();			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	if (killed) {			       IRQ_NOREQUEST, 0);
		task_lock(child);			       IRQ_NOREQUEST, 0);
		child->vfork_done = NULL;			       IRQ_NOREQUEST, 0);
		task_unlock(child);			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	put_task_struct(child);			       IRQ_NOREQUEST, 0);
	return killed;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/* Please note the differences between mmput and mm_release.			       IRQ_NOREQUEST, 0);
 * mmput is called whenever we stop holding onto a mm_struct,			       IRQ_NOREQUEST, 0);
 * error success whatever.			       IRQ_NOREQUEST, 0);
 *			       IRQ_NOREQUEST, 0);
 * mm_release is called after a mm_struct has been removed			       IRQ_NOREQUEST, 0);
 * from the current process.			       IRQ_NOREQUEST, 0);
 *			       IRQ_NOREQUEST, 0);
 * This difference is important for error handling, when we			       IRQ_NOREQUEST, 0);
 * only half set up a mm_struct for a new process and need to restore			       IRQ_NOREQUEST, 0);
 * the old one.  Because we mmput the new mm_struct before			       IRQ_NOREQUEST, 0);
 * restoring the old one. . .			       IRQ_NOREQUEST, 0);
 * Eric Biederman 10 January 1998			       IRQ_NOREQUEST, 0);
 */			       IRQ_NOREQUEST, 0);
void mm_release(struct task_struct *tsk, struct mm_struct *mm)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	/* Get rid of any futexes when releasing the mm */			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_FUTEX			       IRQ_NOREQUEST, 0);
	if (unlikely(tsk->robust_list)) {			       IRQ_NOREQUEST, 0);
		exit_robust_list(tsk);			       IRQ_NOREQUEST, 0);
		tsk->robust_list = NULL;			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_COMPAT			       IRQ_NOREQUEST, 0);
	if (unlikely(tsk->compat_robust_list)) {			       IRQ_NOREQUEST, 0);
		compat_exit_robust_list(tsk);			       IRQ_NOREQUEST, 0);
		tsk->compat_robust_list = NULL;			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
	if (unlikely(!list_empty(&tsk->pi_state_list)))			       IRQ_NOREQUEST, 0);
		exit_pi_state_list(tsk);			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	uprobe_free_utask(tsk);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/* Get rid of any cached register state */			       IRQ_NOREQUEST, 0);
	deactivate_mm(tsk, mm);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * If we're exiting normally, clear a user-space tid field if			       IRQ_NOREQUEST, 0);
	 * requested.  We leave this alone when dying by signal, to leave			       IRQ_NOREQUEST, 0);
	 * the value intact in a core dump, and to save the unnecessary			       IRQ_NOREQUEST, 0);
	 * trouble, say, a killed vfork parent shouldn't touch this mm.			       IRQ_NOREQUEST, 0);
	 * Userland only wants this done for a sys_exit.			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	if (tsk->clear_child_tid) {			       IRQ_NOREQUEST, 0);
		if (!(tsk->flags & PF_SIGNALED) &&			       IRQ_NOREQUEST, 0);
		    atomic_read(&mm->mm_users) > 1) {			       IRQ_NOREQUEST, 0);
			/*			       IRQ_NOREQUEST, 0);
			 * We don't check the error code - if userspace has			       IRQ_NOREQUEST, 0);
			 * not set up a proper pointer then tough luck.			       IRQ_NOREQUEST, 0);
			 */			       IRQ_NOREQUEST, 0);
			put_user(0, tsk->clear_child_tid);			       IRQ_NOREQUEST, 0);
			sys_futex(tsk->clear_child_tid, FUTEX_WAKE,			       IRQ_NOREQUEST, 0);
					1, NULL, NULL, 0);			       IRQ_NOREQUEST, 0);
		}			       IRQ_NOREQUEST, 0);
		tsk->clear_child_tid = NULL;			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * All done, finally we can wake up parent and return this mm to him.			       IRQ_NOREQUEST, 0);
	 * Also kthread_stop() uses this completion for synchronization.			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	if (tsk->vfork_done)			       IRQ_NOREQUEST, 0);
		complete_vfork_done(tsk);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/*			       IRQ_NOREQUEST, 0);
 * Allocate a new mm structure and copy contents from the			       IRQ_NOREQUEST, 0);
 * mm structure of the passed in task structure.			       IRQ_NOREQUEST, 0);
 */			       IRQ_NOREQUEST, 0);
static struct mm_struct *dup_mm(struct task_struct *tsk)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	struct mm_struct *mm, *oldmm = current->mm;			       IRQ_NOREQUEST, 0);
	int err;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	mm = allocate_mm();			       IRQ_NOREQUEST, 0);
	if (!mm)			       IRQ_NOREQUEST, 0);
		goto fail_nomem;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	memcpy(mm, oldmm, sizeof(*mm));			       IRQ_NOREQUEST, 0);
	mm_init_cpumask(mm);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
#if defined(CONFIG_TRANSPARENT_HUGEPAGE) && !USE_SPLIT_PMD_PTLOCKS			       IRQ_NOREQUEST, 0);
	mm->pmd_huge_pte = NULL;			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
	if (!mm_init(mm, tsk))			       IRQ_NOREQUEST, 0);
		goto fail_nomem;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	if (init_new_context(tsk, mm))			       IRQ_NOREQUEST, 0);
		goto fail_nocontext;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	dup_mm_exe_file(oldmm, mm);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	err = dup_mmap(mm, oldmm);			       IRQ_NOREQUEST, 0);
	if (err)			       IRQ_NOREQUEST, 0);
		goto free_pt;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	mm->hiwater_rss = get_mm_rss(mm);			       IRQ_NOREQUEST, 0);
	mm->hiwater_vm = mm->total_vm;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	if (mm->binfmt && !try_module_get(mm->binfmt->module))			       IRQ_NOREQUEST, 0);
		goto free_pt;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	return mm;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
free_pt:			       IRQ_NOREQUEST, 0);
	/* don't put binfmt in mmput, we haven't got module yet */			       IRQ_NOREQUEST, 0);
	mm->binfmt = NULL;			       IRQ_NOREQUEST, 0);
	mmput(mm);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
fail_nomem:			       IRQ_NOREQUEST, 0);
	return NULL;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
fail_nocontext:			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * If init_new_context() failed, we cannot use mmput() to free the mm			       IRQ_NOREQUEST, 0);
	 * because it calls destroy_context()			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	mm_free_pgd(mm);			       IRQ_NOREQUEST, 0);
	free_mm(mm);			       IRQ_NOREQUEST, 0);
	return NULL;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static int copy_mm(unsigned long clone_flags, struct task_struct *tsk)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	struct mm_struct *mm, *oldmm;			       IRQ_NOREQUEST, 0);
	int retval;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	tsk->min_flt = tsk->maj_flt = 0;			       IRQ_NOREQUEST, 0);
	tsk->nvcsw = tsk->nivcsw = 0;			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_DETECT_HUNG_TASK			       IRQ_NOREQUEST, 0);
	tsk->last_switch_count = tsk->nvcsw + tsk->nivcsw;			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	tsk->mm = NULL;			       IRQ_NOREQUEST, 0);
	tsk->active_mm = NULL;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * Are we cloning a kernel thread?			       IRQ_NOREQUEST, 0);
	 *			       IRQ_NOREQUEST, 0);
	 * We need to steal a active VM for that..			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	oldmm = current->mm;			       IRQ_NOREQUEST, 0);
	if (!oldmm)			       IRQ_NOREQUEST, 0);
		return 0;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	if (clone_flags & CLONE_VM) {			       IRQ_NOREQUEST, 0);
		atomic_inc(&oldmm->mm_users);			       IRQ_NOREQUEST, 0);
		mm = oldmm;			       IRQ_NOREQUEST, 0);
		goto good_mm;			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	retval = -ENOMEM;			       IRQ_NOREQUEST, 0);
	mm = dup_mm(tsk);			       IRQ_NOREQUEST, 0);
	if (!mm)			       IRQ_NOREQUEST, 0);
		goto fail_nomem;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
good_mm:			       IRQ_NOREQUEST, 0);
	tsk->mm = mm;			       IRQ_NOREQUEST, 0);
	tsk->active_mm = mm;			       IRQ_NOREQUEST, 0);
	return 0;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
fail_nomem:			       IRQ_NOREQUEST, 0);
	return retval;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static int copy_fs(unsigned long clone_flags, struct task_struct *tsk)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	struct fs_struct *fs = current->fs;			       IRQ_NOREQUEST, 0);
	if (clone_flags & CLONE_FS) {			       IRQ_NOREQUEST, 0);
		/* tsk->fs is already what we want */			       IRQ_NOREQUEST, 0);
		spin_lock(&fs->lock);			       IRQ_NOREQUEST, 0);
		if (fs->in_exec) {			       IRQ_NOREQUEST, 0);
			spin_unlock(&fs->lock);			       IRQ_NOREQUEST, 0);
			return -EAGAIN;			       IRQ_NOREQUEST, 0);
		}			       IRQ_NOREQUEST, 0);
		fs->users++;			       IRQ_NOREQUEST, 0);
		spin_unlock(&fs->lock);			       IRQ_NOREQUEST, 0);
		return 0;			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
	tsk->fs = copy_fs_struct(fs);			       IRQ_NOREQUEST, 0);
	if (!tsk->fs)			       IRQ_NOREQUEST, 0);
		return -ENOMEM;			       IRQ_NOREQUEST, 0);
	return 0;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static int copy_files(unsigned long clone_flags, struct task_struct *tsk)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	struct files_struct *oldf, *newf;			       IRQ_NOREQUEST, 0);
	int error = 0;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * A background process may not have any files ...			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	oldf = current->files;			       IRQ_NOREQUEST, 0);
	if (!oldf)			       IRQ_NOREQUEST, 0);
		goto out;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	if (clone_flags & CLONE_FILES) {			       IRQ_NOREQUEST, 0);
		atomic_inc(&oldf->count);			       IRQ_NOREQUEST, 0);
		goto out;			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	newf = dup_fd(oldf, &error);			       IRQ_NOREQUEST, 0);
	if (!newf)			       IRQ_NOREQUEST, 0);
		goto out;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	tsk->files = newf;			       IRQ_NOREQUEST, 0);
	error = 0;			       IRQ_NOREQUEST, 0);
out:			       IRQ_NOREQUEST, 0);
	return error;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static int copy_io(unsigned long clone_flags, struct task_struct *tsk)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_BLOCK			       IRQ_NOREQUEST, 0);
	struct io_context *ioc = current->io_context;			       IRQ_NOREQUEST, 0);
	struct io_context *new_ioc;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	if (!ioc)			       IRQ_NOREQUEST, 0);
		return 0;			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * Share io context with parent, if CLONE_IO is set			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	if (clone_flags & CLONE_IO) {			       IRQ_NOREQUEST, 0);
		ioc_task_link(ioc);			       IRQ_NOREQUEST, 0);
		tsk->io_context = ioc;			       IRQ_NOREQUEST, 0);
	} else if (ioprio_valid(ioc->ioprio)) {			       IRQ_NOREQUEST, 0);
		new_ioc = get_task_io_context(tsk, GFP_KERNEL, NUMA_NO_NODE);			       IRQ_NOREQUEST, 0);
		if (unlikely(!new_ioc))			       IRQ_NOREQUEST, 0);
			return -ENOMEM;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		new_ioc->ioprio = ioc->ioprio;			       IRQ_NOREQUEST, 0);
		put_io_context(new_ioc);			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
	return 0;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static int copy_sighand(unsigned long clone_flags, struct task_struct *tsk)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	struct sighand_struct *sig;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	if (clone_flags & CLONE_SIGHAND) {			       IRQ_NOREQUEST, 0);
		atomic_inc(&current->sighand->count);			       IRQ_NOREQUEST, 0);
		return 0;			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
	sig = kmem_cache_alloc(sighand_cachep, GFP_KERNEL);			       IRQ_NOREQUEST, 0);
	rcu_assign_pointer(tsk->sighand, sig);			       IRQ_NOREQUEST, 0);
	if (!sig)			       IRQ_NOREQUEST, 0);
		return -ENOMEM;			       IRQ_NOREQUEST, 0);
	atomic_set(&sig->count, 1);			       IRQ_NOREQUEST, 0);
	memcpy(sig->action, current->sighand->action, sizeof(sig->action));			       IRQ_NOREQUEST, 0);
	return 0;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
void __cleanup_sighand(struct sighand_struct *sighand)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	if (atomic_dec_and_test(&sighand->count)) {			       IRQ_NOREQUEST, 0);
		signalfd_cleanup(sighand);			       IRQ_NOREQUEST, 0);
		kmem_cache_free(sighand_cachep, sighand);			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/*			       IRQ_NOREQUEST, 0);
 * Initialize POSIX timer handling for a thread group.			       IRQ_NOREQUEST, 0);
 */			       IRQ_NOREQUEST, 0);
static void posix_cpu_timers_init_group(struct signal_struct *sig)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	unsigned long cpu_limit;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/* Thread group counters. */			       IRQ_NOREQUEST, 0);
	thread_group_cputime_init(sig);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	cpu_limit = ACCESS_ONCE(sig->rlim[RLIMIT_CPU].rlim_cur);			       IRQ_NOREQUEST, 0);
	if (cpu_limit != RLIM_INFINITY) {			       IRQ_NOREQUEST, 0);
		sig->cputime_expires.prof_exp = secs_to_cputime(cpu_limit);			       IRQ_NOREQUEST, 0);
		sig->cputimer.running = 1;			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/* The timer lists. */			       IRQ_NOREQUEST, 0);
	INIT_LIST_HEAD(&sig->cpu_timers[0]);			       IRQ_NOREQUEST, 0);
	INIT_LIST_HEAD(&sig->cpu_timers[1]);			       IRQ_NOREQUEST, 0);
	INIT_LIST_HEAD(&sig->cpu_timers[2]);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static int copy_signal(unsigned long clone_flags, struct task_struct *tsk)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	struct signal_struct *sig;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	if (clone_flags & CLONE_THREAD)			       IRQ_NOREQUEST, 0);
		return 0;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	sig = kmem_cache_zalloc(signal_cachep, GFP_KERNEL);			       IRQ_NOREQUEST, 0);
	tsk->signal = sig;			       IRQ_NOREQUEST, 0);
	if (!sig)			       IRQ_NOREQUEST, 0);
		return -ENOMEM;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	sig->nr_threads = 1;			       IRQ_NOREQUEST, 0);
	atomic_set(&sig->live, 1);			       IRQ_NOREQUEST, 0);
	atomic_set(&sig->sigcnt, 1);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/* list_add(thread_node, thread_head) without INIT_LIST_HEAD() */			       IRQ_NOREQUEST, 0);
	sig->thread_head = (struct list_head)LIST_HEAD_INIT(tsk->thread_node);			       IRQ_NOREQUEST, 0);
	tsk->thread_node = (struct list_head)LIST_HEAD_INIT(sig->thread_head);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	init_waitqueue_head(&sig->wait_chldexit);			       IRQ_NOREQUEST, 0);
	sig->curr_target = tsk;			       IRQ_NOREQUEST, 0);
	init_sigpending(&sig->shared_pending);			       IRQ_NOREQUEST, 0);
	INIT_LIST_HEAD(&sig->posix_timers);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	hrtimer_init(&sig->real_timer, CLOCK_MONOTONIC, HRTIMER_MODE_REL);			       IRQ_NOREQUEST, 0);
	sig->real_timer.function = it_real_fn;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	task_lock(current->group_leader);			       IRQ_NOREQUEST, 0);
	memcpy(sig->rlim, current->signal->rlim, sizeof sig->rlim);			       IRQ_NOREQUEST, 0);
	task_unlock(current->group_leader);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	posix_cpu_timers_init_group(sig);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	tty_audit_fork(sig);			       IRQ_NOREQUEST, 0);
	sched_autogroup_fork(sig);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_CGROUPS			       IRQ_NOREQUEST, 0);
	init_rwsem(&sig->group_rwsem);			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	sig->oom_score_adj = current->signal->oom_score_adj;			       IRQ_NOREQUEST, 0);
	sig->oom_score_adj_min = current->signal->oom_score_adj_min;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	sig->has_child_subreaper = current->signal->has_child_subreaper ||			       IRQ_NOREQUEST, 0);
				   current->signal->is_child_subreaper;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	mutex_init(&sig->cred_guard_mutex);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	return 0;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static void copy_flags(unsigned long clone_flags, struct task_struct *p)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	unsigned long new_flags = p->flags;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	new_flags &= ~(PF_SUPERPRIV | PF_WQ_WORKER);			       IRQ_NOREQUEST, 0);
	new_flags |= PF_FORKNOEXEC;			       IRQ_NOREQUEST, 0);
	p->flags = new_flags;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
SYSCALL_DEFINE1(set_tid_address, int __user *, tidptr)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	current->clear_child_tid = tidptr;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	return task_pid_vnr(current);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static void rt_mutex_init_task(struct task_struct *p)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	raw_spin_lock_init(&p->pi_lock);			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_RT_MUTEXES			       IRQ_NOREQUEST, 0);
	p->pi_waiters = RB_ROOT;			       IRQ_NOREQUEST, 0);
	p->pi_waiters_leftmost = NULL;			       IRQ_NOREQUEST, 0);
	p->pi_blocked_on = NULL;			       IRQ_NOREQUEST, 0);
	p->pi_top_task = NULL;			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_MM_OWNER			       IRQ_NOREQUEST, 0);
void mm_init_owner(struct mm_struct *mm, struct task_struct *p)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	mm->owner = p;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
#endif /* CONFIG_MM_OWNER */			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/*			       IRQ_NOREQUEST, 0);
 * Initialize POSIX timer handling for a single task.			       IRQ_NOREQUEST, 0);
 */			       IRQ_NOREQUEST, 0);
static void posix_cpu_timers_init(struct task_struct *tsk)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	tsk->cputime_expires.prof_exp = 0;			       IRQ_NOREQUEST, 0);
	tsk->cputime_expires.virt_exp = 0;			       IRQ_NOREQUEST, 0);
	tsk->cputime_expires.sched_exp = 0;			       IRQ_NOREQUEST, 0);
	INIT_LIST_HEAD(&tsk->cpu_timers[0]);			       IRQ_NOREQUEST, 0);
	INIT_LIST_HEAD(&tsk->cpu_timers[1]);			       IRQ_NOREQUEST, 0);
	INIT_LIST_HEAD(&tsk->cpu_timers[2]);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static inline void			       IRQ_NOREQUEST, 0);
init_task_pid(struct task_struct *task, enum pid_type type, struct pid *pid)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	 task->pids[type].pid = pid;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/*			       IRQ_NOREQUEST, 0);
 * This creates a new process as a copy of the old one,			       IRQ_NOREQUEST, 0);
 * but does not actually start it yet.			       IRQ_NOREQUEST, 0);
 *			       IRQ_NOREQUEST, 0);
 * It copies the registers, and all the appropriate			       IRQ_NOREQUEST, 0);
 * parts of the process environment (as per the clone			       IRQ_NOREQUEST, 0);
 * flags). The actual kick-off is left to the caller.			       IRQ_NOREQUEST, 0);
 */			       IRQ_NOREQUEST, 0);
static struct task_struct *copy_process(unsigned long clone_flags,			       IRQ_NOREQUEST, 0);
					unsigned long stack_start,			       IRQ_NOREQUEST, 0);
					unsigned long stack_size,			       IRQ_NOREQUEST, 0);
					int __user *child_tidptr,			       IRQ_NOREQUEST, 0);
					struct pid *pid,			       IRQ_NOREQUEST, 0);
					int trace)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	int retval;			       IRQ_NOREQUEST, 0);
	struct task_struct *p;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	if ((clone_flags & (CLONE_NEWNS|CLONE_FS)) == (CLONE_NEWNS|CLONE_FS))			       IRQ_NOREQUEST, 0);
		return ERR_PTR(-EINVAL);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	if ((clone_flags & (CLONE_NEWUSER|CLONE_FS)) == (CLONE_NEWUSER|CLONE_FS))			       IRQ_NOREQUEST, 0);
		return ERR_PTR(-EINVAL);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * Thread groups must share signals as well, and detached threads			       IRQ_NOREQUEST, 0);
	 * can only be started up within the thread group.			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	if ((clone_flags & CLONE_THREAD) && !(clone_flags & CLONE_SIGHAND))			       IRQ_NOREQUEST, 0);
		return ERR_PTR(-EINVAL);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * Shared signal handlers imply shared VM. By way of the above,			       IRQ_NOREQUEST, 0);
	 * thread groups also imply shared VM. Blocking this case allows			       IRQ_NOREQUEST, 0);
	 * for various simplifications in other code.			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	if ((clone_flags & CLONE_SIGHAND) && !(clone_flags & CLONE_VM))			       IRQ_NOREQUEST, 0);
		return ERR_PTR(-EINVAL);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * Siblings of global init remain as zombies on exit since they are			       IRQ_NOREQUEST, 0);
	 * not reaped by their parent (swapper). To solve this and to avoid			       IRQ_NOREQUEST, 0);
	 * multi-rooted process trees, prevent global and container-inits			       IRQ_NOREQUEST, 0);
	 * from creating siblings.			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	if ((clone_flags & CLONE_PARENT) &&			       IRQ_NOREQUEST, 0);
				current->signal->flags & SIGNAL_UNKILLABLE)			       IRQ_NOREQUEST, 0);
		return ERR_PTR(-EINVAL);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * If the new process will be in a different pid or user namespace			       IRQ_NOREQUEST, 0);
	 * do not allow it to share a thread group or signal handlers or			       IRQ_NOREQUEST, 0);
	 * parent with the forking task.			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	if (clone_flags & CLONE_SIGHAND) {			       IRQ_NOREQUEST, 0);
		if ((clone_flags & (CLONE_NEWUSER | CLONE_NEWPID)) ||			       IRQ_NOREQUEST, 0);
		    (task_active_pid_ns(current) !=			       IRQ_NOREQUEST, 0);
				current->nsproxy->pid_ns_for_children))			       IRQ_NOREQUEST, 0);
			return ERR_PTR(-EINVAL);			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	retval = security_task_create(clone_flags);			       IRQ_NOREQUEST, 0);
	if (retval)			       IRQ_NOREQUEST, 0);
		goto fork_out;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	retval = -ENOMEM;			       IRQ_NOREQUEST, 0);
	p = dup_task_struct(current);			       IRQ_NOREQUEST, 0);
	if (!p)			       IRQ_NOREQUEST, 0);
		goto fork_out;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	ftrace_graph_init_task(p);			       IRQ_NOREQUEST, 0);
	get_seccomp_filter(p);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	rt_mutex_init_task(p);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_PROVE_LOCKING			       IRQ_NOREQUEST, 0);
	DEBUG_LOCKS_WARN_ON(!p->hardirqs_enabled);			       IRQ_NOREQUEST, 0);
	DEBUG_LOCKS_WARN_ON(!p->softirqs_enabled);			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
	retval = -EAGAIN;			       IRQ_NOREQUEST, 0);
	if (atomic_read(&p->real_cred->user->processes) >=			       IRQ_NOREQUEST, 0);
			task_rlimit(p, RLIMIT_NPROC)) {			       IRQ_NOREQUEST, 0);
		if (p->real_cred->user != INIT_USER &&			       IRQ_NOREQUEST, 0);
		    !capable(CAP_SYS_RESOURCE) && !capable(CAP_SYS_ADMIN))			       IRQ_NOREQUEST, 0);
			goto bad_fork_free;			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
	current->flags &= ~PF_NPROC_EXCEEDED;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	retval = copy_creds(p, clone_flags);			       IRQ_NOREQUEST, 0);
	if (retval < 0)			       IRQ_NOREQUEST, 0);
		goto bad_fork_free;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * If multiple threads are within copy_process(), then this check			       IRQ_NOREQUEST, 0);
	 * triggers too late. This doesn't hurt, the check is only there			       IRQ_NOREQUEST, 0);
	 * to stop root fork bombs.			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	retval = -EAGAIN;			       IRQ_NOREQUEST, 0);
	if (nr_threads >= max_threads)			       IRQ_NOREQUEST, 0);
		goto bad_fork_cleanup_count;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	if (!try_module_get(task_thread_info(p)->exec_domain->module))			       IRQ_NOREQUEST, 0);
		goto bad_fork_cleanup_count;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	delayacct_tsk_init(p);	/* Must remain after dup_task_struct() */			       IRQ_NOREQUEST, 0);
	copy_flags(clone_flags, p);			       IRQ_NOREQUEST, 0);
	INIT_LIST_HEAD(&p->children);			       IRQ_NOREQUEST, 0);
	INIT_LIST_HEAD(&p->sibling);			       IRQ_NOREQUEST, 0);
	rcu_copy_process(p);			       IRQ_NOREQUEST, 0);
	p->vfork_done = NULL;			       IRQ_NOREQUEST, 0);
	spin_lock_init(&p->alloc_lock);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	init_sigpending(&p->pending);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	p->utime = p->stime = p->gtime = 0;			       IRQ_NOREQUEST, 0);
	p->utimescaled = p->stimescaled = 0;			       IRQ_NOREQUEST, 0);
#ifndef CONFIG_VIRT_CPU_ACCOUNTING_NATIVE			       IRQ_NOREQUEST, 0);
	p->prev_cputime.utime = p->prev_cputime.stime = 0;			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_VIRT_CPU_ACCOUNTING_GEN			       IRQ_NOREQUEST, 0);
	seqlock_init(&p->vtime_seqlock);			       IRQ_NOREQUEST, 0);
	p->vtime_snap = 0;			       IRQ_NOREQUEST, 0);
	p->vtime_snap_whence = VTIME_SLEEPING;			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
#if defined(SPLIT_RSS_COUNTING)			       IRQ_NOREQUEST, 0);
	memset(&p->rss_stat, 0, sizeof(p->rss_stat));			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	p->default_timer_slack_ns = current->timer_slack_ns;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	task_io_accounting_init(&p->ioac);			       IRQ_NOREQUEST, 0);
	acct_clear_integrals(p);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	posix_cpu_timers_init(p);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	do_posix_clock_monotonic_gettime(&p->start_time);			       IRQ_NOREQUEST, 0);
	p->real_start_time = p->start_time;			       IRQ_NOREQUEST, 0);
	monotonic_to_bootbased(&p->real_start_time);			       IRQ_NOREQUEST, 0);
	p->io_context = NULL;			       IRQ_NOREQUEST, 0);
	p->audit_context = NULL;			       IRQ_NOREQUEST, 0);
	if (clone_flags & CLONE_THREAD)			       IRQ_NOREQUEST, 0);
		threadgroup_change_begin(current);			       IRQ_NOREQUEST, 0);
	cgroup_fork(p);			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_NUMA			       IRQ_NOREQUEST, 0);
	p->mempolicy = mpol_dup(p->mempolicy);			       IRQ_NOREQUEST, 0);
	if (IS_ERR(p->mempolicy)) {			       IRQ_NOREQUEST, 0);
		retval = PTR_ERR(p->mempolicy);			       IRQ_NOREQUEST, 0);
		p->mempolicy = NULL;			       IRQ_NOREQUEST, 0);
		goto bad_fork_cleanup_cgroup;			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
	mpol_fix_fork_child_flag(p);			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_CPUSETS			       IRQ_NOREQUEST, 0);
	p->cpuset_mem_spread_rotor = NUMA_NO_NODE;			       IRQ_NOREQUEST, 0);
	p->cpuset_slab_spread_rotor = NUMA_NO_NODE;			       IRQ_NOREQUEST, 0);
	seqcount_init(&p->mems_allowed_seq);			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_TRACE_IRQFLAGS			       IRQ_NOREQUEST, 0);
	p->irq_events = 0;			       IRQ_NOREQUEST, 0);
	p->hardirqs_enabled = 0;			       IRQ_NOREQUEST, 0);
	p->hardirq_enable_ip = 0;			       IRQ_NOREQUEST, 0);
	p->hardirq_enable_event = 0;			       IRQ_NOREQUEST, 0);
	p->hardirq_disable_ip = _THIS_IP_;			       IRQ_NOREQUEST, 0);
	p->hardirq_disable_event = 0;			       IRQ_NOREQUEST, 0);
	p->softirqs_enabled = 1;			       IRQ_NOREQUEST, 0);
	p->softirq_enable_ip = _THIS_IP_;			       IRQ_NOREQUEST, 0);
	p->softirq_enable_event = 0;			       IRQ_NOREQUEST, 0);
	p->softirq_disable_ip = 0;			       IRQ_NOREQUEST, 0);
	p->softirq_disable_event = 0;			       IRQ_NOREQUEST, 0);
	p->hardirq_context = 0;			       IRQ_NOREQUEST, 0);
	p->softirq_context = 0;			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_LOCKDEP			       IRQ_NOREQUEST, 0);
	p->lockdep_depth = 0; /* no locks held yet */			       IRQ_NOREQUEST, 0);
	p->curr_chain_key = 0;			       IRQ_NOREQUEST, 0);
	p->lockdep_recursion = 0;			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_DEBUG_MUTEXES			       IRQ_NOREQUEST, 0);
	p->blocked_on = NULL; /* not blocked yet */			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_MEMCG			       IRQ_NOREQUEST, 0);
	p->memcg_batch.do_batch = 0;			       IRQ_NOREQUEST, 0);
	p->memcg_batch.memcg = NULL;			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_BCACHE			       IRQ_NOREQUEST, 0);
	p->sequential_io	= 0;			       IRQ_NOREQUEST, 0);
	p->sequential_io_avg	= 0;			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/* Perform scheduler related setup. Assign this task to a CPU. */			       IRQ_NOREQUEST, 0);
	retval = sched_fork(clone_flags, p);			       IRQ_NOREQUEST, 0);
	if (retval)			       IRQ_NOREQUEST, 0);
		goto bad_fork_cleanup_policy;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	retval = perf_event_init_task(p);			       IRQ_NOREQUEST, 0);
	if (retval)			       IRQ_NOREQUEST, 0);
		goto bad_fork_cleanup_policy;			       IRQ_NOREQUEST, 0);
	retval = audit_alloc(p);			       IRQ_NOREQUEST, 0);
	if (retval)			       IRQ_NOREQUEST, 0);
		goto bad_fork_cleanup_policy;			       IRQ_NOREQUEST, 0);
	/* copy all the process information */			       IRQ_NOREQUEST, 0);
	retval = copy_semundo(clone_flags, p);			       IRQ_NOREQUEST, 0);
	if (retval)			       IRQ_NOREQUEST, 0);
		goto bad_fork_cleanup_audit;			       IRQ_NOREQUEST, 0);
	retval = copy_files(clone_flags, p);			       IRQ_NOREQUEST, 0);
	if (retval)			       IRQ_NOREQUEST, 0);
		goto bad_fork_cleanup_semundo;			       IRQ_NOREQUEST, 0);
	retval = copy_fs(clone_flags, p);			       IRQ_NOREQUEST, 0);
	if (retval)			       IRQ_NOREQUEST, 0);
		goto bad_fork_cleanup_files;			       IRQ_NOREQUEST, 0);
	retval = copy_sighand(clone_flags, p);			       IRQ_NOREQUEST, 0);
	if (retval)			       IRQ_NOREQUEST, 0);
		goto bad_fork_cleanup_fs;			       IRQ_NOREQUEST, 0);
	retval = copy_signal(clone_flags, p);			       IRQ_NOREQUEST, 0);
	if (retval)			       IRQ_NOREQUEST, 0);
		goto bad_fork_cleanup_sighand;			       IRQ_NOREQUEST, 0);
	retval = copy_mm(clone_flags, p);			       IRQ_NOREQUEST, 0);
	if (retval)			       IRQ_NOREQUEST, 0);
		goto bad_fork_cleanup_signal;			       IRQ_NOREQUEST, 0);
	retval = copy_namespaces(clone_flags, p);			       IRQ_NOREQUEST, 0);
	if (retval)			       IRQ_NOREQUEST, 0);
		goto bad_fork_cleanup_mm;			       IRQ_NOREQUEST, 0);
	retval = copy_io(clone_flags, p);			       IRQ_NOREQUEST, 0);
	if (retval)			       IRQ_NOREQUEST, 0);
		goto bad_fork_cleanup_namespaces;			       IRQ_NOREQUEST, 0);
	retval = copy_thread(clone_flags, stack_start, stack_size, p);			       IRQ_NOREQUEST, 0);
	if (retval)			       IRQ_NOREQUEST, 0);
		goto bad_fork_cleanup_io;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	if (pid != &init_struct_pid) {			       IRQ_NOREQUEST, 0);
		retval = -ENOMEM;			       IRQ_NOREQUEST, 0);
		pid = alloc_pid(p->nsproxy->pid_ns_for_children);			       IRQ_NOREQUEST, 0);
		if (!pid)			       IRQ_NOREQUEST, 0);
			goto bad_fork_cleanup_io;			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	p->set_child_tid = (clone_flags & CLONE_CHILD_SETTID) ? child_tidptr : NULL;			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * Clear TID on mm_release()?			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	p->clear_child_tid = (clone_flags & CLONE_CHILD_CLEARTID) ? child_tidptr : NULL;			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_BLOCK			       IRQ_NOREQUEST, 0);
	p->plug = NULL;			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_FUTEX			       IRQ_NOREQUEST, 0);
	p->robust_list = NULL;			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_COMPAT			       IRQ_NOREQUEST, 0);
	p->compat_robust_list = NULL;			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
	INIT_LIST_HEAD(&p->pi_state_list);			       IRQ_NOREQUEST, 0);
	p->pi_state_cache = NULL;			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * sigaltstack should be cleared when sharing the same VM			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	if ((clone_flags & (CLONE_VM|CLONE_VFORK)) == CLONE_VM)			       IRQ_NOREQUEST, 0);
		p->sas_ss_sp = p->sas_ss_size = 0;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * Syscall tracing and stepping should be turned off in the			       IRQ_NOREQUEST, 0);
	 * child regardless of CLONE_PTRACE.			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	user_disable_single_step(p);			       IRQ_NOREQUEST, 0);
	clear_tsk_thread_flag(p, TIF_SYSCALL_TRACE);			       IRQ_NOREQUEST, 0);
#ifdef TIF_SYSCALL_EMU			       IRQ_NOREQUEST, 0);
	clear_tsk_thread_flag(p, TIF_SYSCALL_EMU);			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
	clear_all_latency_tracing(p);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/* ok, now we should be set up.. */			       IRQ_NOREQUEST, 0);
	p->pid = pid_nr(pid);			       IRQ_NOREQUEST, 0);
	if (clone_flags & CLONE_THREAD) {			       IRQ_NOREQUEST, 0);
		p->exit_signal = -1;			       IRQ_NOREQUEST, 0);
		p->group_leader = current->group_leader;			       IRQ_NOREQUEST, 0);
		p->tgid = current->tgid;			       IRQ_NOREQUEST, 0);
	} else {			       IRQ_NOREQUEST, 0);
		if (clone_flags & CLONE_PARENT)			       IRQ_NOREQUEST, 0);
			p->exit_signal = current->group_leader->exit_signal;			       IRQ_NOREQUEST, 0);
		else			       IRQ_NOREQUEST, 0);
			p->exit_signal = (clone_flags & CSIGNAL);			       IRQ_NOREQUEST, 0);
		p->group_leader = p;			       IRQ_NOREQUEST, 0);
		p->tgid = p->pid;			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	p->nr_dirtied = 0;			       IRQ_NOREQUEST, 0);
	p->nr_dirtied_pause = 128 >> (PAGE_SHIFT - 10);			       IRQ_NOREQUEST, 0);
	p->dirty_paused_when = 0;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	p->pdeath_signal = 0;			       IRQ_NOREQUEST, 0);
	INIT_LIST_HEAD(&p->thread_group);			       IRQ_NOREQUEST, 0);
	p->task_works = NULL;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * Make it visible to the rest of the system, but dont wake it up yet.			       IRQ_NOREQUEST, 0);
	 * Need tasklist lock for parent etc handling!			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	write_lock_irq(&tasklist_lock);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/* CLONE_PARENT re-uses the old parent */			       IRQ_NOREQUEST, 0);
	if (clone_flags & (CLONE_PARENT|CLONE_THREAD)) {			       IRQ_NOREQUEST, 0);
		p->real_parent = current->real_parent;			       IRQ_NOREQUEST, 0);
		p->parent_exec_id = current->parent_exec_id;			       IRQ_NOREQUEST, 0);
	} else {			       IRQ_NOREQUEST, 0);
		p->real_parent = current;			       IRQ_NOREQUEST, 0);
		p->parent_exec_id = current->self_exec_id;			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	spin_lock(&current->sighand->siglock);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * Process group and session signals need to be delivered to just the			       IRQ_NOREQUEST, 0);
	 * parent before the fork or both the parent and the child after the			       IRQ_NOREQUEST, 0);
	 * fork. Restart if a signal comes in before we add the new process to			       IRQ_NOREQUEST, 0);
	 * it's process group.			       IRQ_NOREQUEST, 0);
	 * A fatal signal pending means that current will exit, so the new			       IRQ_NOREQUEST, 0);
	 * thread can't slip out of an OOM kill (or normal SIGKILL).			       IRQ_NOREQUEST, 0);
	*/			       IRQ_NOREQUEST, 0);
	recalc_sigpending();			       IRQ_NOREQUEST, 0);
	if (signal_pending(current)) {			       IRQ_NOREQUEST, 0);
		spin_unlock(&current->sighand->siglock);			       IRQ_NOREQUEST, 0);
		write_unlock_irq(&tasklist_lock);			       IRQ_NOREQUEST, 0);
		retval = -ERESTARTNOINTR;			       IRQ_NOREQUEST, 0);
		goto bad_fork_free_pid;			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	if (likely(p->pid)) {			       IRQ_NOREQUEST, 0);
		ptrace_init_task(p, (clone_flags & CLONE_PTRACE) || trace);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		init_task_pid(p, PIDTYPE_PID, pid);			       IRQ_NOREQUEST, 0);
		if (thread_group_leader(p)) {			       IRQ_NOREQUEST, 0);
			init_task_pid(p, PIDTYPE_PGID, task_pgrp(current));			       IRQ_NOREQUEST, 0);
			init_task_pid(p, PIDTYPE_SID, task_session(current));			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
			if (is_child_reaper(pid)) {			       IRQ_NOREQUEST, 0);
				ns_of_pid(pid)->child_reaper = p;			       IRQ_NOREQUEST, 0);
				p->signal->flags |= SIGNAL_UNKILLABLE;			       IRQ_NOREQUEST, 0);
			}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
			p->signal->leader_pid = pid;			       IRQ_NOREQUEST, 0);
			p->signal->tty = tty_kref_get(current->signal->tty);			       IRQ_NOREQUEST, 0);
			list_add_tail(&p->sibling, &p->real_parent->children);			       IRQ_NOREQUEST, 0);
			list_add_tail_rcu(&p->tasks, &init_task.tasks);			       IRQ_NOREQUEST, 0);
			attach_pid(p, PIDTYPE_PGID);			       IRQ_NOREQUEST, 0);
			attach_pid(p, PIDTYPE_SID);			       IRQ_NOREQUEST, 0);
			__this_cpu_inc(process_counts);			       IRQ_NOREQUEST, 0);
		} else {			       IRQ_NOREQUEST, 0);
			current->signal->nr_threads++;			       IRQ_NOREQUEST, 0);
			atomic_inc(&current->signal->live);			       IRQ_NOREQUEST, 0);
			atomic_inc(&current->signal->sigcnt);			       IRQ_NOREQUEST, 0);
			list_add_tail_rcu(&p->thread_group,			       IRQ_NOREQUEST, 0);
					  &p->group_leader->thread_group);			       IRQ_NOREQUEST, 0);
			list_add_tail_rcu(&p->thread_node,			       IRQ_NOREQUEST, 0);
					  &p->signal->thread_head);			       IRQ_NOREQUEST, 0);
		}			       IRQ_NOREQUEST, 0);
		attach_pid(p, PIDTYPE_PID);			       IRQ_NOREQUEST, 0);
		nr_threads++;			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	total_forks++;			       IRQ_NOREQUEST, 0);
	spin_unlock(&current->sighand->siglock);			       IRQ_NOREQUEST, 0);
	write_unlock_irq(&tasklist_lock);			       IRQ_NOREQUEST, 0);
	proc_fork_connector(p);			       IRQ_NOREQUEST, 0);
	cgroup_post_fork(p);			       IRQ_NOREQUEST, 0);
	if (clone_flags & CLONE_THREAD)			       IRQ_NOREQUEST, 0);
		threadgroup_change_end(current);			       IRQ_NOREQUEST, 0);
	perf_event_fork(p);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	trace_task_newtask(p, clone_flags);			       IRQ_NOREQUEST, 0);
	uprobe_copy_process(p, clone_flags);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	return p;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
bad_fork_free_pid:			       IRQ_NOREQUEST, 0);
	if (pid != &init_struct_pid)			       IRQ_NOREQUEST, 0);
		free_pid(pid);			       IRQ_NOREQUEST, 0);
bad_fork_cleanup_io:			       IRQ_NOREQUEST, 0);
	if (p->io_context)			       IRQ_NOREQUEST, 0);
		exit_io_context(p);			       IRQ_NOREQUEST, 0);
bad_fork_cleanup_namespaces:			       IRQ_NOREQUEST, 0);
	exit_task_namespaces(p);			       IRQ_NOREQUEST, 0);
bad_fork_cleanup_mm:			       IRQ_NOREQUEST, 0);
	if (p->mm)			       IRQ_NOREQUEST, 0);
		mmput(p->mm);			       IRQ_NOREQUEST, 0);
bad_fork_cleanup_signal:			       IRQ_NOREQUEST, 0);
	if (!(clone_flags & CLONE_THREAD))			       IRQ_NOREQUEST, 0);
		free_signal_struct(p->signal);			       IRQ_NOREQUEST, 0);
bad_fork_cleanup_sighand:			       IRQ_NOREQUEST, 0);
	__cleanup_sighand(p->sighand);			       IRQ_NOREQUEST, 0);
bad_fork_cleanup_fs:			       IRQ_NOREQUEST, 0);
	exit_fs(p); /* blocking */			       IRQ_NOREQUEST, 0);
bad_fork_cleanup_files:			       IRQ_NOREQUEST, 0);
	exit_files(p); /* blocking */			       IRQ_NOREQUEST, 0);
bad_fork_cleanup_semundo:			       IRQ_NOREQUEST, 0);
	exit_sem(p);			       IRQ_NOREQUEST, 0);
bad_fork_cleanup_audit:			       IRQ_NOREQUEST, 0);
	audit_free(p);			       IRQ_NOREQUEST, 0);
bad_fork_cleanup_policy:			       IRQ_NOREQUEST, 0);
	perf_event_free_task(p);			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_NUMA			       IRQ_NOREQUEST, 0);
	mpol_put(p->mempolicy);			       IRQ_NOREQUEST, 0);
bad_fork_cleanup_cgroup:			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
	if (clone_flags & CLONE_THREAD)			       IRQ_NOREQUEST, 0);
		threadgroup_change_end(current);			       IRQ_NOREQUEST, 0);
	cgroup_exit(p, 0);			       IRQ_NOREQUEST, 0);
	delayacct_tsk_free(p);			       IRQ_NOREQUEST, 0);
	module_put(task_thread_info(p)->exec_domain->module);			       IRQ_NOREQUEST, 0);
bad_fork_cleanup_count:			       IRQ_NOREQUEST, 0);
	atomic_dec(&p->cred->user->processes);			       IRQ_NOREQUEST, 0);
	exit_creds(p);			       IRQ_NOREQUEST, 0);
bad_fork_free:			       IRQ_NOREQUEST, 0);
	free_task(p);			       IRQ_NOREQUEST, 0);
fork_out:			       IRQ_NOREQUEST, 0);
	return ERR_PTR(retval);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static inline void init_idle_pids(struct pid_link *links)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	enum pid_type type;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	for (type = PIDTYPE_PID; type < PIDTYPE_MAX; ++type) {			       IRQ_NOREQUEST, 0);
		INIT_HLIST_NODE(&links[type].node); /* not really needed */			       IRQ_NOREQUEST, 0);
		links[type].pid = &init_struct_pid;			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
struct task_struct *fork_idle(int cpu)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	struct task_struct *task;			       IRQ_NOREQUEST, 0);
	task = copy_process(CLONE_VM, 0, 0, NULL, &init_struct_pid, 0);			       IRQ_NOREQUEST, 0);
	if (!IS_ERR(task)) {			       IRQ_NOREQUEST, 0);
		init_idle_pids(task->pids);			       IRQ_NOREQUEST, 0);
		init_idle(task, cpu);			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	return task;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/*			       IRQ_NOREQUEST, 0);
 *  Ok, this is the main fork-routine.			       IRQ_NOREQUEST, 0);
 *			       IRQ_NOREQUEST, 0);
 * It copies the process, and if successful kick-starts			       IRQ_NOREQUEST, 0);
 * it and waits for it to finish using the VM if required.			       IRQ_NOREQUEST, 0);
 */			       IRQ_NOREQUEST, 0);
long do_fork(unsigned long clone_flags,			       IRQ_NOREQUEST, 0);
	      unsigned long stack_start,			       IRQ_NOREQUEST, 0);
	      unsigned long stack_size,			       IRQ_NOREQUEST, 0);
	      int __user *parent_tidptr,			       IRQ_NOREQUEST, 0);
	      int __user *child_tidptr)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	struct task_struct *p;			       IRQ_NOREQUEST, 0);
	int trace = 0;			       IRQ_NOREQUEST, 0);
	long nr;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * Determine whether and which event to report to ptracer.  When			       IRQ_NOREQUEST, 0);
	 * called from kernel_thread or CLONE_UNTRACED is explicitly			       IRQ_NOREQUEST, 0);
	 * requested, no event is reported; otherwise, report if the event			       IRQ_NOREQUEST, 0);
	 * for the type of forking is enabled.			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	if (!(clone_flags & CLONE_UNTRACED)) {			       IRQ_NOREQUEST, 0);
		if (clone_flags & CLONE_VFORK)			       IRQ_NOREQUEST, 0);
			trace = PTRACE_EVENT_VFORK;			       IRQ_NOREQUEST, 0);
		else if ((clone_flags & CSIGNAL) != SIGCHLD)			       IRQ_NOREQUEST, 0);
			trace = PTRACE_EVENT_CLONE;			       IRQ_NOREQUEST, 0);
		else			       IRQ_NOREQUEST, 0);
			trace = PTRACE_EVENT_FORK;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		if (likely(!ptrace_event_enabled(current, trace)))			       IRQ_NOREQUEST, 0);
			trace = 0;			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	p = copy_process(clone_flags, stack_start, stack_size,			       IRQ_NOREQUEST, 0);
			 child_tidptr, NULL, trace);			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * Do this prior waking up the new thread - the thread pointer			       IRQ_NOREQUEST, 0);
	 * might get invalid after that point, if the thread exits quickly.			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	if (!IS_ERR(p)) {			       IRQ_NOREQUEST, 0);
		struct completion vfork;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		trace_sched_process_fork(current, p);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		nr = task_pid_vnr(p);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		if (clone_flags & CLONE_PARENT_SETTID)			       IRQ_NOREQUEST, 0);
			put_user(nr, parent_tidptr);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		if (clone_flags & CLONE_VFORK) {			       IRQ_NOREQUEST, 0);
			p->vfork_done = &vfork;			       IRQ_NOREQUEST, 0);
			init_completion(&vfork);			       IRQ_NOREQUEST, 0);
			get_task_struct(p);			       IRQ_NOREQUEST, 0);
		}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		wake_up_new_task(p);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		/* forking complete and child started to run, tell ptracer */			       IRQ_NOREQUEST, 0);
		if (unlikely(trace))			       IRQ_NOREQUEST, 0);
			ptrace_event(trace, nr);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		if (clone_flags & CLONE_VFORK) {			       IRQ_NOREQUEST, 0);
			if (!wait_for_vfork_done(p, &vfork))			       IRQ_NOREQUEST, 0);
				ptrace_event(PTRACE_EVENT_VFORK_DONE, nr);			       IRQ_NOREQUEST, 0);
		}			       IRQ_NOREQUEST, 0);
	} else {			       IRQ_NOREQUEST, 0);
		nr = PTR_ERR(p);			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
	return nr;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/*			       IRQ_NOREQUEST, 0);
 * Create a kernel thread.			       IRQ_NOREQUEST, 0);
 */			       IRQ_NOREQUEST, 0);
pid_t kernel_thread(int (*fn)(void *), void *arg, unsigned long flags)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	return do_fork(flags|CLONE_VM|CLONE_UNTRACED, (unsigned long)fn,			       IRQ_NOREQUEST, 0);
		(unsigned long)arg, NULL, NULL);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
#ifdef __ARCH_WANT_SYS_FORK			       IRQ_NOREQUEST, 0);
SYSCALL_DEFINE0(fork)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_MMU			       IRQ_NOREQUEST, 0);
	return do_fork(SIGCHLD, 0, 0, NULL, NULL);			       IRQ_NOREQUEST, 0);
#else			       IRQ_NOREQUEST, 0);
	/* can not support in nommu mode */			       IRQ_NOREQUEST, 0);
	return -EINVAL;			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
#ifdef __ARCH_WANT_SYS_VFORK			       IRQ_NOREQUEST, 0);
SYSCALL_DEFINE0(vfork)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	return do_fork(CLONE_VFORK | CLONE_VM | SIGCHLD, 0,			       IRQ_NOREQUEST, 0);
			0, NULL, NULL);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
#ifdef __ARCH_WANT_SYS_CLONE			       IRQ_NOREQUEST, 0);
#ifdef CONFIG_CLONE_BACKWARDS			       IRQ_NOREQUEST, 0);
SYSCALL_DEFINE5(clone, unsigned long, clone_flags, unsigned long, newsp,			       IRQ_NOREQUEST, 0);
		 int __user *, parent_tidptr,			       IRQ_NOREQUEST, 0);
		 int, tls_val,			       IRQ_NOREQUEST, 0);
		 int __user *, child_tidptr)			       IRQ_NOREQUEST, 0);
#elif defined(CONFIG_CLONE_BACKWARDS2)			       IRQ_NOREQUEST, 0);
SYSCALL_DEFINE5(clone, unsigned long, newsp, unsigned long, clone_flags,			       IRQ_NOREQUEST, 0);
		 int __user *, parent_tidptr,			       IRQ_NOREQUEST, 0);
		 int __user *, child_tidptr,			       IRQ_NOREQUEST, 0);
		 int, tls_val)			       IRQ_NOREQUEST, 0);
#elif defined(CONFIG_CLONE_BACKWARDS3)			       IRQ_NOREQUEST, 0);
SYSCALL_DEFINE6(clone, unsigned long, clone_flags, unsigned long, newsp,			       IRQ_NOREQUEST, 0);
		int, stack_size,			       IRQ_NOREQUEST, 0);
		int __user *, parent_tidptr,			       IRQ_NOREQUEST, 0);
		int __user *, child_tidptr,			       IRQ_NOREQUEST, 0);
		int, tls_val)			       IRQ_NOREQUEST, 0);
#else			       IRQ_NOREQUEST, 0);
SYSCALL_DEFINE5(clone, unsigned long, clone_flags, unsigned long, newsp,			       IRQ_NOREQUEST, 0);
		 int __user *, parent_tidptr,			       IRQ_NOREQUEST, 0);
		 int __user *, child_tidptr,			       IRQ_NOREQUEST, 0);
		 int, tls_val)			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	return do_fork(clone_flags, newsp, 0, parent_tidptr, child_tidptr);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
#ifndef ARCH_MIN_MMSTRUCT_ALIGN			       IRQ_NOREQUEST, 0);
#define ARCH_MIN_MMSTRUCT_ALIGN 0			       IRQ_NOREQUEST, 0);
#endif			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
static void sighand_ctor(void *data)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	struct sighand_struct *sighand = data;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	spin_lock_init(&sighand->siglock);			       IRQ_NOREQUEST, 0);
	init_waitqueue_head(&sighand->signalfd_wqh);			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
void __init proc_caches_init(void)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	sighand_cachep = kmem_cache_create("sighand_cache",			       IRQ_NOREQUEST, 0);
			sizeof(struct sighand_struct), 0,			       IRQ_NOREQUEST, 0);
			SLAB_HWCACHE_ALIGN|SLAB_PANIC|SLAB_DESTROY_BY_RCU|			       IRQ_NOREQUEST, 0);
			SLAB_NOTRACK, sighand_ctor);			       IRQ_NOREQUEST, 0);
	signal_cachep = kmem_cache_create("signal_cache",			       IRQ_NOREQUEST, 0);
			sizeof(struct signal_struct), 0,			       IRQ_NOREQUEST, 0);
			SLAB_HWCACHE_ALIGN|SLAB_PANIC|SLAB_NOTRACK, NULL);			       IRQ_NOREQUEST, 0);
	files_cachep = kmem_cache_create("files_cache",			       IRQ_NOREQUEST, 0);
			sizeof(struct files_struct), 0,			       IRQ_NOREQUEST, 0);
			SLAB_HWCACHE_ALIGN|SLAB_PANIC|SLAB_NOTRACK, NULL);			       IRQ_NOREQUEST, 0);
	fs_cachep = kmem_cache_create("fs_cache",			       IRQ_NOREQUEST, 0);
			sizeof(struct fs_struct), 0,			       IRQ_NOREQUEST, 0);
			SLAB_HWCACHE_ALIGN|SLAB_PANIC|SLAB_NOTRACK, NULL);			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * FIXME! The "sizeof(struct mm_struct)" currently includes the			       IRQ_NOREQUEST, 0);
	 * whole struct cpumask for the OFFSTACK case. We could change			       IRQ_NOREQUEST, 0);
	 * this to *only* allocate as much of it as required by the			       IRQ_NOREQUEST, 0);
	 * maximum number of CPU's we can ever have.  The cpumask_allocation			       IRQ_NOREQUEST, 0);
	 * is at the end of the structure, exactly for that reason.			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	mm_cachep = kmem_cache_create("mm_struct",			       IRQ_NOREQUEST, 0);
			sizeof(struct mm_struct), ARCH_MIN_MMSTRUCT_ALIGN,			       IRQ_NOREQUEST, 0);
			SLAB_HWCACHE_ALIGN|SLAB_PANIC|SLAB_NOTRACK, NULL);			       IRQ_NOREQUEST, 0);
	vm_area_cachep = KMEM_CACHE(vm_area_struct, SLAB_PANIC);			       IRQ_NOREQUEST, 0);
	mmap_init();			       IRQ_NOREQUEST, 0);
	nsproxy_cache_init();			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/*			       IRQ_NOREQUEST, 0);
 * Check constraints on flags passed to the unshare system call.			       IRQ_NOREQUEST, 0);
 */			       IRQ_NOREQUEST, 0);
static int check_unshare_flags(unsigned long unshare_flags)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	if (unshare_flags & ~(CLONE_THREAD|CLONE_FS|CLONE_NEWNS|CLONE_SIGHAND|			       IRQ_NOREQUEST, 0);
				CLONE_VM|CLONE_FILES|CLONE_SYSVSEM|			       IRQ_NOREQUEST, 0);
				CLONE_NEWUTS|CLONE_NEWIPC|CLONE_NEWNET|			       IRQ_NOREQUEST, 0);
				CLONE_NEWUSER|CLONE_NEWPID))			       IRQ_NOREQUEST, 0);
		return -EINVAL;			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * Not implemented, but pretend it works if there is nothing to			       IRQ_NOREQUEST, 0);
	 * unshare. Note that unsharing CLONE_THREAD or CLONE_SIGHAND			       IRQ_NOREQUEST, 0);
	 * needs to unshare vm.			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	if (unshare_flags & (CLONE_THREAD | CLONE_SIGHAND | CLONE_VM)) {			       IRQ_NOREQUEST, 0);
		/* FIXME: get_task_mm() increments ->mm_users */			       IRQ_NOREQUEST, 0);
		if (atomic_read(&current->mm->mm_users) > 1)			       IRQ_NOREQUEST, 0);
			return -EINVAL;			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	return 0;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/*			       IRQ_NOREQUEST, 0);
 * Unshare the filesystem structure if it is being shared			       IRQ_NOREQUEST, 0);
 */			       IRQ_NOREQUEST, 0);
static int unshare_fs(unsigned long unshare_flags, struct fs_struct **new_fsp)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	struct fs_struct *fs = current->fs;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	if (!(unshare_flags & CLONE_FS) || !fs)			       IRQ_NOREQUEST, 0);
		return 0;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/* don't need lock here; in the worst case we'll do useless copy */			       IRQ_NOREQUEST, 0);
	if (fs->users == 1)			       IRQ_NOREQUEST, 0);
		return 0;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	*new_fsp = copy_fs_struct(fs);			       IRQ_NOREQUEST, 0);
	if (!*new_fsp)			       IRQ_NOREQUEST, 0);
		return -ENOMEM;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	return 0;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/*			       IRQ_NOREQUEST, 0);
 * Unshare file descriptor table if it is being shared			       IRQ_NOREQUEST, 0);
 */			       IRQ_NOREQUEST, 0);
static int unshare_fd(unsigned long unshare_flags, struct files_struct **new_fdp)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	struct files_struct *fd = current->files;			       IRQ_NOREQUEST, 0);
	int error = 0;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	if ((unshare_flags & CLONE_FILES) &&			       IRQ_NOREQUEST, 0);
	    (fd && atomic_read(&fd->count) > 1)) {			       IRQ_NOREQUEST, 0);
		*new_fdp = dup_fd(fd, &error);			       IRQ_NOREQUEST, 0);
		if (!*new_fdp)			       IRQ_NOREQUEST, 0);
			return error;			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	return 0;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/*			       IRQ_NOREQUEST, 0);
 * unshare allows a process to 'unshare' part of the process			       IRQ_NOREQUEST, 0);
 * context which was originally shared using clone.  copy_*			       IRQ_NOREQUEST, 0);
 * functions used by do_fork() cannot be used here directly			       IRQ_NOREQUEST, 0);
 * because they modify an inactive task_struct that is being			       IRQ_NOREQUEST, 0);
 * constructed. Here we are modifying the current, active,			       IRQ_NOREQUEST, 0);
 * task_struct.			       IRQ_NOREQUEST, 0);
 */			       IRQ_NOREQUEST, 0);
SYSCALL_DEFINE1(unshare, unsigned long, unshare_flags)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	struct fs_struct *fs, *new_fs = NULL;			       IRQ_NOREQUEST, 0);
	struct files_struct *fd, *new_fd = NULL;			       IRQ_NOREQUEST, 0);
	struct cred *new_cred = NULL;			       IRQ_NOREQUEST, 0);
	struct nsproxy *new_nsproxy = NULL;			       IRQ_NOREQUEST, 0);
	int do_sysvsem = 0;			       IRQ_NOREQUEST, 0);
	int err;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * If unsharing a user namespace must also unshare the thread.			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	if (unshare_flags & CLONE_NEWUSER)			       IRQ_NOREQUEST, 0);
		unshare_flags |= CLONE_THREAD | CLONE_FS;			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * If unsharing a thread from a thread group, must also unshare vm.			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	if (unshare_flags & CLONE_THREAD)			       IRQ_NOREQUEST, 0);
		unshare_flags |= CLONE_VM;			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * If unsharing vm, must also unshare signal handlers.			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	if (unshare_flags & CLONE_VM)			       IRQ_NOREQUEST, 0);
		unshare_flags |= CLONE_SIGHAND;			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * If unsharing namespace, must also unshare filesystem information.			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	if (unshare_flags & CLONE_NEWNS)			       IRQ_NOREQUEST, 0);
		unshare_flags |= CLONE_FS;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	err = check_unshare_flags(unshare_flags);			       IRQ_NOREQUEST, 0);
	if (err)			       IRQ_NOREQUEST, 0);
		goto bad_unshare_out;			       IRQ_NOREQUEST, 0);
	/*			       IRQ_NOREQUEST, 0);
	 * CLONE_NEWIPC must also detach from the undolist: after switching			       IRQ_NOREQUEST, 0);
	 * to a new ipc namespace, the semaphore arrays from the old			       IRQ_NOREQUEST, 0);
	 * namespace are unreachable.			       IRQ_NOREQUEST, 0);
	 */			       IRQ_NOREQUEST, 0);
	if (unshare_flags & (CLONE_NEWIPC|CLONE_SYSVSEM))			       IRQ_NOREQUEST, 0);
		do_sysvsem = 1;			       IRQ_NOREQUEST, 0);
	err = unshare_fs(unshare_flags, &new_fs);			       IRQ_NOREQUEST, 0);
	if (err)			       IRQ_NOREQUEST, 0);
		goto bad_unshare_out;			       IRQ_NOREQUEST, 0);
	err = unshare_fd(unshare_flags, &new_fd);			       IRQ_NOREQUEST, 0);
	if (err)			       IRQ_NOREQUEST, 0);
		goto bad_unshare_cleanup_fs;			       IRQ_NOREQUEST, 0);
	err = unshare_userns(unshare_flags, &new_cred);			       IRQ_NOREQUEST, 0);
	if (err)			       IRQ_NOREQUEST, 0);
		goto bad_unshare_cleanup_fd;			       IRQ_NOREQUEST, 0);
	err = unshare_nsproxy_namespaces(unshare_flags, &new_nsproxy,			       IRQ_NOREQUEST, 0);
					 new_cred, new_fs);			       IRQ_NOREQUEST, 0);
	if (err)			       IRQ_NOREQUEST, 0);
		goto bad_unshare_cleanup_cred;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	if (new_fs || new_fd || do_sysvsem || new_cred || new_nsproxy) {			       IRQ_NOREQUEST, 0);
		if (do_sysvsem) {			       IRQ_NOREQUEST, 0);
			/*			       IRQ_NOREQUEST, 0);
			 * CLONE_SYSVSEM is equivalent to sys_exit().			       IRQ_NOREQUEST, 0);
			 */			       IRQ_NOREQUEST, 0);
			exit_sem(current);			       IRQ_NOREQUEST, 0);
		}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		if (new_nsproxy)			       IRQ_NOREQUEST, 0);
			switch_task_namespaces(current, new_nsproxy);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		task_lock(current);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		if (new_fs) {			       IRQ_NOREQUEST, 0);
			fs = current->fs;			       IRQ_NOREQUEST, 0);
			spin_lock(&fs->lock);			       IRQ_NOREQUEST, 0);
			current->fs = new_fs;			       IRQ_NOREQUEST, 0);
			if (--fs->users)			       IRQ_NOREQUEST, 0);
				new_fs = NULL;			       IRQ_NOREQUEST, 0);
			else			       IRQ_NOREQUEST, 0);
				new_fs = fs;			       IRQ_NOREQUEST, 0);
			spin_unlock(&fs->lock);			       IRQ_NOREQUEST, 0);
		}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		if (new_fd) {			       IRQ_NOREQUEST, 0);
			fd = current->files;			       IRQ_NOREQUEST, 0);
			current->files = new_fd;			       IRQ_NOREQUEST, 0);
			new_fd = fd;			       IRQ_NOREQUEST, 0);
		}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		task_unlock(current);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
		if (new_cred) {			       IRQ_NOREQUEST, 0);
			/* Install the new user namespace */			       IRQ_NOREQUEST, 0);
			commit_creds(new_cred);			       IRQ_NOREQUEST, 0);
			new_cred = NULL;			       IRQ_NOREQUEST, 0);
		}			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
bad_unshare_cleanup_cred:			       IRQ_NOREQUEST, 0);
	if (new_cred)			       IRQ_NOREQUEST, 0);
		put_cred(new_cred);			       IRQ_NOREQUEST, 0);
bad_unshare_cleanup_fd:			       IRQ_NOREQUEST, 0);
	if (new_fd)			       IRQ_NOREQUEST, 0);
		put_files_struct(new_fd);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
bad_unshare_cleanup_fs:			       IRQ_NOREQUEST, 0);
	if (new_fs)			       IRQ_NOREQUEST, 0);
		free_fs_struct(new_fs);			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
bad_unshare_out:			       IRQ_NOREQUEST, 0);
	return err;			       IRQ_NOREQUEST, 0);
}			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
/*			       IRQ_NOREQUEST, 0);
 *	Helper to unshare the files of the current task.			       IRQ_NOREQUEST, 0);
 *	We don't want to expose copy_files internals to			       IRQ_NOREQUEST, 0);
 *	the exec layer of the kernel.			       IRQ_NOREQUEST, 0);
 */			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
int unshare_files(struct files_struct **displaced)			       IRQ_NOREQUEST, 0);
{			       IRQ_NOREQUEST, 0);
	struct task_struct *task = current;			       IRQ_NOREQUEST, 0);
	struct files_struct *copy = NULL;			       IRQ_NOREQUEST, 0);
	int error;			       IRQ_NOREQUEST, 0);
			       IRQ_NOREQUEST, 0);
	error = unshare_fd(CLONE_FILES, &copy);			       IRQ_NOREQUEST, 0);
	if (error || !copy) {			       IRQ_NOREQUEST, 0);
		*displaced = NULL;			       IRQ_NOREQUEST, 0);
		return error;			       IRQ_NOREQUEST, 0);
	}			       IRQ_NOREQUEST, 0);
	*displaced = task->files;			       IRQ_NOREQUEST, 0);
	task_lock(task);			       IRQ_NOREQUEST, 0);
	task->files = copy;			       IRQ_NOREQUEST, 0);
	task_unlock(task);			       IRQ_NOREQUEST, 0);
	return 0;			       IRQ_NOREQUEST, 0);
}