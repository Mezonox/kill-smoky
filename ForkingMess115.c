static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 *  linux/kernel/fork.cstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 *static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 *  Copyright (C) 1991, 1992  Linus Torvaldsstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 *  'fork.c' contains the help-routines for the 'fork' system callstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * (see also entry.S and others).static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * Fork is rather simple, once you get the hang of it, but the memorystatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * management can be a bitch. See 'mm/memory.c': 'copy_page_range()'static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/slab.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/init.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/unistd.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/module.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/vmalloc.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/completion.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/personality.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/mempolicy.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/sem.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/file.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/fdtable.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/iocontext.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/key.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/binfmts.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/mman.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/mmu_notifier.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/fs.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/nsproxy.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/capability.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/cpu.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/cgroup.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/security.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/hugetlb.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/seccomp.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/swap.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/syscalls.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/jiffies.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/futex.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/compat.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/kthread.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/task_io_accounting_ops.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/rcupdate.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/ptrace.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/mount.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/audit.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/memcontrol.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/ftrace.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/proc_fs.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/profile.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/rmap.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/ksm.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/acct.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/tsacct_kern.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/cn_proc.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/freezer.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/delayacct.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/taskstats_kern.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/random.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/tty.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/blkdev.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/fs_struct.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/magic.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/perf_event.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/posix-timers.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/user-return-notifier.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/oom.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/khugepaged.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/signalfd.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/uprobes.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/aio.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <asm/pgtable.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <asm/pgalloc.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <asm/uaccess.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <asm/mmu_context.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <asm/cacheflush.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <asm/tlbflush.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <trace/events/sched.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#define CREATE_TRACE_POINTSstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <trace/events/task.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * Protected counters by write_lock_irq(&tasklist_lock)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
unsigned long total_forks;	/* Handle normal Linux uptimes. */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
int nr_threads;			/* The idle threads do not count.. */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
int max_threads;		/* tunable limit on nr_threads */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
DEFINE_PER_CPU(unsigned long, process_counts) = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
__cacheline_aligned DEFINE_RWLOCK(tasklist_lock);  /* outer */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_PROVE_RCUstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
int lockdep_tasklist_lock_is_held(void)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return lockdep_is_held(&tasklist_lock);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
EXPORT_SYMBOL_GPL(lockdep_tasklist_lock_is_held);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endif /* #ifdef CONFIG_PROVE_RCU */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
int nr_processes(void)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	int cpu;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	int total = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	for_each_possible_cpu(cpu)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		total += per_cpu(process_counts, cpu);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return total;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
void __weak arch_release_task_struct(struct task_struct *tsk)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifndef CONFIG_ARCH_TASK_STRUCT_ALLOCATORstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static struct kmem_cache *task_struct_cachep;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static inline struct task_struct *alloc_task_struct_node(int node)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return kmem_cache_alloc_node(task_struct_cachep, GFP_KERNEL, node);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static inline void free_task_struct(struct task_struct *tsk)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	kmem_cache_free(task_struct_cachep, tsk);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
void __weak arch_release_thread_info(struct thread_info *ti)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifndef CONFIG_ARCH_THREAD_INFO_ALLOCATORstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * Allocate pages if THREAD_SIZE is >= PAGE_SIZE, otherwise use astatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * kmemcache based allocator.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
# if THREAD_SIZE >= PAGE_SIZEstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static struct thread_info *alloc_thread_info_node(struct task_struct *tsk,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
						  int node)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct page *page = alloc_pages_node(node, THREADINFO_GFP_ACCOUNTED,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
					     THREAD_SIZE_ORDER);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return page ? page_address(page) : NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static inline void free_thread_info(struct thread_info *ti)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	free_memcg_kmem_pages((unsigned long)ti, THREAD_SIZE_ORDER);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
# elsestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static struct kmem_cache *thread_info_cache;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static struct thread_info *alloc_thread_info_node(struct task_struct *tsk,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
						  int node)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return kmem_cache_alloc_node(thread_info_cache, THREADINFO_GFP, node);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static void free_thread_info(struct thread_info *ti)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	kmem_cache_free(thread_info_cache, ti);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
void thread_info_cache_init(void)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	thread_info_cache = kmem_cache_create("thread_info", THREAD_SIZE,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
					      THREAD_SIZE, 0, NULL);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	BUG_ON(thread_info_cache == NULL);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
# endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/* SLAB cache for signal_struct structures (tsk->signal) */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static struct kmem_cache *signal_cachep;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/* SLAB cache for sighand_struct structures (tsk->sighand) */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
struct kmem_cache *sighand_cachep;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/* SLAB cache for files_struct structures (tsk->files) */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
struct kmem_cache *files_cachep;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/* SLAB cache for fs_struct structures (tsk->fs) */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
struct kmem_cache *fs_cachep;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/* SLAB cache for vm_area_struct structures */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
struct kmem_cache *vm_area_cachep;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/* SLAB cache for mm_struct structures (tsk->mm) */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static struct kmem_cache *mm_cachep;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static void account_kernel_stack(struct thread_info *ti, int account)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct zone *zone = page_zone(virt_to_page(ti));static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mod_zone_page_state(zone, NR_KERNEL_STACK, account);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
void free_task(struct task_struct *tsk)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	account_kernel_stack(tsk->stack, -1);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	arch_release_thread_info(tsk->stack);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	free_thread_info(tsk->stack);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	rt_mutex_debug_task_free(tsk);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	ftrace_graph_exit_task(tsk);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	put_seccomp_filter(tsk);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	arch_release_task_struct(tsk);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	free_task_struct(tsk);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
EXPORT_SYMBOL(free_task);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static inline void free_signal_struct(struct signal_struct *sig)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	taskstats_tgid_free(sig);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	sched_autogroup_exit(sig);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	kmem_cache_free(signal_cachep, sig);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static inline void put_signal_struct(struct signal_struct *sig)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (atomic_dec_and_test(&sig->sigcnt))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		free_signal_struct(sig);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
void __put_task_struct(struct task_struct *tsk)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	WARN_ON(!tsk->exit_state);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	WARN_ON(atomic_read(&tsk->usage));static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	WARN_ON(tsk == current);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	task_numa_free(tsk);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	security_task_free(tsk);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	exit_creds(tsk);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	delayacct_tsk_free(tsk);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	put_signal_struct(tsk->signal);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (!profile_handoff_task(tsk))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		free_task(tsk);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
EXPORT_SYMBOL_GPL(__put_task_struct);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
void __init __weak arch_task_cache_init(void) { }static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
void __init fork_init(unsigned long mempages)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifndef CONFIG_ARCH_TASK_STRUCT_ALLOCATORstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifndef ARCH_MIN_TASKALIGNstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#define ARCH_MIN_TASKALIGN	L1_CACHE_BYTESstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/* create a slab on which task_structs can be allocated */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	task_struct_cachep =static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		kmem_cache_create("task_struct", sizeof(struct task_struct),static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			ARCH_MIN_TASKALIGN, SLAB_PANIC | SLAB_NOTRACK, NULL);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/* do the arch specific task caches init */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	arch_task_cache_init();static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * The default maximum number of threads is set to a safestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * value: the thread structures can take up at most halfstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * of memory.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	max_threads = mempages / (8 * THREAD_SIZE / PAGE_SIZE);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * we need to allow at least 20 threads to boot a systemstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (max_threads < 20)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		max_threads = 20;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	init_task.signal->rlim[RLIMIT_NPROC].rlim_cur = max_threads/2;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	init_task.signal->rlim[RLIMIT_NPROC].rlim_max = max_threads/2;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	init_task.signal->rlim[RLIMIT_SIGPENDING] =static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		init_task.signal->rlim[RLIMIT_NPROC];static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
int __attribute__((weak)) arch_dup_task_struct(struct task_struct *dst,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
					       struct task_struct *src)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	*dst = *src;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static struct task_struct *dup_task_struct(struct task_struct *orig)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct task_struct *tsk;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct thread_info *ti;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	unsigned long *stackend;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	int node = tsk_fork_get_node(orig);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	int err;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	tsk = alloc_task_struct_node(node);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (!tsk)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		return NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	ti = alloc_thread_info_node(tsk, node);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (!ti)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto free_tsk;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	err = arch_dup_task_struct(tsk, orig);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (err)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto free_ti;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	tsk->stack = ti;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	setup_thread_stack(tsk, orig);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	clear_user_return_notifier(tsk);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	clear_tsk_need_resched(tsk);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	stackend = end_of_stack(tsk);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	*stackend = STACK_END_MAGIC;	/* for overflow detection */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_CC_STACKPROTECTORstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	tsk->stack_canary = get_random_int();static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * One for us, one for whoever does the "release_task()" (usuallystatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * parent)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	atomic_set(&tsk->usage, 2);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_BLK_DEV_IO_TRACEstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	tsk->btrace_seq = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	tsk->splice_pipe = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	tsk->task_frag.page = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	account_kernel_stack(ti, 1);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return tsk;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
free_ti:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	free_thread_info(ti);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
free_tsk:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	free_task_struct(tsk);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_MMUstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int dup_mmap(struct mm_struct *mm, struct mm_struct *oldmm)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct vm_area_struct *mpnt, *tmp, *prev, **pprev;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct rb_node **rb_link, *rb_parent;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	int retval;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	unsigned long charge;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	uprobe_start_dup_mmap();static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	down_write(&oldmm->mmap_sem);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	flush_cache_dup_mm(oldmm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	uprobe_dup_mmap(oldmm, mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * Not linked in yet - no deadlock potential:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	down_write_nested(&mm->mmap_sem, SINGLE_DEPTH_NESTING);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm->locked_vm = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm->mmap = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm->mmap_cache = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm->map_count = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	cpumask_clear(mm_cpumask(mm));static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm->mm_rb = RB_ROOT;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	rb_link = &mm->mm_rb.rb_node;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	rb_parent = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	pprev = &mm->mmap;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	retval = ksm_fork(mm, oldmm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (retval)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto out;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	retval = khugepaged_fork(mm, oldmm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (retval)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto out;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	prev = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	for (mpnt = oldmm->mmap; mpnt; mpnt = mpnt->vm_next) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		struct file *file;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (mpnt->vm_flags & VM_DONTCOPY) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			vm_stat_account(mm, mpnt->vm_flags, mpnt->vm_file,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
							-vma_pages(mpnt));static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			continue;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		charge = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (mpnt->vm_flags & VM_ACCOUNT) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			unsigned long len = vma_pages(mpnt);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			if (security_vm_enough_memory_mm(oldmm, len)) /* sic */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
				goto fail_nomem;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			charge = len;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		tmp = kmem_cache_alloc(vm_area_cachep, GFP_KERNEL);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (!tmp)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			goto fail_nomem;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		*tmp = *mpnt;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		INIT_LIST_HEAD(&tmp->anon_vma_chain);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		retval = vma_dup_policy(mpnt, tmp);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (retval)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			goto fail_nomem_policy;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		tmp->vm_mm = mm;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (anon_vma_fork(tmp, mpnt))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			goto fail_nomem_anon_vma_fork;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		tmp->vm_flags &= ~VM_LOCKED;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		tmp->vm_next = tmp->vm_prev = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		file = tmp->vm_file;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (file) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			struct inode *inode = file_inode(file);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			struct address_space *mapping = file->f_mapping;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			get_file(file);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			if (tmp->vm_flags & VM_DENYWRITE)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
				atomic_dec(&inode->i_writecount);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			mutex_lock(&mapping->i_mmap_mutex);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			if (tmp->vm_flags & VM_SHARED)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
				mapping->i_mmap_writable++;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			flush_dcache_mmap_lock(mapping);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			/* insert tmp into the share list, just after mpnt */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			if (unlikely(tmp->vm_flags & VM_NONLINEAR))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
				vma_nonlinear_insert(tmp,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
						&mapping->i_mmap_nonlinear);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			elsestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
				vma_interval_tree_insert_after(tmp, mpnt,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
							&mapping->i_mmap);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			flush_dcache_mmap_unlock(mapping);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			mutex_unlock(&mapping->i_mmap_mutex);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		 * Clear hugetlb-related page reserves for children. This onlystatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		 * affects MAP_PRIVATE mappings. Faults generated by the childstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		 * are not guaranteed to succeed, even if read-onlystatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (is_vm_hugetlb_page(tmp))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			reset_vma_resv_huge_pages(tmp);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		 * Link in the new vma and copy the page table entries.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		*pprev = tmp;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		pprev = &tmp->vm_next;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		tmp->vm_prev = prev;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		prev = tmp;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		__vma_link_rb(mm, tmp, rb_link, rb_parent);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		rb_link = &tmp->vm_rb.rb_right;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		rb_parent = &tmp->vm_rb;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		mm->map_count++;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		retval = copy_page_range(mm, oldmm, mpnt);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (tmp->vm_ops && tmp->vm_ops->open)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			tmp->vm_ops->open(tmp);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (retval)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			goto out;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/* a new mm has just been created */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	arch_dup_mmap(oldmm, mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	retval = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
out:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	up_write(&mm->mmap_sem);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	flush_tlb_mm(oldmm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	up_write(&oldmm->mmap_sem);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	uprobe_end_dup_mmap();static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return retval;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
fail_nomem_anon_vma_fork:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mpol_put(vma_policy(tmp));static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
fail_nomem_policy:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	kmem_cache_free(vm_area_cachep, tmp);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
fail_nomem:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	retval = -ENOMEM;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	vm_unacct_memory(charge);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	goto out;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static inline int mm_alloc_pgd(struct mm_struct *mm)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm->pgd = pgd_alloc(mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (unlikely(!mm->pgd))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		return -ENOMEM;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static inline void mm_free_pgd(struct mm_struct *mm)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	pgd_free(mm, mm->pgd);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#elsestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#define dup_mmap(mm, oldmm)	(0)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#define mm_alloc_pgd(mm)	(0)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#define mm_free_pgd(mm)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endif /* CONFIG_MMU */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
__cacheline_aligned_in_smp DEFINE_SPINLOCK(mmlist_lock);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#define allocate_mm()	(kmem_cache_alloc(mm_cachep, GFP_KERNEL))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#define free_mm(mm)	(kmem_cache_free(mm_cachep, (mm)))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static unsigned long default_dump_filter = MMF_DUMP_FILTER_DEFAULT;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int __init coredump_filter_setup(char *s)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	default_dump_filter =static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		(simple_strtoul(s, NULL, 0) << MMF_DUMP_FILTER_SHIFT) &static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		MMF_DUMP_FILTER_MASK;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return 1;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
__setup("coredump_filter=", coredump_filter_setup);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#include <linux/init_task.h>static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static void mm_init_aio(struct mm_struct *mm)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_AIOstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	spin_lock_init(&mm->ioctx_lock);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm->ioctx_table = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static struct mm_struct *mm_init(struct mm_struct *mm, struct task_struct *p)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	atomic_set(&mm->mm_users, 1);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	atomic_set(&mm->mm_count, 1);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	init_rwsem(&mm->mmap_sem);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	INIT_LIST_HEAD(&mm->mmlist);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm->flags = (current->mm) ?static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		(current->mm->flags & MMF_INIT_MASK) : default_dump_filter;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm->core_state = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	atomic_long_set(&mm->nr_ptes, 0);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	memset(&mm->rss_stat, 0, sizeof(mm->rss_stat));static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	spin_lock_init(&mm->page_table_lock);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm_init_aio(mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm_init_owner(mm, p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	clear_tlb_flush_pending(mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (likely(!mm_alloc_pgd(mm))) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		mm->def_flags = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		mmu_notifier_mm_init(mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		return mm;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	free_mm(mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static void check_mm(struct mm_struct *mm)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	int i;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	for (i = 0; i < NR_MM_COUNTERS; i++) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		long x = atomic_long_read(&mm->rss_stat.count[i]);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (unlikely(x))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			printk(KERN_ALERT "BUG: Bad rss-counter state "static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
					  "mm:%p idx:%d val:%ld\n", mm, i, x);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#if defined(CONFIG_TRANSPARENT_HUGEPAGE) && !USE_SPLIT_PMD_PTLOCKSstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	VM_BUG_ON(mm->pmd_huge_pte);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * Allocate and initialize an mm_struct.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
struct mm_struct *mm_alloc(void)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct mm_struct *mm;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm = allocate_mm();static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (!mm)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		return NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	memset(mm, 0, sizeof(*mm));static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm_init_cpumask(mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return mm_init(mm, current);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * Called when the last reference to the mmstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * is dropped: either by a lazy thread or bystatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * mmput. Free the page directory and the mm.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
void __mmdrop(struct mm_struct *mm)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	BUG_ON(mm == &init_mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm_free_pgd(mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	destroy_context(mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mmu_notifier_mm_destroy(mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	check_mm(mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	free_mm(mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
EXPORT_SYMBOL_GPL(__mmdrop);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * Decrement the use count and release all resources for an mm.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
void mmput(struct mm_struct *mm)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	might_sleep();static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (atomic_dec_and_test(&mm->mm_users)) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		uprobe_clear_state(mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		exit_aio(mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		ksm_exit(mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		khugepaged_exit(mm); /* must run before exit_mmap */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		exit_mmap(mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		set_mm_exe_file(mm, NULL);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (!list_empty(&mm->mmlist)) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			spin_lock(&mmlist_lock);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			list_del(&mm->mmlist);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			spin_unlock(&mmlist_lock);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (mm->binfmt)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			module_put(mm->binfmt->module);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		mmdrop(mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
EXPORT_SYMBOL_GPL(mmput);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
void set_mm_exe_file(struct mm_struct *mm, struct file *new_exe_file)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (new_exe_file)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		get_file(new_exe_file);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (mm->exe_file)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		fput(mm->exe_file);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm->exe_file = new_exe_file;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
struct file *get_mm_exe_file(struct mm_struct *mm)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct file *exe_file;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/* We need mmap_sem to protect against races with removal of exe_file */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	down_read(&mm->mmap_sem);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	exe_file = mm->exe_file;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (exe_file)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		get_file(exe_file);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	up_read(&mm->mmap_sem);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return exe_file;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static void dup_mm_exe_file(struct mm_struct *oldmm, struct mm_struct *newmm)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/* It's safe to write the exe_file pointer without exe_file_lock becausestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * this is called during fork when the task is not yet in /proc */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	newmm->exe_file = get_mm_exe_file(oldmm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/**static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * get_task_mm - acquire a reference to the task's mmstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 *static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * Returns %NULL if the task has no mm.  Checks PF_KTHREAD (meaningstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * this kernel workthread has transiently adopted a user mm with use_mm,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * to do its AIO) is not set and if so returns a reference to it, afterstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * bumping up the use count.  User must release the mm via mmput()static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * after use.  Typically used by /proc and ptrace.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
struct mm_struct *get_task_mm(struct task_struct *task)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct mm_struct *mm;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	task_lock(task);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm = task->mm;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (mm) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (task->flags & PF_KTHREAD)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			mm = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		elsestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			atomic_inc(&mm->mm_users);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	task_unlock(task);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return mm;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
EXPORT_SYMBOL_GPL(get_task_mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
struct mm_struct *mm_access(struct task_struct *task, unsigned int mode)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct mm_struct *mm;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	int err;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	err =  mutex_lock_killable(&task->signal->cred_guard_mutex);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (err)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		return ERR_PTR(err);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm = get_task_mm(task);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (mm && mm != current->mm &&static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			!ptrace_may_access(task, mode)) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		mmput(mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		mm = ERR_PTR(-EACCES);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mutex_unlock(&task->signal->cred_guard_mutex);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return mm;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static void complete_vfork_done(struct task_struct *tsk)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct completion *vfork;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	task_lock(tsk);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	vfork = tsk->vfork_done;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (likely(vfork)) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		tsk->vfork_done = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		complete(vfork);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	task_unlock(tsk);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int wait_for_vfork_done(struct task_struct *child,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
				struct completion *vfork)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	int killed;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	freezer_do_not_count();static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	killed = wait_for_completion_killable(vfork);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	freezer_count();static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (killed) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		task_lock(child);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		child->vfork_done = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		task_unlock(child);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	put_task_struct(child);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return killed;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/* Please note the differences between mmput and mm_release.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * mmput is called whenever we stop holding onto a mm_struct,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * error success whatever.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 *static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * mm_release is called after a mm_struct has been removedstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * from the current process.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 *static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * This difference is important for error handling, when westatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * only half set up a mm_struct for a new process and need to restorestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * the old one.  Because we mmput the new mm_struct beforestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * restoring the old one. . .static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * Eric Biederman 10 January 1998static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
void mm_release(struct task_struct *tsk, struct mm_struct *mm)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/* Get rid of any futexes when releasing the mm */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_FUTEXstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (unlikely(tsk->robust_list)) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		exit_robust_list(tsk);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		tsk->robust_list = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_COMPATstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (unlikely(tsk->compat_robust_list)) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		compat_exit_robust_list(tsk);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		tsk->compat_robust_list = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (unlikely(!list_empty(&tsk->pi_state_list)))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		exit_pi_state_list(tsk);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	uprobe_free_utask(tsk);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/* Get rid of any cached register state */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	deactivate_mm(tsk, mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * If we're exiting normally, clear a user-space tid field ifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * requested.  We leave this alone when dying by signal, to leavestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * the value intact in a core dump, and to save the unnecessarystatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * trouble, say, a killed vfork parent shouldn't touch this mm.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * Userland only wants this done for a sys_exit.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (tsk->clear_child_tid) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (!(tsk->flags & PF_SIGNALED) &&static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		    atomic_read(&mm->mm_users) > 1) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			 * We don't check the error code - if userspace hasstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			 * not set up a proper pointer then tough luck.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			put_user(0, tsk->clear_child_tid);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			sys_futex(tsk->clear_child_tid, FUTEX_WAKE,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
					1, NULL, NULL, 0);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		tsk->clear_child_tid = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * All done, finally we can wake up parent and return this mm to him.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * Also kthread_stop() uses this completion for synchronization.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (tsk->vfork_done)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		complete_vfork_done(tsk);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * Allocate a new mm structure and copy contents from thestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * mm structure of the passed in task structure.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static struct mm_struct *dup_mm(struct task_struct *tsk)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct mm_struct *mm, *oldmm = current->mm;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	int err;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm = allocate_mm();static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (!mm)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto fail_nomem;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	memcpy(mm, oldmm, sizeof(*mm));static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm_init_cpumask(mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#if defined(CONFIG_TRANSPARENT_HUGEPAGE) && !USE_SPLIT_PMD_PTLOCKSstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm->pmd_huge_pte = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (!mm_init(mm, tsk))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto fail_nomem;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (init_new_context(tsk, mm))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto fail_nocontext;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	dup_mm_exe_file(oldmm, mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	err = dup_mmap(mm, oldmm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (err)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto free_pt;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm->hiwater_rss = get_mm_rss(mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm->hiwater_vm = mm->total_vm;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (mm->binfmt && !try_module_get(mm->binfmt->module))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto free_pt;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return mm;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
free_pt:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/* don't put binfmt in mmput, we haven't got module yet */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm->binfmt = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mmput(mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
fail_nomem:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
fail_nocontext:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * If init_new_context() failed, we cannot use mmput() to free the mmstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * because it calls destroy_context()static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm_free_pgd(mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	free_mm(mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int copy_mm(unsigned long clone_flags, struct task_struct *tsk)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct mm_struct *mm, *oldmm;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	int retval;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	tsk->min_flt = tsk->maj_flt = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	tsk->nvcsw = tsk->nivcsw = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_DETECT_HUNG_TASKstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	tsk->last_switch_count = tsk->nvcsw + tsk->nivcsw;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	tsk->mm = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	tsk->active_mm = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * Are we cloning a kernel thread?static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 *static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * We need to steal a active VM for that..static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	oldmm = current->mm;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (!oldmm)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		return 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (clone_flags & CLONE_VM) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		atomic_inc(&oldmm->mm_users);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		mm = oldmm;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto good_mm;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	retval = -ENOMEM;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm = dup_mm(tsk);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (!mm)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto fail_nomem;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
good_mm:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	tsk->mm = mm;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	tsk->active_mm = mm;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
fail_nomem:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return retval;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int copy_fs(unsigned long clone_flags, struct task_struct *tsk)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct fs_struct *fs = current->fs;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (clone_flags & CLONE_FS) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		/* tsk->fs is already what we want */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		spin_lock(&fs->lock);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (fs->in_exec) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			spin_unlock(&fs->lock);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			return -EAGAIN;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		fs->users++;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		spin_unlock(&fs->lock);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		return 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	tsk->fs = copy_fs_struct(fs);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (!tsk->fs)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		return -ENOMEM;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int copy_files(unsigned long clone_flags, struct task_struct *tsk)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct files_struct *oldf, *newf;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	int error = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * A background process may not have any files ...static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	oldf = current->files;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (!oldf)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto out;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (clone_flags & CLONE_FILES) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		atomic_inc(&oldf->count);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto out;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	newf = dup_fd(oldf, &error);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (!newf)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto out;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	tsk->files = newf;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	error = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
out:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return error;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int copy_io(unsigned long clone_flags, struct task_struct *tsk)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_BLOCKstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct io_context *ioc = current->io_context;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct io_context *new_ioc;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (!ioc)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		return 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * Share io context with parent, if CLONE_IO is setstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (clone_flags & CLONE_IO) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		ioc_task_link(ioc);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		tsk->io_context = ioc;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	} else if (ioprio_valid(ioc->ioprio)) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		new_ioc = get_task_io_context(tsk, GFP_KERNEL, NUMA_NO_NODE);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (unlikely(!new_ioc))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			return -ENOMEM;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		new_ioc->ioprio = ioc->ioprio;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		put_io_context(new_ioc);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int copy_sighand(unsigned long clone_flags, struct task_struct *tsk)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct sighand_struct *sig;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (clone_flags & CLONE_SIGHAND) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		atomic_inc(&current->sighand->count);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		return 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	sig = kmem_cache_alloc(sighand_cachep, GFP_KERNEL);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	rcu_assign_pointer(tsk->sighand, sig);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (!sig)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		return -ENOMEM;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	atomic_set(&sig->count, 1);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	memcpy(sig->action, current->sighand->action, sizeof(sig->action));static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
void __cleanup_sighand(struct sighand_struct *sighand)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (atomic_dec_and_test(&sighand->count)) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		signalfd_cleanup(sighand);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		kmem_cache_free(sighand_cachep, sighand);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * Initialize POSIX timer handling for a thread group.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static void posix_cpu_timers_init_group(struct signal_struct *sig)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	unsigned long cpu_limit;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/* Thread group counters. */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	thread_group_cputime_init(sig);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	cpu_limit = ACCESS_ONCE(sig->rlim[RLIMIT_CPU].rlim_cur);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (cpu_limit != RLIM_INFINITY) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		sig->cputime_expires.prof_exp = secs_to_cputime(cpu_limit);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		sig->cputimer.running = 1;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/* The timer lists. */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	INIT_LIST_HEAD(&sig->cpu_timers[0]);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	INIT_LIST_HEAD(&sig->cpu_timers[1]);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	INIT_LIST_HEAD(&sig->cpu_timers[2]);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int copy_signal(unsigned long clone_flags, struct task_struct *tsk)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct signal_struct *sig;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (clone_flags & CLONE_THREAD)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		return 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	sig = kmem_cache_zalloc(signal_cachep, GFP_KERNEL);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	tsk->signal = sig;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (!sig)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		return -ENOMEM;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	sig->nr_threads = 1;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	atomic_set(&sig->live, 1);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	atomic_set(&sig->sigcnt, 1);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/* list_add(thread_node, thread_head) without INIT_LIST_HEAD() */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	sig->thread_head = (struct list_head)LIST_HEAD_INIT(tsk->thread_node);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	tsk->thread_node = (struct list_head)LIST_HEAD_INIT(sig->thread_head);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	init_waitqueue_head(&sig->wait_chldexit);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	sig->curr_target = tsk;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	init_sigpending(&sig->shared_pending);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	INIT_LIST_HEAD(&sig->posix_timers);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	hrtimer_init(&sig->real_timer, CLOCK_MONOTONIC, HRTIMER_MODE_REL);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	sig->real_timer.function = it_real_fn;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	task_lock(current->group_leader);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	memcpy(sig->rlim, current->signal->rlim, sizeof sig->rlim);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	task_unlock(current->group_leader);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	posix_cpu_timers_init_group(sig);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	tty_audit_fork(sig);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	sched_autogroup_fork(sig);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_CGROUPSstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	init_rwsem(&sig->group_rwsem);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	sig->oom_score_adj = current->signal->oom_score_adj;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	sig->oom_score_adj_min = current->signal->oom_score_adj_min;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	sig->has_child_subreaper = current->signal->has_child_subreaper ||static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
				   current->signal->is_child_subreaper;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mutex_init(&sig->cred_guard_mutex);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static void copy_flags(unsigned long clone_flags, struct task_struct *p)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	unsigned long new_flags = p->flags;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	new_flags &= ~(PF_SUPERPRIV | PF_WQ_WORKER);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	new_flags |= PF_FORKNOEXEC;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->flags = new_flags;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
SYSCALL_DEFINE1(set_tid_address, int __user *, tidptr)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	current->clear_child_tid = tidptr;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return task_pid_vnr(current);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static void rt_mutex_init_task(struct task_struct *p)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	raw_spin_lock_init(&p->pi_lock);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_RT_MUTEXESstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->pi_waiters = RB_ROOT;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->pi_waiters_leftmost = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->pi_blocked_on = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->pi_top_task = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_MM_OWNERstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
void mm_init_owner(struct mm_struct *mm, struct task_struct *p)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm->owner = p;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endif /* CONFIG_MM_OWNER */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * Initialize POSIX timer handling for a single task.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static void posix_cpu_timers_init(struct task_struct *tsk)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	tsk->cputime_expires.prof_exp = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	tsk->cputime_expires.virt_exp = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	tsk->cputime_expires.sched_exp = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	INIT_LIST_HEAD(&tsk->cpu_timers[0]);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	INIT_LIST_HEAD(&tsk->cpu_timers[1]);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	INIT_LIST_HEAD(&tsk->cpu_timers[2]);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static inline voidstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
init_task_pid(struct task_struct *task, enum pid_type type, struct pid *pid)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 task->pids[type].pid = pid;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * This creates a new process as a copy of the old one,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * but does not actually start it yet.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 *static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * It copies the registers, and all the appropriatestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * parts of the process environment (as per the clonestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * flags). The actual kick-off is left to the caller.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static struct task_struct *copy_process(unsigned long clone_flags,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
					unsigned long stack_start,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
					unsigned long stack_size,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
					int __user *child_tidptr,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
					struct pid *pid,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
					int trace)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	int retval;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct task_struct *p;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if ((clone_flags & (CLONE_NEWNS|CLONE_FS)) == (CLONE_NEWNS|CLONE_FS))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		return ERR_PTR(-EINVAL);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if ((clone_flags & (CLONE_NEWUSER|CLONE_FS)) == (CLONE_NEWUSER|CLONE_FS))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		return ERR_PTR(-EINVAL);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * Thread groups must share signals as well, and detached threadsstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * can only be started up within the thread group.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if ((clone_flags & CLONE_THREAD) && !(clone_flags & CLONE_SIGHAND))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		return ERR_PTR(-EINVAL);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * Shared signal handlers imply shared VM. By way of the above,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * thread groups also imply shared VM. Blocking this case allowsstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * for various simplifications in other code.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if ((clone_flags & CLONE_SIGHAND) && !(clone_flags & CLONE_VM))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		return ERR_PTR(-EINVAL);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * Siblings of global init remain as zombies on exit since they arestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * not reaped by their parent (swapper). To solve this and to avoidstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * multi-rooted process trees, prevent global and container-initsstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * from creating siblings.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if ((clone_flags & CLONE_PARENT) &&static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
				current->signal->flags & SIGNAL_UNKILLABLE)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		return ERR_PTR(-EINVAL);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * If the new process will be in a different pid or user namespacestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * do not allow it to share a thread group or signal handlers orstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * parent with the forking task.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (clone_flags & CLONE_SIGHAND) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if ((clone_flags & (CLONE_NEWUSER | CLONE_NEWPID)) ||static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		    (task_active_pid_ns(current) !=static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
				current->nsproxy->pid_ns_for_children))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			return ERR_PTR(-EINVAL);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	retval = security_task_create(clone_flags);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (retval)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto fork_out;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	retval = -ENOMEM;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p = dup_task_struct(current);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (!p)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto fork_out;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	ftrace_graph_init_task(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	get_seccomp_filter(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	rt_mutex_init_task(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_PROVE_LOCKINGstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	DEBUG_LOCKS_WARN_ON(!p->hardirqs_enabled);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	DEBUG_LOCKS_WARN_ON(!p->softirqs_enabled);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	retval = -EAGAIN;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (atomic_read(&p->real_cred->user->processes) >=static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			task_rlimit(p, RLIMIT_NPROC)) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (p->real_cred->user != INIT_USER &&static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		    !capable(CAP_SYS_RESOURCE) && !capable(CAP_SYS_ADMIN))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			goto bad_fork_free;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	current->flags &= ~PF_NPROC_EXCEEDED;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	retval = copy_creds(p, clone_flags);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (retval < 0)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto bad_fork_free;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * If multiple threads are within copy_process(), then this checkstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * triggers too late. This doesn't hurt, the check is only therestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * to stop root fork bombs.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	retval = -EAGAIN;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (nr_threads >= max_threads)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto bad_fork_cleanup_count;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (!try_module_get(task_thread_info(p)->exec_domain->module))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto bad_fork_cleanup_count;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	delayacct_tsk_init(p);	/* Must remain after dup_task_struct() */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	copy_flags(clone_flags, p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	INIT_LIST_HEAD(&p->children);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	INIT_LIST_HEAD(&p->sibling);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	rcu_copy_process(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->vfork_done = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	spin_lock_init(&p->alloc_lock);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	init_sigpending(&p->pending);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->utime = p->stime = p->gtime = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->utimescaled = p->stimescaled = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifndef CONFIG_VIRT_CPU_ACCOUNTING_NATIVEstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->prev_cputime.utime = p->prev_cputime.stime = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_VIRT_CPU_ACCOUNTING_GENstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	seqlock_init(&p->vtime_seqlock);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->vtime_snap = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->vtime_snap_whence = VTIME_SLEEPING;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#if defined(SPLIT_RSS_COUNTING)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	memset(&p->rss_stat, 0, sizeof(p->rss_stat));static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->default_timer_slack_ns = current->timer_slack_ns;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	task_io_accounting_init(&p->ioac);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	acct_clear_integrals(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	posix_cpu_timers_init(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	do_posix_clock_monotonic_gettime(&p->start_time);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->real_start_time = p->start_time;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	monotonic_to_bootbased(&p->real_start_time);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->io_context = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->audit_context = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (clone_flags & CLONE_THREAD)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		threadgroup_change_begin(current);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	cgroup_fork(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_NUMAstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->mempolicy = mpol_dup(p->mempolicy);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (IS_ERR(p->mempolicy)) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		retval = PTR_ERR(p->mempolicy);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		p->mempolicy = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto bad_fork_cleanup_cgroup;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mpol_fix_fork_child_flag(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_CPUSETSstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->cpuset_mem_spread_rotor = NUMA_NO_NODE;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->cpuset_slab_spread_rotor = NUMA_NO_NODE;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	seqcount_init(&p->mems_allowed_seq);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_TRACE_IRQFLAGSstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->irq_events = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->hardirqs_enabled = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->hardirq_enable_ip = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->hardirq_enable_event = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->hardirq_disable_ip = _THIS_IP_;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->hardirq_disable_event = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->softirqs_enabled = 1;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->softirq_enable_ip = _THIS_IP_;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->softirq_enable_event = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->softirq_disable_ip = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->softirq_disable_event = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->hardirq_context = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->softirq_context = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_LOCKDEPstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->lockdep_depth = 0; /* no locks held yet */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->curr_chain_key = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->lockdep_recursion = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_DEBUG_MUTEXESstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->blocked_on = NULL; /* not blocked yet */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_MEMCGstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->memcg_batch.do_batch = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->memcg_batch.memcg = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_BCACHEstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->sequential_io	= 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->sequential_io_avg	= 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/* Perform scheduler related setup. Assign this task to a CPU. */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	retval = sched_fork(clone_flags, p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (retval)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto bad_fork_cleanup_policy;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	retval = perf_event_init_task(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (retval)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto bad_fork_cleanup_policy;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	retval = audit_alloc(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (retval)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto bad_fork_cleanup_policy;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/* copy all the process information */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	retval = copy_semundo(clone_flags, p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (retval)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto bad_fork_cleanup_audit;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	retval = copy_files(clone_flags, p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (retval)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto bad_fork_cleanup_semundo;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	retval = copy_fs(clone_flags, p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (retval)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto bad_fork_cleanup_files;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	retval = copy_sighand(clone_flags, p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (retval)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto bad_fork_cleanup_fs;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	retval = copy_signal(clone_flags, p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (retval)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto bad_fork_cleanup_sighand;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	retval = copy_mm(clone_flags, p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (retval)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto bad_fork_cleanup_signal;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	retval = copy_namespaces(clone_flags, p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (retval)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto bad_fork_cleanup_mm;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	retval = copy_io(clone_flags, p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (retval)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto bad_fork_cleanup_namespaces;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	retval = copy_thread(clone_flags, stack_start, stack_size, p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (retval)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto bad_fork_cleanup_io;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (pid != &init_struct_pid) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		retval = -ENOMEM;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		pid = alloc_pid(p->nsproxy->pid_ns_for_children);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (!pid)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			goto bad_fork_cleanup_io;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->set_child_tid = (clone_flags & CLONE_CHILD_SETTID) ? child_tidptr : NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * Clear TID on mm_release()?static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->clear_child_tid = (clone_flags & CLONE_CHILD_CLEARTID) ? child_tidptr : NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_BLOCKstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->plug = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_FUTEXstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->robust_list = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_COMPATstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->compat_robust_list = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	INIT_LIST_HEAD(&p->pi_state_list);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->pi_state_cache = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * sigaltstack should be cleared when sharing the same VMstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if ((clone_flags & (CLONE_VM|CLONE_VFORK)) == CLONE_VM)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		p->sas_ss_sp = p->sas_ss_size = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * Syscall tracing and stepping should be turned off in thestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * child regardless of CLONE_PTRACE.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	user_disable_single_step(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	clear_tsk_thread_flag(p, TIF_SYSCALL_TRACE);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef TIF_SYSCALL_EMUstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	clear_tsk_thread_flag(p, TIF_SYSCALL_EMU);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	clear_all_latency_tracing(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/* ok, now we should be set up.. */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->pid = pid_nr(pid);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (clone_flags & CLONE_THREAD) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		p->exit_signal = -1;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		p->group_leader = current->group_leader;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		p->tgid = current->tgid;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	} else {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (clone_flags & CLONE_PARENT)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			p->exit_signal = current->group_leader->exit_signal;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		elsestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			p->exit_signal = (clone_flags & CSIGNAL);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		p->group_leader = p;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		p->tgid = p->pid;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->nr_dirtied = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->nr_dirtied_pause = 128 >> (PAGE_SHIFT - 10);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->dirty_paused_when = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->pdeath_signal = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	INIT_LIST_HEAD(&p->thread_group);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p->task_works = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * Make it visible to the rest of the system, but dont wake it up yet.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * Need tasklist lock for parent etc handling!static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	write_lock_irq(&tasklist_lock);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/* CLONE_PARENT re-uses the old parent */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (clone_flags & (CLONE_PARENT|CLONE_THREAD)) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		p->real_parent = current->real_parent;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		p->parent_exec_id = current->parent_exec_id;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	} else {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		p->real_parent = current;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		p->parent_exec_id = current->self_exec_id;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	spin_lock(&current->sighand->siglock);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * Process group and session signals need to be delivered to just thestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * parent before the fork or both the parent and the child after thestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * fork. Restart if a signal comes in before we add the new process tostatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * it's process group.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * A fatal signal pending means that current will exit, so the newstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * thread can't slip out of an OOM kill (or normal SIGKILL).static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	*/static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	recalc_sigpending();static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (signal_pending(current)) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		spin_unlock(&current->sighand->siglock);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		write_unlock_irq(&tasklist_lock);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		retval = -ERESTARTNOINTR;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto bad_fork_free_pid;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (likely(p->pid)) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		ptrace_init_task(p, (clone_flags & CLONE_PTRACE) || trace);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		init_task_pid(p, PIDTYPE_PID, pid);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (thread_group_leader(p)) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			init_task_pid(p, PIDTYPE_PGID, task_pgrp(current));static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			init_task_pid(p, PIDTYPE_SID, task_session(current));static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			if (is_child_reaper(pid)) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
				ns_of_pid(pid)->child_reaper = p;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
				p->signal->flags |= SIGNAL_UNKILLABLE;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			p->signal->leader_pid = pid;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			p->signal->tty = tty_kref_get(current->signal->tty);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			list_add_tail(&p->sibling, &p->real_parent->children);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			list_add_tail_rcu(&p->tasks, &init_task.tasks);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			attach_pid(p, PIDTYPE_PGID);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			attach_pid(p, PIDTYPE_SID);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			__this_cpu_inc(process_counts);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		} else {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			current->signal->nr_threads++;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			atomic_inc(&current->signal->live);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			atomic_inc(&current->signal->sigcnt);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			list_add_tail_rcu(&p->thread_group,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
					  &p->group_leader->thread_group);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			list_add_tail_rcu(&p->thread_node,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
					  &p->signal->thread_head);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		attach_pid(p, PIDTYPE_PID);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		nr_threads++;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	total_forks++;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	spin_unlock(&current->sighand->siglock);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	write_unlock_irq(&tasklist_lock);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	proc_fork_connector(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	cgroup_post_fork(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (clone_flags & CLONE_THREAD)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		threadgroup_change_end(current);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	perf_event_fork(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	trace_task_newtask(p, clone_flags);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	uprobe_copy_process(p, clone_flags);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return p;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
bad_fork_free_pid:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (pid != &init_struct_pid)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		free_pid(pid);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
bad_fork_cleanup_io:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (p->io_context)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		exit_io_context(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
bad_fork_cleanup_namespaces:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	exit_task_namespaces(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
bad_fork_cleanup_mm:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (p->mm)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		mmput(p->mm);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
bad_fork_cleanup_signal:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (!(clone_flags & CLONE_THREAD))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		free_signal_struct(p->signal);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
bad_fork_cleanup_sighand:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	__cleanup_sighand(p->sighand);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
bad_fork_cleanup_fs:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	exit_fs(p); /* blocking */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
bad_fork_cleanup_files:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	exit_files(p); /* blocking */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
bad_fork_cleanup_semundo:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	exit_sem(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
bad_fork_cleanup_audit:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	audit_free(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
bad_fork_cleanup_policy:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	perf_event_free_task(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_NUMAstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mpol_put(p->mempolicy);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
bad_fork_cleanup_cgroup:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (clone_flags & CLONE_THREAD)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		threadgroup_change_end(current);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	cgroup_exit(p, 0);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	delayacct_tsk_free(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	module_put(task_thread_info(p)->exec_domain->module);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
bad_fork_cleanup_count:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	atomic_dec(&p->cred->user->processes);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	exit_creds(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
bad_fork_free:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	free_task(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
fork_out:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return ERR_PTR(retval);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static inline void init_idle_pids(struct pid_link *links)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	enum pid_type type;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	for (type = PIDTYPE_PID; type < PIDTYPE_MAX; ++type) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		INIT_HLIST_NODE(&links[type].node); /* not really needed */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		links[type].pid = &init_struct_pid;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
struct task_struct *fork_idle(int cpu)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct task_struct *task;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	task = copy_process(CLONE_VM, 0, 0, NULL, &init_struct_pid, 0);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (!IS_ERR(task)) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		init_idle_pids(task->pids);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		init_idle(task, cpu);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return task;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 *  Ok, this is the main fork-routine.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 *static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * It copies the process, and if successful kick-startsstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * it and waits for it to finish using the VM if required.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
long do_fork(unsigned long clone_flags,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	      unsigned long stack_start,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	      unsigned long stack_size,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	      int __user *parent_tidptr,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	      int __user *child_tidptr)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct task_struct *p;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	int trace = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	long nr;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * Determine whether and which event to report to ptracer.  Whenstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * called from kernel_thread or CLONE_UNTRACED is explicitlystatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * requested, no event is reported; otherwise, report if the eventstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * for the type of forking is enabled.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (!(clone_flags & CLONE_UNTRACED)) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (clone_flags & CLONE_VFORK)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			trace = PTRACE_EVENT_VFORK;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		else if ((clone_flags & CSIGNAL) != SIGCHLD)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			trace = PTRACE_EVENT_CLONE;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		elsestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			trace = PTRACE_EVENT_FORK;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (likely(!ptrace_event_enabled(current, trace)))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			trace = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	p = copy_process(clone_flags, stack_start, stack_size,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			 child_tidptr, NULL, trace);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * Do this prior waking up the new thread - the thread pointerstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * might get invalid after that point, if the thread exits quickly.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (!IS_ERR(p)) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		struct completion vfork;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		trace_sched_process_fork(current, p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		nr = task_pid_vnr(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (clone_flags & CLONE_PARENT_SETTID)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			put_user(nr, parent_tidptr);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (clone_flags & CLONE_VFORK) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			p->vfork_done = &vfork;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			init_completion(&vfork);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			get_task_struct(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		wake_up_new_task(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		/* forking complete and child started to run, tell ptracer */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (unlikely(trace))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			ptrace_event(trace, nr);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (clone_flags & CLONE_VFORK) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			if (!wait_for_vfork_done(p, &vfork))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
				ptrace_event(PTRACE_EVENT_VFORK_DONE, nr);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	} else {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		nr = PTR_ERR(p);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return nr;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * Create a kernel thread.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
pid_t kernel_thread(int (*fn)(void *), void *arg, unsigned long flags)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return do_fork(flags|CLONE_VM|CLONE_UNTRACED, (unsigned long)fn,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		(unsigned long)arg, NULL, NULL);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef __ARCH_WANT_SYS_FORKstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
SYSCALL_DEFINE0(fork)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_MMUstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return do_fork(SIGCHLD, 0, 0, NULL, NULL);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#elsestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/* can not support in nommu mode */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return -EINVAL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef __ARCH_WANT_SYS_VFORKstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
SYSCALL_DEFINE0(vfork)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return do_fork(CLONE_VFORK | CLONE_VM | SIGCHLD, 0,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			0, NULL, NULL);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef __ARCH_WANT_SYS_CLONEstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifdef CONFIG_CLONE_BACKWARDSstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
SYSCALL_DEFINE5(clone, unsigned long, clone_flags, unsigned long, newsp,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		 int __user *, parent_tidptr,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		 int, tls_val,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		 int __user *, child_tidptr)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#elif defined(CONFIG_CLONE_BACKWARDS2)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
SYSCALL_DEFINE5(clone, unsigned long, newsp, unsigned long, clone_flags,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		 int __user *, parent_tidptr,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		 int __user *, child_tidptr,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		 int, tls_val)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#elif defined(CONFIG_CLONE_BACKWARDS3)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
SYSCALL_DEFINE6(clone, unsigned long, clone_flags, unsigned long, newsp,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		int, stack_size,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		int __user *, parent_tidptr,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		int __user *, child_tidptr,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		int, tls_val)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#elsestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
SYSCALL_DEFINE5(clone, unsigned long, clone_flags, unsigned long, newsp,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		 int __user *, parent_tidptr,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		 int __user *, child_tidptr,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		 int, tls_val)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return do_fork(clone_flags, newsp, 0, parent_tidptr, child_tidptr);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#ifndef ARCH_MIN_MMSTRUCT_ALIGNstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#define ARCH_MIN_MMSTRUCT_ALIGN 0static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
#endifstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static void sighand_ctor(void *data)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct sighand_struct *sighand = data;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	spin_lock_init(&sighand->siglock);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	init_waitqueue_head(&sighand->signalfd_wqh);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
void __init proc_caches_init(void)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	sighand_cachep = kmem_cache_create("sighand_cache",static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			sizeof(struct sighand_struct), 0,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			SLAB_HWCACHE_ALIGN|SLAB_PANIC|SLAB_DESTROY_BY_RCU|static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			SLAB_NOTRACK, sighand_ctor);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	signal_cachep = kmem_cache_create("signal_cache",static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			sizeof(struct signal_struct), 0,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			SLAB_HWCACHE_ALIGN|SLAB_PANIC|SLAB_NOTRACK, NULL);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	files_cachep = kmem_cache_create("files_cache",static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			sizeof(struct files_struct), 0,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			SLAB_HWCACHE_ALIGN|SLAB_PANIC|SLAB_NOTRACK, NULL);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	fs_cachep = kmem_cache_create("fs_cache",static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			sizeof(struct fs_struct), 0,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			SLAB_HWCACHE_ALIGN|SLAB_PANIC|SLAB_NOTRACK, NULL);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * FIXME! The "sizeof(struct mm_struct)" currently includes thestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * whole struct cpumask for the OFFSTACK case. We could changestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * this to *only* allocate as much of it as required by thestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * maximum number of CPU's we can ever have.  The cpumask_allocationstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * is at the end of the structure, exactly for that reason.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mm_cachep = kmem_cache_create("mm_struct",static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			sizeof(struct mm_struct), ARCH_MIN_MMSTRUCT_ALIGN,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			SLAB_HWCACHE_ALIGN|SLAB_PANIC|SLAB_NOTRACK, NULL);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	vm_area_cachep = KMEM_CACHE(vm_area_struct, SLAB_PANIC);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	mmap_init();static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	nsproxy_cache_init();static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * Check constraints on flags passed to the unshare system call.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int check_unshare_flags(unsigned long unshare_flags)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (unshare_flags & ~(CLONE_THREAD|CLONE_FS|CLONE_NEWNS|CLONE_SIGHAND|static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
				CLONE_VM|CLONE_FILES|CLONE_SYSVSEM|static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
				CLONE_NEWUTS|CLONE_NEWIPC|CLONE_NEWNET|static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
				CLONE_NEWUSER|CLONE_NEWPID))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		return -EINVAL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * Not implemented, but pretend it works if there is nothing tostatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * unshare. Note that unsharing CLONE_THREAD or CLONE_SIGHANDstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * needs to unshare vm.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (unshare_flags & (CLONE_THREAD | CLONE_SIGHAND | CLONE_VM)) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		/* FIXME: get_task_mm() increments ->mm_users */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (atomic_read(&current->mm->mm_users) > 1)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			return -EINVAL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * Unshare the filesystem structure if it is being sharedstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int unshare_fs(unsigned long unshare_flags, struct fs_struct **new_fsp)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct fs_struct *fs = current->fs;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (!(unshare_flags & CLONE_FS) || !fs)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		return 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/* don't need lock here; in the worst case we'll do useless copy */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (fs->users == 1)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		return 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	*new_fsp = copy_fs_struct(fs);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (!*new_fsp)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		return -ENOMEM;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * Unshare file descriptor table if it is being sharedstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int unshare_fd(unsigned long unshare_flags, struct files_struct **new_fdp)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct files_struct *fd = current->files;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	int error = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if ((unshare_flags & CLONE_FILES) &&static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	    (fd && atomic_read(&fd->count) > 1)) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		*new_fdp = dup_fd(fd, &error);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (!*new_fdp)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			return error;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * unshare allows a process to 'unshare' part of the processstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * context which was originally shared using clone.  copy_*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * functions used by do_fork() cannot be used here directlystatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * because they modify an inactive task_struct that is beingstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * constructed. Here we are modifying the current, active,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 * task_struct.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
SYSCALL_DEFINE1(unshare, unsigned long, unshare_flags)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct fs_struct *fs, *new_fs = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct files_struct *fd, *new_fd = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct cred *new_cred = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct nsproxy *new_nsproxy = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	int do_sysvsem = 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	int err;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * If unsharing a user namespace must also unshare the thread.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (unshare_flags & CLONE_NEWUSER)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		unshare_flags |= CLONE_THREAD | CLONE_FS;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * If unsharing a thread from a thread group, must also unshare vm.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (unshare_flags & CLONE_THREAD)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		unshare_flags |= CLONE_VM;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * If unsharing vm, must also unshare signal handlers.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (unshare_flags & CLONE_VM)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		unshare_flags |= CLONE_SIGHAND;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * If unsharing namespace, must also unshare filesystem information.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (unshare_flags & CLONE_NEWNS)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		unshare_flags |= CLONE_FS;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	err = check_unshare_flags(unshare_flags);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (err)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto bad_unshare_out;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * CLONE_NEWIPC must also detach from the undolist: after switchingstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * to a new ipc namespace, the semaphore arrays from the oldstatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 * namespace are unreachable.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (unshare_flags & (CLONE_NEWIPC|CLONE_SYSVSEM))static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		do_sysvsem = 1;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	err = unshare_fs(unshare_flags, &new_fs);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (err)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto bad_unshare_out;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	err = unshare_fd(unshare_flags, &new_fd);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (err)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto bad_unshare_cleanup_fs;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	err = unshare_userns(unshare_flags, &new_cred);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (err)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto bad_unshare_cleanup_fd;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	err = unshare_nsproxy_namespaces(unshare_flags, &new_nsproxy,static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
					 new_cred, new_fs);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (err)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		goto bad_unshare_cleanup_cred;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (new_fs || new_fd || do_sysvsem || new_cred || new_nsproxy) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (do_sysvsem) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			 * CLONE_SYSVSEM is equivalent to sys_exit().static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			exit_sem(current);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (new_nsproxy)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			switch_task_namespaces(current, new_nsproxy);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		task_lock(current);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (new_fs) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			fs = current->fs;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			spin_lock(&fs->lock);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			current->fs = new_fs;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			if (--fs->users)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
				new_fs = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			elsestatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
				new_fs = fs;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			spin_unlock(&fs->lock);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (new_fd) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			fd = current->files;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			current->files = new_fd;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			new_fd = fd;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		task_unlock(current);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		if (new_cred) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			/* Install the new user namespace */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			commit_creds(new_cred);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
			new_cred = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
bad_unshare_cleanup_cred:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (new_cred)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		put_cred(new_cred);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
bad_unshare_cleanup_fd:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (new_fd)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		put_files_struct(new_fd);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
bad_unshare_cleanup_fs:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (new_fs)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		free_fs_struct(new_fs);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
bad_unshare_out:static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return err;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
/*static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 *	Helper to unshare the files of the current task.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 *	We don't want to expose copy_files internals tostatic int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 *	the exec layer of the kernel.static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
 */static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
int unshare_files(struct files_struct **displaced)static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
{static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct task_struct *task = current;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	struct files_struct *copy = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	int error;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	error = unshare_fd(CLONE_FILES, &copy);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	if (error || !copy) {static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		*displaced = NULL;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
		return error;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	}static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	*displaced = task->files;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	task_lock(task);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	task->files = copy;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	task_unlock(task);static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
	return 0;static int gpio_set_wake_irq(struct irq_data *d, u32 enable)
}