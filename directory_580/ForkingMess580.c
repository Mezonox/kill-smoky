	chained_irq_enter(chip, desc);
/*	chained_irq_enter(chip, desc);
 *  linux/kernel/fork.c	chained_irq_enter(chip, desc);
 *	chained_irq_enter(chip, desc);
 *  Copyright (C) 1991, 1992  Linus Torvalds	chained_irq_enter(chip, desc);
 */	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/*	chained_irq_enter(chip, desc);
 *  'fork.c' contains the help-routines for the 'fork' system call	chained_irq_enter(chip, desc);
 * (see also entry.S and others).	chained_irq_enter(chip, desc);
 * Fork is rather simple, once you get the hang of it, but the memory	chained_irq_enter(chip, desc);
 * management can be a bitch. See 'mm/memory.c': 'copy_page_range()'	chained_irq_enter(chip, desc);
 */	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
#include <linux/slab.h>	chained_irq_enter(chip, desc);
#include <linux/init.h>	chained_irq_enter(chip, desc);
#include <linux/unistd.h>	chained_irq_enter(chip, desc);
#include <linux/module.h>	chained_irq_enter(chip, desc);
#include <linux/vmalloc.h>	chained_irq_enter(chip, desc);
#include <linux/completion.h>	chained_irq_enter(chip, desc);
#include <linux/personality.h>	chained_irq_enter(chip, desc);
#include <linux/mempolicy.h>	chained_irq_enter(chip, desc);
#include <linux/sem.h>	chained_irq_enter(chip, desc);
#include <linux/file.h>	chained_irq_enter(chip, desc);
#include <linux/fdtable.h>	chained_irq_enter(chip, desc);
#include <linux/iocontext.h>	chained_irq_enter(chip, desc);
#include <linux/key.h>	chained_irq_enter(chip, desc);
#include <linux/binfmts.h>	chained_irq_enter(chip, desc);
#include <linux/mman.h>	chained_irq_enter(chip, desc);
#include <linux/mmu_notifier.h>	chained_irq_enter(chip, desc);
#include <linux/fs.h>	chained_irq_enter(chip, desc);
#include <linux/nsproxy.h>	chained_irq_enter(chip, desc);
#include <linux/capability.h>	chained_irq_enter(chip, desc);
#include <linux/cpu.h>	chained_irq_enter(chip, desc);
#include <linux/cgroup.h>	chained_irq_enter(chip, desc);
#include <linux/security.h>	chained_irq_enter(chip, desc);
#include <linux/hugetlb.h>	chained_irq_enter(chip, desc);
#include <linux/seccomp.h>	chained_irq_enter(chip, desc);
#include <linux/swap.h>	chained_irq_enter(chip, desc);
#include <linux/syscalls.h>	chained_irq_enter(chip, desc);
#include <linux/jiffies.h>	chained_irq_enter(chip, desc);
#include <linux/futex.h>	chained_irq_enter(chip, desc);
#include <linux/compat.h>	chained_irq_enter(chip, desc);
#include <linux/kthread.h>	chained_irq_enter(chip, desc);
#include <linux/task_io_accounting_ops.h>	chained_irq_enter(chip, desc);
#include <linux/rcupdate.h>	chained_irq_enter(chip, desc);
#include <linux/ptrace.h>	chained_irq_enter(chip, desc);
#include <linux/mount.h>	chained_irq_enter(chip, desc);
#include <linux/audit.h>	chained_irq_enter(chip, desc);
#include <linux/memcontrol.h>	chained_irq_enter(chip, desc);
#include <linux/ftrace.h>	chained_irq_enter(chip, desc);
#include <linux/proc_fs.h>	chained_irq_enter(chip, desc);
#include <linux/profile.h>	chained_irq_enter(chip, desc);
#include <linux/rmap.h>	chained_irq_enter(chip, desc);
#include <linux/ksm.h>	chained_irq_enter(chip, desc);
#include <linux/acct.h>	chained_irq_enter(chip, desc);
#include <linux/tsacct_kern.h>	chained_irq_enter(chip, desc);
#include <linux/cn_proc.h>	chained_irq_enter(chip, desc);
#include <linux/freezer.h>	chained_irq_enter(chip, desc);
#include <linux/delayacct.h>	chained_irq_enter(chip, desc);
#include <linux/taskstats_kern.h>	chained_irq_enter(chip, desc);
#include <linux/random.h>	chained_irq_enter(chip, desc);
#include <linux/tty.h>	chained_irq_enter(chip, desc);
#include <linux/blkdev.h>	chained_irq_enter(chip, desc);
#include <linux/fs_struct.h>	chained_irq_enter(chip, desc);
#include <linux/magic.h>	chained_irq_enter(chip, desc);
#include <linux/perf_event.h>	chained_irq_enter(chip, desc);
#include <linux/posix-timers.h>	chained_irq_enter(chip, desc);
#include <linux/user-return-notifier.h>	chained_irq_enter(chip, desc);
#include <linux/oom.h>	chained_irq_enter(chip, desc);
#include <linux/khugepaged.h>	chained_irq_enter(chip, desc);
#include <linux/signalfd.h>	chained_irq_enter(chip, desc);
#include <linux/uprobes.h>	chained_irq_enter(chip, desc);
#include <linux/aio.h>	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
#include <asm/pgtable.h>	chained_irq_enter(chip, desc);
#include <asm/pgalloc.h>	chained_irq_enter(chip, desc);
#include <asm/uaccess.h>	chained_irq_enter(chip, desc);
#include <asm/mmu_context.h>	chained_irq_enter(chip, desc);
#include <asm/cacheflush.h>	chained_irq_enter(chip, desc);
#include <asm/tlbflush.h>	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
#include <trace/events/sched.h>	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
#define CREATE_TRACE_POINTS	chained_irq_enter(chip, desc);
#include <trace/events/task.h>	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/*	chained_irq_enter(chip, desc);
 * Protected counters by write_lock_irq(&tasklist_lock)	chained_irq_enter(chip, desc);
 */	chained_irq_enter(chip, desc);
unsigned long total_forks;	/* Handle normal Linux uptimes. */	chained_irq_enter(chip, desc);
int nr_threads;			/* The idle threads do not count.. */	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
int max_threads;		/* tunable limit on nr_threads */	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
DEFINE_PER_CPU(unsigned long, process_counts) = 0;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
__cacheline_aligned DEFINE_RWLOCK(tasklist_lock);  /* outer */	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
#ifdef CONFIG_PROVE_RCU	chained_irq_enter(chip, desc);
int lockdep_tasklist_lock_is_held(void)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	return lockdep_is_held(&tasklist_lock);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
EXPORT_SYMBOL_GPL(lockdep_tasklist_lock_is_held);	chained_irq_enter(chip, desc);
#endif /* #ifdef CONFIG_PROVE_RCU */	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
int nr_processes(void)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	int cpu;	chained_irq_enter(chip, desc);
	int total = 0;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	for_each_possible_cpu(cpu)	chained_irq_enter(chip, desc);
		total += per_cpu(process_counts, cpu);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	return total;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
void __weak arch_release_task_struct(struct task_struct *tsk)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
#ifndef CONFIG_ARCH_TASK_STRUCT_ALLOCATOR	chained_irq_enter(chip, desc);
static struct kmem_cache *task_struct_cachep;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static inline struct task_struct *alloc_task_struct_node(int node)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	return kmem_cache_alloc_node(task_struct_cachep, GFP_KERNEL, node);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static inline void free_task_struct(struct task_struct *tsk)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	kmem_cache_free(task_struct_cachep, tsk);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
void __weak arch_release_thread_info(struct thread_info *ti)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
#ifndef CONFIG_ARCH_THREAD_INFO_ALLOCATOR	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/*	chained_irq_enter(chip, desc);
 * Allocate pages if THREAD_SIZE is >= PAGE_SIZE, otherwise use a	chained_irq_enter(chip, desc);
 * kmemcache based allocator.	chained_irq_enter(chip, desc);
 */	chained_irq_enter(chip, desc);
# if THREAD_SIZE >= PAGE_SIZE	chained_irq_enter(chip, desc);
static struct thread_info *alloc_thread_info_node(struct task_struct *tsk,	chained_irq_enter(chip, desc);
						  int node)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	struct page *page = alloc_pages_node(node, THREADINFO_GFP_ACCOUNTED,	chained_irq_enter(chip, desc);
					     THREAD_SIZE_ORDER);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	return page ? page_address(page) : NULL;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static inline void free_thread_info(struct thread_info *ti)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	free_memcg_kmem_pages((unsigned long)ti, THREAD_SIZE_ORDER);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
# else	chained_irq_enter(chip, desc);
static struct kmem_cache *thread_info_cache;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static struct thread_info *alloc_thread_info_node(struct task_struct *tsk,	chained_irq_enter(chip, desc);
						  int node)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	return kmem_cache_alloc_node(thread_info_cache, THREADINFO_GFP, node);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static void free_thread_info(struct thread_info *ti)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	kmem_cache_free(thread_info_cache, ti);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
void thread_info_cache_init(void)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	thread_info_cache = kmem_cache_create("thread_info", THREAD_SIZE,	chained_irq_enter(chip, desc);
					      THREAD_SIZE, 0, NULL);	chained_irq_enter(chip, desc);
	BUG_ON(thread_info_cache == NULL);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
# endif	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/* SLAB cache for signal_struct structures (tsk->signal) */	chained_irq_enter(chip, desc);
static struct kmem_cache *signal_cachep;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/* SLAB cache for sighand_struct structures (tsk->sighand) */	chained_irq_enter(chip, desc);
struct kmem_cache *sighand_cachep;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/* SLAB cache for files_struct structures (tsk->files) */	chained_irq_enter(chip, desc);
struct kmem_cache *files_cachep;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/* SLAB cache for fs_struct structures (tsk->fs) */	chained_irq_enter(chip, desc);
struct kmem_cache *fs_cachep;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/* SLAB cache for vm_area_struct structures */	chained_irq_enter(chip, desc);
struct kmem_cache *vm_area_cachep;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/* SLAB cache for mm_struct structures (tsk->mm) */	chained_irq_enter(chip, desc);
static struct kmem_cache *mm_cachep;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static void account_kernel_stack(struct thread_info *ti, int account)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	struct zone *zone = page_zone(virt_to_page(ti));	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	mod_zone_page_state(zone, NR_KERNEL_STACK, account);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
void free_task(struct task_struct *tsk)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	account_kernel_stack(tsk->stack, -1);	chained_irq_enter(chip, desc);
	arch_release_thread_info(tsk->stack);	chained_irq_enter(chip, desc);
	free_thread_info(tsk->stack);	chained_irq_enter(chip, desc);
	rt_mutex_debug_task_free(tsk);	chained_irq_enter(chip, desc);
	ftrace_graph_exit_task(tsk);	chained_irq_enter(chip, desc);
	put_seccomp_filter(tsk);	chained_irq_enter(chip, desc);
	arch_release_task_struct(tsk);	chained_irq_enter(chip, desc);
	free_task_struct(tsk);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
EXPORT_SYMBOL(free_task);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static inline void free_signal_struct(struct signal_struct *sig)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	taskstats_tgid_free(sig);	chained_irq_enter(chip, desc);
	sched_autogroup_exit(sig);	chained_irq_enter(chip, desc);
	kmem_cache_free(signal_cachep, sig);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static inline void put_signal_struct(struct signal_struct *sig)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	if (atomic_dec_and_test(&sig->sigcnt))	chained_irq_enter(chip, desc);
		free_signal_struct(sig);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
void __put_task_struct(struct task_struct *tsk)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	WARN_ON(!tsk->exit_state);	chained_irq_enter(chip, desc);
	WARN_ON(atomic_read(&tsk->usage));	chained_irq_enter(chip, desc);
	WARN_ON(tsk == current);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	task_numa_free(tsk);	chained_irq_enter(chip, desc);
	security_task_free(tsk);	chained_irq_enter(chip, desc);
	exit_creds(tsk);	chained_irq_enter(chip, desc);
	delayacct_tsk_free(tsk);	chained_irq_enter(chip, desc);
	put_signal_struct(tsk->signal);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	if (!profile_handoff_task(tsk))	chained_irq_enter(chip, desc);
		free_task(tsk);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
EXPORT_SYMBOL_GPL(__put_task_struct);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
void __init __weak arch_task_cache_init(void) { }	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
void __init fork_init(unsigned long mempages)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
#ifndef CONFIG_ARCH_TASK_STRUCT_ALLOCATOR	chained_irq_enter(chip, desc);
#ifndef ARCH_MIN_TASKALIGN	chained_irq_enter(chip, desc);
#define ARCH_MIN_TASKALIGN	L1_CACHE_BYTES	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	/* create a slab on which task_structs can be allocated */	chained_irq_enter(chip, desc);
	task_struct_cachep =	chained_irq_enter(chip, desc);
		kmem_cache_create("task_struct", sizeof(struct task_struct),	chained_irq_enter(chip, desc);
			ARCH_MIN_TASKALIGN, SLAB_PANIC | SLAB_NOTRACK, NULL);	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/* do the arch specific task caches init */	chained_irq_enter(chip, desc);
	arch_task_cache_init();	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * The default maximum number of threads is set to a safe	chained_irq_enter(chip, desc);
	 * value: the thread structures can take up at most half	chained_irq_enter(chip, desc);
	 * of memory.	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	max_threads = mempages / (8 * THREAD_SIZE / PAGE_SIZE);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * we need to allow at least 20 threads to boot a system	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	if (max_threads < 20)	chained_irq_enter(chip, desc);
		max_threads = 20;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	init_task.signal->rlim[RLIMIT_NPROC].rlim_cur = max_threads/2;	chained_irq_enter(chip, desc);
	init_task.signal->rlim[RLIMIT_NPROC].rlim_max = max_threads/2;	chained_irq_enter(chip, desc);
	init_task.signal->rlim[RLIMIT_SIGPENDING] =	chained_irq_enter(chip, desc);
		init_task.signal->rlim[RLIMIT_NPROC];	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
int __attribute__((weak)) arch_dup_task_struct(struct task_struct *dst,	chained_irq_enter(chip, desc);
					       struct task_struct *src)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	*dst = *src;	chained_irq_enter(chip, desc);
	return 0;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static struct task_struct *dup_task_struct(struct task_struct *orig)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	struct task_struct *tsk;	chained_irq_enter(chip, desc);
	struct thread_info *ti;	chained_irq_enter(chip, desc);
	unsigned long *stackend;	chained_irq_enter(chip, desc);
	int node = tsk_fork_get_node(orig);	chained_irq_enter(chip, desc);
	int err;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	tsk = alloc_task_struct_node(node);	chained_irq_enter(chip, desc);
	if (!tsk)	chained_irq_enter(chip, desc);
		return NULL;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	ti = alloc_thread_info_node(tsk, node);	chained_irq_enter(chip, desc);
	if (!ti)	chained_irq_enter(chip, desc);
		goto free_tsk;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	err = arch_dup_task_struct(tsk, orig);	chained_irq_enter(chip, desc);
	if (err)	chained_irq_enter(chip, desc);
		goto free_ti;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	tsk->stack = ti;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	setup_thread_stack(tsk, orig);	chained_irq_enter(chip, desc);
	clear_user_return_notifier(tsk);	chained_irq_enter(chip, desc);
	clear_tsk_need_resched(tsk);	chained_irq_enter(chip, desc);
	stackend = end_of_stack(tsk);	chained_irq_enter(chip, desc);
	*stackend = STACK_END_MAGIC;	/* for overflow detection */	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
#ifdef CONFIG_CC_STACKPROTECTOR	chained_irq_enter(chip, desc);
	tsk->stack_canary = get_random_int();	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * One for us, one for whoever does the "release_task()" (usually	chained_irq_enter(chip, desc);
	 * parent)	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	atomic_set(&tsk->usage, 2);	chained_irq_enter(chip, desc);
#ifdef CONFIG_BLK_DEV_IO_TRACE	chained_irq_enter(chip, desc);
	tsk->btrace_seq = 0;	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	tsk->splice_pipe = NULL;	chained_irq_enter(chip, desc);
	tsk->task_frag.page = NULL;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	account_kernel_stack(ti, 1);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	return tsk;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
free_ti:	chained_irq_enter(chip, desc);
	free_thread_info(ti);	chained_irq_enter(chip, desc);
free_tsk:	chained_irq_enter(chip, desc);
	free_task_struct(tsk);	chained_irq_enter(chip, desc);
	return NULL;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
#ifdef CONFIG_MMU	chained_irq_enter(chip, desc);
static int dup_mmap(struct mm_struct *mm, struct mm_struct *oldmm)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	struct vm_area_struct *mpnt, *tmp, *prev, **pprev;	chained_irq_enter(chip, desc);
	struct rb_node **rb_link, *rb_parent;	chained_irq_enter(chip, desc);
	int retval;	chained_irq_enter(chip, desc);
	unsigned long charge;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	uprobe_start_dup_mmap();	chained_irq_enter(chip, desc);
	down_write(&oldmm->mmap_sem);	chained_irq_enter(chip, desc);
	flush_cache_dup_mm(oldmm);	chained_irq_enter(chip, desc);
	uprobe_dup_mmap(oldmm, mm);	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * Not linked in yet - no deadlock potential:	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	down_write_nested(&mm->mmap_sem, SINGLE_DEPTH_NESTING);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	mm->locked_vm = 0;	chained_irq_enter(chip, desc);
	mm->mmap = NULL;	chained_irq_enter(chip, desc);
	mm->mmap_cache = NULL;	chained_irq_enter(chip, desc);
	mm->map_count = 0;	chained_irq_enter(chip, desc);
	cpumask_clear(mm_cpumask(mm));	chained_irq_enter(chip, desc);
	mm->mm_rb = RB_ROOT;	chained_irq_enter(chip, desc);
	rb_link = &mm->mm_rb.rb_node;	chained_irq_enter(chip, desc);
	rb_parent = NULL;	chained_irq_enter(chip, desc);
	pprev = &mm->mmap;	chained_irq_enter(chip, desc);
	retval = ksm_fork(mm, oldmm);	chained_irq_enter(chip, desc);
	if (retval)	chained_irq_enter(chip, desc);
		goto out;	chained_irq_enter(chip, desc);
	retval = khugepaged_fork(mm, oldmm);	chained_irq_enter(chip, desc);
	if (retval)	chained_irq_enter(chip, desc);
		goto out;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	prev = NULL;	chained_irq_enter(chip, desc);
	for (mpnt = oldmm->mmap; mpnt; mpnt = mpnt->vm_next) {	chained_irq_enter(chip, desc);
		struct file *file;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		if (mpnt->vm_flags & VM_DONTCOPY) {	chained_irq_enter(chip, desc);
			vm_stat_account(mm, mpnt->vm_flags, mpnt->vm_file,	chained_irq_enter(chip, desc);
							-vma_pages(mpnt));	chained_irq_enter(chip, desc);
			continue;	chained_irq_enter(chip, desc);
		}	chained_irq_enter(chip, desc);
		charge = 0;	chained_irq_enter(chip, desc);
		if (mpnt->vm_flags & VM_ACCOUNT) {	chained_irq_enter(chip, desc);
			unsigned long len = vma_pages(mpnt);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
			if (security_vm_enough_memory_mm(oldmm, len)) /* sic */	chained_irq_enter(chip, desc);
				goto fail_nomem;	chained_irq_enter(chip, desc);
			charge = len;	chained_irq_enter(chip, desc);
		}	chained_irq_enter(chip, desc);
		tmp = kmem_cache_alloc(vm_area_cachep, GFP_KERNEL);	chained_irq_enter(chip, desc);
		if (!tmp)	chained_irq_enter(chip, desc);
			goto fail_nomem;	chained_irq_enter(chip, desc);
		*tmp = *mpnt;	chained_irq_enter(chip, desc);
		INIT_LIST_HEAD(&tmp->anon_vma_chain);	chained_irq_enter(chip, desc);
		retval = vma_dup_policy(mpnt, tmp);	chained_irq_enter(chip, desc);
		if (retval)	chained_irq_enter(chip, desc);
			goto fail_nomem_policy;	chained_irq_enter(chip, desc);
		tmp->vm_mm = mm;	chained_irq_enter(chip, desc);
		if (anon_vma_fork(tmp, mpnt))	chained_irq_enter(chip, desc);
			goto fail_nomem_anon_vma_fork;	chained_irq_enter(chip, desc);
		tmp->vm_flags &= ~VM_LOCKED;	chained_irq_enter(chip, desc);
		tmp->vm_next = tmp->vm_prev = NULL;	chained_irq_enter(chip, desc);
		file = tmp->vm_file;	chained_irq_enter(chip, desc);
		if (file) {	chained_irq_enter(chip, desc);
			struct inode *inode = file_inode(file);	chained_irq_enter(chip, desc);
			struct address_space *mapping = file->f_mapping;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
			get_file(file);	chained_irq_enter(chip, desc);
			if (tmp->vm_flags & VM_DENYWRITE)	chained_irq_enter(chip, desc);
				atomic_dec(&inode->i_writecount);	chained_irq_enter(chip, desc);
			mutex_lock(&mapping->i_mmap_mutex);	chained_irq_enter(chip, desc);
			if (tmp->vm_flags & VM_SHARED)	chained_irq_enter(chip, desc);
				mapping->i_mmap_writable++;	chained_irq_enter(chip, desc);
			flush_dcache_mmap_lock(mapping);	chained_irq_enter(chip, desc);
			/* insert tmp into the share list, just after mpnt */	chained_irq_enter(chip, desc);
			if (unlikely(tmp->vm_flags & VM_NONLINEAR))	chained_irq_enter(chip, desc);
				vma_nonlinear_insert(tmp,	chained_irq_enter(chip, desc);
						&mapping->i_mmap_nonlinear);	chained_irq_enter(chip, desc);
			else	chained_irq_enter(chip, desc);
				vma_interval_tree_insert_after(tmp, mpnt,	chained_irq_enter(chip, desc);
							&mapping->i_mmap);	chained_irq_enter(chip, desc);
			flush_dcache_mmap_unlock(mapping);	chained_irq_enter(chip, desc);
			mutex_unlock(&mapping->i_mmap_mutex);	chained_irq_enter(chip, desc);
		}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		/*	chained_irq_enter(chip, desc);
		 * Clear hugetlb-related page reserves for children. This only	chained_irq_enter(chip, desc);
		 * affects MAP_PRIVATE mappings. Faults generated by the child	chained_irq_enter(chip, desc);
		 * are not guaranteed to succeed, even if read-only	chained_irq_enter(chip, desc);
		 */	chained_irq_enter(chip, desc);
		if (is_vm_hugetlb_page(tmp))	chained_irq_enter(chip, desc);
			reset_vma_resv_huge_pages(tmp);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		/*	chained_irq_enter(chip, desc);
		 * Link in the new vma and copy the page table entries.	chained_irq_enter(chip, desc);
		 */	chained_irq_enter(chip, desc);
		*pprev = tmp;	chained_irq_enter(chip, desc);
		pprev = &tmp->vm_next;	chained_irq_enter(chip, desc);
		tmp->vm_prev = prev;	chained_irq_enter(chip, desc);
		prev = tmp;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		__vma_link_rb(mm, tmp, rb_link, rb_parent);	chained_irq_enter(chip, desc);
		rb_link = &tmp->vm_rb.rb_right;	chained_irq_enter(chip, desc);
		rb_parent = &tmp->vm_rb;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		mm->map_count++;	chained_irq_enter(chip, desc);
		retval = copy_page_range(mm, oldmm, mpnt);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		if (tmp->vm_ops && tmp->vm_ops->open)	chained_irq_enter(chip, desc);
			tmp->vm_ops->open(tmp);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		if (retval)	chained_irq_enter(chip, desc);
			goto out;	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	/* a new mm has just been created */	chained_irq_enter(chip, desc);
	arch_dup_mmap(oldmm, mm);	chained_irq_enter(chip, desc);
	retval = 0;	chained_irq_enter(chip, desc);
out:	chained_irq_enter(chip, desc);
	up_write(&mm->mmap_sem);	chained_irq_enter(chip, desc);
	flush_tlb_mm(oldmm);	chained_irq_enter(chip, desc);
	up_write(&oldmm->mmap_sem);	chained_irq_enter(chip, desc);
	uprobe_end_dup_mmap();	chained_irq_enter(chip, desc);
	return retval;	chained_irq_enter(chip, desc);
fail_nomem_anon_vma_fork:	chained_irq_enter(chip, desc);
	mpol_put(vma_policy(tmp));	chained_irq_enter(chip, desc);
fail_nomem_policy:	chained_irq_enter(chip, desc);
	kmem_cache_free(vm_area_cachep, tmp);	chained_irq_enter(chip, desc);
fail_nomem:	chained_irq_enter(chip, desc);
	retval = -ENOMEM;	chained_irq_enter(chip, desc);
	vm_unacct_memory(charge);	chained_irq_enter(chip, desc);
	goto out;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static inline int mm_alloc_pgd(struct mm_struct *mm)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	mm->pgd = pgd_alloc(mm);	chained_irq_enter(chip, desc);
	if (unlikely(!mm->pgd))	chained_irq_enter(chip, desc);
		return -ENOMEM;	chained_irq_enter(chip, desc);
	return 0;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static inline void mm_free_pgd(struct mm_struct *mm)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	pgd_free(mm, mm->pgd);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
#else	chained_irq_enter(chip, desc);
#define dup_mmap(mm, oldmm)	(0)	chained_irq_enter(chip, desc);
#define mm_alloc_pgd(mm)	(0)	chained_irq_enter(chip, desc);
#define mm_free_pgd(mm)	chained_irq_enter(chip, desc);
#endif /* CONFIG_MMU */	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
__cacheline_aligned_in_smp DEFINE_SPINLOCK(mmlist_lock);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
#define allocate_mm()	(kmem_cache_alloc(mm_cachep, GFP_KERNEL))	chained_irq_enter(chip, desc);
#define free_mm(mm)	(kmem_cache_free(mm_cachep, (mm)))	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static unsigned long default_dump_filter = MMF_DUMP_FILTER_DEFAULT;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static int __init coredump_filter_setup(char *s)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	default_dump_filter =	chained_irq_enter(chip, desc);
		(simple_strtoul(s, NULL, 0) << MMF_DUMP_FILTER_SHIFT) &	chained_irq_enter(chip, desc);
		MMF_DUMP_FILTER_MASK;	chained_irq_enter(chip, desc);
	return 1;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
__setup("coredump_filter=", coredump_filter_setup);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
#include <linux/init_task.h>	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static void mm_init_aio(struct mm_struct *mm)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
#ifdef CONFIG_AIO	chained_irq_enter(chip, desc);
	spin_lock_init(&mm->ioctx_lock);	chained_irq_enter(chip, desc);
	mm->ioctx_table = NULL;	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static struct mm_struct *mm_init(struct mm_struct *mm, struct task_struct *p)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	atomic_set(&mm->mm_users, 1);	chained_irq_enter(chip, desc);
	atomic_set(&mm->mm_count, 1);	chained_irq_enter(chip, desc);
	init_rwsem(&mm->mmap_sem);	chained_irq_enter(chip, desc);
	INIT_LIST_HEAD(&mm->mmlist);	chained_irq_enter(chip, desc);
	mm->flags = (current->mm) ?	chained_irq_enter(chip, desc);
		(current->mm->flags & MMF_INIT_MASK) : default_dump_filter;	chained_irq_enter(chip, desc);
	mm->core_state = NULL;	chained_irq_enter(chip, desc);
	atomic_long_set(&mm->nr_ptes, 0);	chained_irq_enter(chip, desc);
	memset(&mm->rss_stat, 0, sizeof(mm->rss_stat));	chained_irq_enter(chip, desc);
	spin_lock_init(&mm->page_table_lock);	chained_irq_enter(chip, desc);
	mm_init_aio(mm);	chained_irq_enter(chip, desc);
	mm_init_owner(mm, p);	chained_irq_enter(chip, desc);
	clear_tlb_flush_pending(mm);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	if (likely(!mm_alloc_pgd(mm))) {	chained_irq_enter(chip, desc);
		mm->def_flags = 0;	chained_irq_enter(chip, desc);
		mmu_notifier_mm_init(mm);	chained_irq_enter(chip, desc);
		return mm;	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	free_mm(mm);	chained_irq_enter(chip, desc);
	return NULL;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static void check_mm(struct mm_struct *mm)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	int i;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	for (i = 0; i < NR_MM_COUNTERS; i++) {	chained_irq_enter(chip, desc);
		long x = atomic_long_read(&mm->rss_stat.count[i]);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		if (unlikely(x))	chained_irq_enter(chip, desc);
			printk(KERN_ALERT "BUG: Bad rss-counter state "	chained_irq_enter(chip, desc);
					  "mm:%p idx:%d val:%ld\n", mm, i, x);	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
#if defined(CONFIG_TRANSPARENT_HUGEPAGE) && !USE_SPLIT_PMD_PTLOCKS	chained_irq_enter(chip, desc);
	VM_BUG_ON(mm->pmd_huge_pte);	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/*	chained_irq_enter(chip, desc);
 * Allocate and initialize an mm_struct.	chained_irq_enter(chip, desc);
 */	chained_irq_enter(chip, desc);
struct mm_struct *mm_alloc(void)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	struct mm_struct *mm;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	mm = allocate_mm();	chained_irq_enter(chip, desc);
	if (!mm)	chained_irq_enter(chip, desc);
		return NULL;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	memset(mm, 0, sizeof(*mm));	chained_irq_enter(chip, desc);
	mm_init_cpumask(mm);	chained_irq_enter(chip, desc);
	return mm_init(mm, current);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/*	chained_irq_enter(chip, desc);
 * Called when the last reference to the mm	chained_irq_enter(chip, desc);
 * is dropped: either by a lazy thread or by	chained_irq_enter(chip, desc);
 * mmput. Free the page directory and the mm.	chained_irq_enter(chip, desc);
 */	chained_irq_enter(chip, desc);
void __mmdrop(struct mm_struct *mm)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	BUG_ON(mm == &init_mm);	chained_irq_enter(chip, desc);
	mm_free_pgd(mm);	chained_irq_enter(chip, desc);
	destroy_context(mm);	chained_irq_enter(chip, desc);
	mmu_notifier_mm_destroy(mm);	chained_irq_enter(chip, desc);
	check_mm(mm);	chained_irq_enter(chip, desc);
	free_mm(mm);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
EXPORT_SYMBOL_GPL(__mmdrop);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/*	chained_irq_enter(chip, desc);
 * Decrement the use count and release all resources for an mm.	chained_irq_enter(chip, desc);
 */	chained_irq_enter(chip, desc);
void mmput(struct mm_struct *mm)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	might_sleep();	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	if (atomic_dec_and_test(&mm->mm_users)) {	chained_irq_enter(chip, desc);
		uprobe_clear_state(mm);	chained_irq_enter(chip, desc);
		exit_aio(mm);	chained_irq_enter(chip, desc);
		ksm_exit(mm);	chained_irq_enter(chip, desc);
		khugepaged_exit(mm); /* must run before exit_mmap */	chained_irq_enter(chip, desc);
		exit_mmap(mm);	chained_irq_enter(chip, desc);
		set_mm_exe_file(mm, NULL);	chained_irq_enter(chip, desc);
		if (!list_empty(&mm->mmlist)) {	chained_irq_enter(chip, desc);
			spin_lock(&mmlist_lock);	chained_irq_enter(chip, desc);
			list_del(&mm->mmlist);	chained_irq_enter(chip, desc);
			spin_unlock(&mmlist_lock);	chained_irq_enter(chip, desc);
		}	chained_irq_enter(chip, desc);
		if (mm->binfmt)	chained_irq_enter(chip, desc);
			module_put(mm->binfmt->module);	chained_irq_enter(chip, desc);
		mmdrop(mm);	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
EXPORT_SYMBOL_GPL(mmput);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
void set_mm_exe_file(struct mm_struct *mm, struct file *new_exe_file)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	if (new_exe_file)	chained_irq_enter(chip, desc);
		get_file(new_exe_file);	chained_irq_enter(chip, desc);
	if (mm->exe_file)	chained_irq_enter(chip, desc);
		fput(mm->exe_file);	chained_irq_enter(chip, desc);
	mm->exe_file = new_exe_file;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
struct file *get_mm_exe_file(struct mm_struct *mm)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	struct file *exe_file;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/* We need mmap_sem to protect against races with removal of exe_file */	chained_irq_enter(chip, desc);
	down_read(&mm->mmap_sem);	chained_irq_enter(chip, desc);
	exe_file = mm->exe_file;	chained_irq_enter(chip, desc);
	if (exe_file)	chained_irq_enter(chip, desc);
		get_file(exe_file);	chained_irq_enter(chip, desc);
	up_read(&mm->mmap_sem);	chained_irq_enter(chip, desc);
	return exe_file;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static void dup_mm_exe_file(struct mm_struct *oldmm, struct mm_struct *newmm)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	/* It's safe to write the exe_file pointer without exe_file_lock because	chained_irq_enter(chip, desc);
	 * this is called during fork when the task is not yet in /proc */	chained_irq_enter(chip, desc);
	newmm->exe_file = get_mm_exe_file(oldmm);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/**	chained_irq_enter(chip, desc);
 * get_task_mm - acquire a reference to the task's mm	chained_irq_enter(chip, desc);
 *	chained_irq_enter(chip, desc);
 * Returns %NULL if the task has no mm.  Checks PF_KTHREAD (meaning	chained_irq_enter(chip, desc);
 * this kernel workthread has transiently adopted a user mm with use_mm,	chained_irq_enter(chip, desc);
 * to do its AIO) is not set and if so returns a reference to it, after	chained_irq_enter(chip, desc);
 * bumping up the use count.  User must release the mm via mmput()	chained_irq_enter(chip, desc);
 * after use.  Typically used by /proc and ptrace.	chained_irq_enter(chip, desc);
 */	chained_irq_enter(chip, desc);
struct mm_struct *get_task_mm(struct task_struct *task)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	struct mm_struct *mm;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	task_lock(task);	chained_irq_enter(chip, desc);
	mm = task->mm;	chained_irq_enter(chip, desc);
	if (mm) {	chained_irq_enter(chip, desc);
		if (task->flags & PF_KTHREAD)	chained_irq_enter(chip, desc);
			mm = NULL;	chained_irq_enter(chip, desc);
		else	chained_irq_enter(chip, desc);
			atomic_inc(&mm->mm_users);	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	task_unlock(task);	chained_irq_enter(chip, desc);
	return mm;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
EXPORT_SYMBOL_GPL(get_task_mm);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
struct mm_struct *mm_access(struct task_struct *task, unsigned int mode)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	struct mm_struct *mm;	chained_irq_enter(chip, desc);
	int err;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	err =  mutex_lock_killable(&task->signal->cred_guard_mutex);	chained_irq_enter(chip, desc);
	if (err)	chained_irq_enter(chip, desc);
		return ERR_PTR(err);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	mm = get_task_mm(task);	chained_irq_enter(chip, desc);
	if (mm && mm != current->mm &&	chained_irq_enter(chip, desc);
			!ptrace_may_access(task, mode)) {	chained_irq_enter(chip, desc);
		mmput(mm);	chained_irq_enter(chip, desc);
		mm = ERR_PTR(-EACCES);	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	mutex_unlock(&task->signal->cred_guard_mutex);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	return mm;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static void complete_vfork_done(struct task_struct *tsk)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	struct completion *vfork;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	task_lock(tsk);	chained_irq_enter(chip, desc);
	vfork = tsk->vfork_done;	chained_irq_enter(chip, desc);
	if (likely(vfork)) {	chained_irq_enter(chip, desc);
		tsk->vfork_done = NULL;	chained_irq_enter(chip, desc);
		complete(vfork);	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	task_unlock(tsk);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static int wait_for_vfork_done(struct task_struct *child,	chained_irq_enter(chip, desc);
				struct completion *vfork)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	int killed;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	freezer_do_not_count();	chained_irq_enter(chip, desc);
	killed = wait_for_completion_killable(vfork);	chained_irq_enter(chip, desc);
	freezer_count();	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	if (killed) {	chained_irq_enter(chip, desc);
		task_lock(child);	chained_irq_enter(chip, desc);
		child->vfork_done = NULL;	chained_irq_enter(chip, desc);
		task_unlock(child);	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	put_task_struct(child);	chained_irq_enter(chip, desc);
	return killed;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/* Please note the differences between mmput and mm_release.	chained_irq_enter(chip, desc);
 * mmput is called whenever we stop holding onto a mm_struct,	chained_irq_enter(chip, desc);
 * error success whatever.	chained_irq_enter(chip, desc);
 *	chained_irq_enter(chip, desc);
 * mm_release is called after a mm_struct has been removed	chained_irq_enter(chip, desc);
 * from the current process.	chained_irq_enter(chip, desc);
 *	chained_irq_enter(chip, desc);
 * This difference is important for error handling, when we	chained_irq_enter(chip, desc);
 * only half set up a mm_struct for a new process and need to restore	chained_irq_enter(chip, desc);
 * the old one.  Because we mmput the new mm_struct before	chained_irq_enter(chip, desc);
 * restoring the old one. . .	chained_irq_enter(chip, desc);
 * Eric Biederman 10 January 1998	chained_irq_enter(chip, desc);
 */	chained_irq_enter(chip, desc);
void mm_release(struct task_struct *tsk, struct mm_struct *mm)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	/* Get rid of any futexes when releasing the mm */	chained_irq_enter(chip, desc);
#ifdef CONFIG_FUTEX	chained_irq_enter(chip, desc);
	if (unlikely(tsk->robust_list)) {	chained_irq_enter(chip, desc);
		exit_robust_list(tsk);	chained_irq_enter(chip, desc);
		tsk->robust_list = NULL;	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
#ifdef CONFIG_COMPAT	chained_irq_enter(chip, desc);
	if (unlikely(tsk->compat_robust_list)) {	chained_irq_enter(chip, desc);
		compat_exit_robust_list(tsk);	chained_irq_enter(chip, desc);
		tsk->compat_robust_list = NULL;	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	if (unlikely(!list_empty(&tsk->pi_state_list)))	chained_irq_enter(chip, desc);
		exit_pi_state_list(tsk);	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	uprobe_free_utask(tsk);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/* Get rid of any cached register state */	chained_irq_enter(chip, desc);
	deactivate_mm(tsk, mm);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * If we're exiting normally, clear a user-space tid field if	chained_irq_enter(chip, desc);
	 * requested.  We leave this alone when dying by signal, to leave	chained_irq_enter(chip, desc);
	 * the value intact in a core dump, and to save the unnecessary	chained_irq_enter(chip, desc);
	 * trouble, say, a killed vfork parent shouldn't touch this mm.	chained_irq_enter(chip, desc);
	 * Userland only wants this done for a sys_exit.	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	if (tsk->clear_child_tid) {	chained_irq_enter(chip, desc);
		if (!(tsk->flags & PF_SIGNALED) &&	chained_irq_enter(chip, desc);
		    atomic_read(&mm->mm_users) > 1) {	chained_irq_enter(chip, desc);
			/*	chained_irq_enter(chip, desc);
			 * We don't check the error code - if userspace has	chained_irq_enter(chip, desc);
			 * not set up a proper pointer then tough luck.	chained_irq_enter(chip, desc);
			 */	chained_irq_enter(chip, desc);
			put_user(0, tsk->clear_child_tid);	chained_irq_enter(chip, desc);
			sys_futex(tsk->clear_child_tid, FUTEX_WAKE,	chained_irq_enter(chip, desc);
					1, NULL, NULL, 0);	chained_irq_enter(chip, desc);
		}	chained_irq_enter(chip, desc);
		tsk->clear_child_tid = NULL;	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * All done, finally we can wake up parent and return this mm to him.	chained_irq_enter(chip, desc);
	 * Also kthread_stop() uses this completion for synchronization.	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	if (tsk->vfork_done)	chained_irq_enter(chip, desc);
		complete_vfork_done(tsk);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/*	chained_irq_enter(chip, desc);
 * Allocate a new mm structure and copy contents from the	chained_irq_enter(chip, desc);
 * mm structure of the passed in task structure.	chained_irq_enter(chip, desc);
 */	chained_irq_enter(chip, desc);
static struct mm_struct *dup_mm(struct task_struct *tsk)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	struct mm_struct *mm, *oldmm = current->mm;	chained_irq_enter(chip, desc);
	int err;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	mm = allocate_mm();	chained_irq_enter(chip, desc);
	if (!mm)	chained_irq_enter(chip, desc);
		goto fail_nomem;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	memcpy(mm, oldmm, sizeof(*mm));	chained_irq_enter(chip, desc);
	mm_init_cpumask(mm);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
#if defined(CONFIG_TRANSPARENT_HUGEPAGE) && !USE_SPLIT_PMD_PTLOCKS	chained_irq_enter(chip, desc);
	mm->pmd_huge_pte = NULL;	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	if (!mm_init(mm, tsk))	chained_irq_enter(chip, desc);
		goto fail_nomem;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	if (init_new_context(tsk, mm))	chained_irq_enter(chip, desc);
		goto fail_nocontext;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	dup_mm_exe_file(oldmm, mm);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	err = dup_mmap(mm, oldmm);	chained_irq_enter(chip, desc);
	if (err)	chained_irq_enter(chip, desc);
		goto free_pt;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	mm->hiwater_rss = get_mm_rss(mm);	chained_irq_enter(chip, desc);
	mm->hiwater_vm = mm->total_vm;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	if (mm->binfmt && !try_module_get(mm->binfmt->module))	chained_irq_enter(chip, desc);
		goto free_pt;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	return mm;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
free_pt:	chained_irq_enter(chip, desc);
	/* don't put binfmt in mmput, we haven't got module yet */	chained_irq_enter(chip, desc);
	mm->binfmt = NULL;	chained_irq_enter(chip, desc);
	mmput(mm);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
fail_nomem:	chained_irq_enter(chip, desc);
	return NULL;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
fail_nocontext:	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * If init_new_context() failed, we cannot use mmput() to free the mm	chained_irq_enter(chip, desc);
	 * because it calls destroy_context()	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	mm_free_pgd(mm);	chained_irq_enter(chip, desc);
	free_mm(mm);	chained_irq_enter(chip, desc);
	return NULL;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static int copy_mm(unsigned long clone_flags, struct task_struct *tsk)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	struct mm_struct *mm, *oldmm;	chained_irq_enter(chip, desc);
	int retval;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	tsk->min_flt = tsk->maj_flt = 0;	chained_irq_enter(chip, desc);
	tsk->nvcsw = tsk->nivcsw = 0;	chained_irq_enter(chip, desc);
#ifdef CONFIG_DETECT_HUNG_TASK	chained_irq_enter(chip, desc);
	tsk->last_switch_count = tsk->nvcsw + tsk->nivcsw;	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	tsk->mm = NULL;	chained_irq_enter(chip, desc);
	tsk->active_mm = NULL;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * Are we cloning a kernel thread?	chained_irq_enter(chip, desc);
	 *	chained_irq_enter(chip, desc);
	 * We need to steal a active VM for that..	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	oldmm = current->mm;	chained_irq_enter(chip, desc);
	if (!oldmm)	chained_irq_enter(chip, desc);
		return 0;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	if (clone_flags & CLONE_VM) {	chained_irq_enter(chip, desc);
		atomic_inc(&oldmm->mm_users);	chained_irq_enter(chip, desc);
		mm = oldmm;	chained_irq_enter(chip, desc);
		goto good_mm;	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	retval = -ENOMEM;	chained_irq_enter(chip, desc);
	mm = dup_mm(tsk);	chained_irq_enter(chip, desc);
	if (!mm)	chained_irq_enter(chip, desc);
		goto fail_nomem;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
good_mm:	chained_irq_enter(chip, desc);
	tsk->mm = mm;	chained_irq_enter(chip, desc);
	tsk->active_mm = mm;	chained_irq_enter(chip, desc);
	return 0;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
fail_nomem:	chained_irq_enter(chip, desc);
	return retval;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static int copy_fs(unsigned long clone_flags, struct task_struct *tsk)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	struct fs_struct *fs = current->fs;	chained_irq_enter(chip, desc);
	if (clone_flags & CLONE_FS) {	chained_irq_enter(chip, desc);
		/* tsk->fs is already what we want */	chained_irq_enter(chip, desc);
		spin_lock(&fs->lock);	chained_irq_enter(chip, desc);
		if (fs->in_exec) {	chained_irq_enter(chip, desc);
			spin_unlock(&fs->lock);	chained_irq_enter(chip, desc);
			return -EAGAIN;	chained_irq_enter(chip, desc);
		}	chained_irq_enter(chip, desc);
		fs->users++;	chained_irq_enter(chip, desc);
		spin_unlock(&fs->lock);	chained_irq_enter(chip, desc);
		return 0;	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	tsk->fs = copy_fs_struct(fs);	chained_irq_enter(chip, desc);
	if (!tsk->fs)	chained_irq_enter(chip, desc);
		return -ENOMEM;	chained_irq_enter(chip, desc);
	return 0;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static int copy_files(unsigned long clone_flags, struct task_struct *tsk)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	struct files_struct *oldf, *newf;	chained_irq_enter(chip, desc);
	int error = 0;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * A background process may not have any files ...	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	oldf = current->files;	chained_irq_enter(chip, desc);
	if (!oldf)	chained_irq_enter(chip, desc);
		goto out;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	if (clone_flags & CLONE_FILES) {	chained_irq_enter(chip, desc);
		atomic_inc(&oldf->count);	chained_irq_enter(chip, desc);
		goto out;	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	newf = dup_fd(oldf, &error);	chained_irq_enter(chip, desc);
	if (!newf)	chained_irq_enter(chip, desc);
		goto out;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	tsk->files = newf;	chained_irq_enter(chip, desc);
	error = 0;	chained_irq_enter(chip, desc);
out:	chained_irq_enter(chip, desc);
	return error;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static int copy_io(unsigned long clone_flags, struct task_struct *tsk)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
#ifdef CONFIG_BLOCK	chained_irq_enter(chip, desc);
	struct io_context *ioc = current->io_context;	chained_irq_enter(chip, desc);
	struct io_context *new_ioc;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	if (!ioc)	chained_irq_enter(chip, desc);
		return 0;	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * Share io context with parent, if CLONE_IO is set	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	if (clone_flags & CLONE_IO) {	chained_irq_enter(chip, desc);
		ioc_task_link(ioc);	chained_irq_enter(chip, desc);
		tsk->io_context = ioc;	chained_irq_enter(chip, desc);
	} else if (ioprio_valid(ioc->ioprio)) {	chained_irq_enter(chip, desc);
		new_ioc = get_task_io_context(tsk, GFP_KERNEL, NUMA_NO_NODE);	chained_irq_enter(chip, desc);
		if (unlikely(!new_ioc))	chained_irq_enter(chip, desc);
			return -ENOMEM;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		new_ioc->ioprio = ioc->ioprio;	chained_irq_enter(chip, desc);
		put_io_context(new_ioc);	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	return 0;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static int copy_sighand(unsigned long clone_flags, struct task_struct *tsk)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	struct sighand_struct *sig;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	if (clone_flags & CLONE_SIGHAND) {	chained_irq_enter(chip, desc);
		atomic_inc(&current->sighand->count);	chained_irq_enter(chip, desc);
		return 0;	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	sig = kmem_cache_alloc(sighand_cachep, GFP_KERNEL);	chained_irq_enter(chip, desc);
	rcu_assign_pointer(tsk->sighand, sig);	chained_irq_enter(chip, desc);
	if (!sig)	chained_irq_enter(chip, desc);
		return -ENOMEM;	chained_irq_enter(chip, desc);
	atomic_set(&sig->count, 1);	chained_irq_enter(chip, desc);
	memcpy(sig->action, current->sighand->action, sizeof(sig->action));	chained_irq_enter(chip, desc);
	return 0;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
void __cleanup_sighand(struct sighand_struct *sighand)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	if (atomic_dec_and_test(&sighand->count)) {	chained_irq_enter(chip, desc);
		signalfd_cleanup(sighand);	chained_irq_enter(chip, desc);
		kmem_cache_free(sighand_cachep, sighand);	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/*	chained_irq_enter(chip, desc);
 * Initialize POSIX timer handling for a thread group.	chained_irq_enter(chip, desc);
 */	chained_irq_enter(chip, desc);
static void posix_cpu_timers_init_group(struct signal_struct *sig)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	unsigned long cpu_limit;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/* Thread group counters. */	chained_irq_enter(chip, desc);
	thread_group_cputime_init(sig);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	cpu_limit = ACCESS_ONCE(sig->rlim[RLIMIT_CPU].rlim_cur);	chained_irq_enter(chip, desc);
	if (cpu_limit != RLIM_INFINITY) {	chained_irq_enter(chip, desc);
		sig->cputime_expires.prof_exp = secs_to_cputime(cpu_limit);	chained_irq_enter(chip, desc);
		sig->cputimer.running = 1;	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/* The timer lists. */	chained_irq_enter(chip, desc);
	INIT_LIST_HEAD(&sig->cpu_timers[0]);	chained_irq_enter(chip, desc);
	INIT_LIST_HEAD(&sig->cpu_timers[1]);	chained_irq_enter(chip, desc);
	INIT_LIST_HEAD(&sig->cpu_timers[2]);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static int copy_signal(unsigned long clone_flags, struct task_struct *tsk)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	struct signal_struct *sig;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	if (clone_flags & CLONE_THREAD)	chained_irq_enter(chip, desc);
		return 0;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	sig = kmem_cache_zalloc(signal_cachep, GFP_KERNEL);	chained_irq_enter(chip, desc);
	tsk->signal = sig;	chained_irq_enter(chip, desc);
	if (!sig)	chained_irq_enter(chip, desc);
		return -ENOMEM;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	sig->nr_threads = 1;	chained_irq_enter(chip, desc);
	atomic_set(&sig->live, 1);	chained_irq_enter(chip, desc);
	atomic_set(&sig->sigcnt, 1);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/* list_add(thread_node, thread_head) without INIT_LIST_HEAD() */	chained_irq_enter(chip, desc);
	sig->thread_head = (struct list_head)LIST_HEAD_INIT(tsk->thread_node);	chained_irq_enter(chip, desc);
	tsk->thread_node = (struct list_head)LIST_HEAD_INIT(sig->thread_head);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	init_waitqueue_head(&sig->wait_chldexit);	chained_irq_enter(chip, desc);
	sig->curr_target = tsk;	chained_irq_enter(chip, desc);
	init_sigpending(&sig->shared_pending);	chained_irq_enter(chip, desc);
	INIT_LIST_HEAD(&sig->posix_timers);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	hrtimer_init(&sig->real_timer, CLOCK_MONOTONIC, HRTIMER_MODE_REL);	chained_irq_enter(chip, desc);
	sig->real_timer.function = it_real_fn;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	task_lock(current->group_leader);	chained_irq_enter(chip, desc);
	memcpy(sig->rlim, current->signal->rlim, sizeof sig->rlim);	chained_irq_enter(chip, desc);
	task_unlock(current->group_leader);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	posix_cpu_timers_init_group(sig);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	tty_audit_fork(sig);	chained_irq_enter(chip, desc);
	sched_autogroup_fork(sig);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
#ifdef CONFIG_CGROUPS	chained_irq_enter(chip, desc);
	init_rwsem(&sig->group_rwsem);	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	sig->oom_score_adj = current->signal->oom_score_adj;	chained_irq_enter(chip, desc);
	sig->oom_score_adj_min = current->signal->oom_score_adj_min;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	sig->has_child_subreaper = current->signal->has_child_subreaper ||	chained_irq_enter(chip, desc);
				   current->signal->is_child_subreaper;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	mutex_init(&sig->cred_guard_mutex);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	return 0;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static void copy_flags(unsigned long clone_flags, struct task_struct *p)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	unsigned long new_flags = p->flags;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	new_flags &= ~(PF_SUPERPRIV | PF_WQ_WORKER);	chained_irq_enter(chip, desc);
	new_flags |= PF_FORKNOEXEC;	chained_irq_enter(chip, desc);
	p->flags = new_flags;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
SYSCALL_DEFINE1(set_tid_address, int __user *, tidptr)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	current->clear_child_tid = tidptr;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	return task_pid_vnr(current);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static void rt_mutex_init_task(struct task_struct *p)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	raw_spin_lock_init(&p->pi_lock);	chained_irq_enter(chip, desc);
#ifdef CONFIG_RT_MUTEXES	chained_irq_enter(chip, desc);
	p->pi_waiters = RB_ROOT;	chained_irq_enter(chip, desc);
	p->pi_waiters_leftmost = NULL;	chained_irq_enter(chip, desc);
	p->pi_blocked_on = NULL;	chained_irq_enter(chip, desc);
	p->pi_top_task = NULL;	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
#ifdef CONFIG_MM_OWNER	chained_irq_enter(chip, desc);
void mm_init_owner(struct mm_struct *mm, struct task_struct *p)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	mm->owner = p;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
#endif /* CONFIG_MM_OWNER */	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/*	chained_irq_enter(chip, desc);
 * Initialize POSIX timer handling for a single task.	chained_irq_enter(chip, desc);
 */	chained_irq_enter(chip, desc);
static void posix_cpu_timers_init(struct task_struct *tsk)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	tsk->cputime_expires.prof_exp = 0;	chained_irq_enter(chip, desc);
	tsk->cputime_expires.virt_exp = 0;	chained_irq_enter(chip, desc);
	tsk->cputime_expires.sched_exp = 0;	chained_irq_enter(chip, desc);
	INIT_LIST_HEAD(&tsk->cpu_timers[0]);	chained_irq_enter(chip, desc);
	INIT_LIST_HEAD(&tsk->cpu_timers[1]);	chained_irq_enter(chip, desc);
	INIT_LIST_HEAD(&tsk->cpu_timers[2]);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static inline void	chained_irq_enter(chip, desc);
init_task_pid(struct task_struct *task, enum pid_type type, struct pid *pid)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	 task->pids[type].pid = pid;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/*	chained_irq_enter(chip, desc);
 * This creates a new process as a copy of the old one,	chained_irq_enter(chip, desc);
 * but does not actually start it yet.	chained_irq_enter(chip, desc);
 *	chained_irq_enter(chip, desc);
 * It copies the registers, and all the appropriate	chained_irq_enter(chip, desc);
 * parts of the process environment (as per the clone	chained_irq_enter(chip, desc);
 * flags). The actual kick-off is left to the caller.	chained_irq_enter(chip, desc);
 */	chained_irq_enter(chip, desc);
static struct task_struct *copy_process(unsigned long clone_flags,	chained_irq_enter(chip, desc);
					unsigned long stack_start,	chained_irq_enter(chip, desc);
					unsigned long stack_size,	chained_irq_enter(chip, desc);
					int __user *child_tidptr,	chained_irq_enter(chip, desc);
					struct pid *pid,	chained_irq_enter(chip, desc);
					int trace)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	int retval;	chained_irq_enter(chip, desc);
	struct task_struct *p;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	if ((clone_flags & (CLONE_NEWNS|CLONE_FS)) == (CLONE_NEWNS|CLONE_FS))	chained_irq_enter(chip, desc);
		return ERR_PTR(-EINVAL);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	if ((clone_flags & (CLONE_NEWUSER|CLONE_FS)) == (CLONE_NEWUSER|CLONE_FS))	chained_irq_enter(chip, desc);
		return ERR_PTR(-EINVAL);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * Thread groups must share signals as well, and detached threads	chained_irq_enter(chip, desc);
	 * can only be started up within the thread group.	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	if ((clone_flags & CLONE_THREAD) && !(clone_flags & CLONE_SIGHAND))	chained_irq_enter(chip, desc);
		return ERR_PTR(-EINVAL);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * Shared signal handlers imply shared VM. By way of the above,	chained_irq_enter(chip, desc);
	 * thread groups also imply shared VM. Blocking this case allows	chained_irq_enter(chip, desc);
	 * for various simplifications in other code.	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	if ((clone_flags & CLONE_SIGHAND) && !(clone_flags & CLONE_VM))	chained_irq_enter(chip, desc);
		return ERR_PTR(-EINVAL);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * Siblings of global init remain as zombies on exit since they are	chained_irq_enter(chip, desc);
	 * not reaped by their parent (swapper). To solve this and to avoid	chained_irq_enter(chip, desc);
	 * multi-rooted process trees, prevent global and container-inits	chained_irq_enter(chip, desc);
	 * from creating siblings.	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	if ((clone_flags & CLONE_PARENT) &&	chained_irq_enter(chip, desc);
				current->signal->flags & SIGNAL_UNKILLABLE)	chained_irq_enter(chip, desc);
		return ERR_PTR(-EINVAL);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * If the new process will be in a different pid or user namespace	chained_irq_enter(chip, desc);
	 * do not allow it to share a thread group or signal handlers or	chained_irq_enter(chip, desc);
	 * parent with the forking task.	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	if (clone_flags & CLONE_SIGHAND) {	chained_irq_enter(chip, desc);
		if ((clone_flags & (CLONE_NEWUSER | CLONE_NEWPID)) ||	chained_irq_enter(chip, desc);
		    (task_active_pid_ns(current) !=	chained_irq_enter(chip, desc);
				current->nsproxy->pid_ns_for_children))	chained_irq_enter(chip, desc);
			return ERR_PTR(-EINVAL);	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	retval = security_task_create(clone_flags);	chained_irq_enter(chip, desc);
	if (retval)	chained_irq_enter(chip, desc);
		goto fork_out;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	retval = -ENOMEM;	chained_irq_enter(chip, desc);
	p = dup_task_struct(current);	chained_irq_enter(chip, desc);
	if (!p)	chained_irq_enter(chip, desc);
		goto fork_out;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	ftrace_graph_init_task(p);	chained_irq_enter(chip, desc);
	get_seccomp_filter(p);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	rt_mutex_init_task(p);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
#ifdef CONFIG_PROVE_LOCKING	chained_irq_enter(chip, desc);
	DEBUG_LOCKS_WARN_ON(!p->hardirqs_enabled);	chained_irq_enter(chip, desc);
	DEBUG_LOCKS_WARN_ON(!p->softirqs_enabled);	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	retval = -EAGAIN;	chained_irq_enter(chip, desc);
	if (atomic_read(&p->real_cred->user->processes) >=	chained_irq_enter(chip, desc);
			task_rlimit(p, RLIMIT_NPROC)) {	chained_irq_enter(chip, desc);
		if (p->real_cred->user != INIT_USER &&	chained_irq_enter(chip, desc);
		    !capable(CAP_SYS_RESOURCE) && !capable(CAP_SYS_ADMIN))	chained_irq_enter(chip, desc);
			goto bad_fork_free;	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	current->flags &= ~PF_NPROC_EXCEEDED;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	retval = copy_creds(p, clone_flags);	chained_irq_enter(chip, desc);
	if (retval < 0)	chained_irq_enter(chip, desc);
		goto bad_fork_free;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * If multiple threads are within copy_process(), then this check	chained_irq_enter(chip, desc);
	 * triggers too late. This doesn't hurt, the check is only there	chained_irq_enter(chip, desc);
	 * to stop root fork bombs.	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	retval = -EAGAIN;	chained_irq_enter(chip, desc);
	if (nr_threads >= max_threads)	chained_irq_enter(chip, desc);
		goto bad_fork_cleanup_count;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	if (!try_module_get(task_thread_info(p)->exec_domain->module))	chained_irq_enter(chip, desc);
		goto bad_fork_cleanup_count;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	delayacct_tsk_init(p);	/* Must remain after dup_task_struct() */	chained_irq_enter(chip, desc);
	copy_flags(clone_flags, p);	chained_irq_enter(chip, desc);
	INIT_LIST_HEAD(&p->children);	chained_irq_enter(chip, desc);
	INIT_LIST_HEAD(&p->sibling);	chained_irq_enter(chip, desc);
	rcu_copy_process(p);	chained_irq_enter(chip, desc);
	p->vfork_done = NULL;	chained_irq_enter(chip, desc);
	spin_lock_init(&p->alloc_lock);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	init_sigpending(&p->pending);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	p->utime = p->stime = p->gtime = 0;	chained_irq_enter(chip, desc);
	p->utimescaled = p->stimescaled = 0;	chained_irq_enter(chip, desc);
#ifndef CONFIG_VIRT_CPU_ACCOUNTING_NATIVE	chained_irq_enter(chip, desc);
	p->prev_cputime.utime = p->prev_cputime.stime = 0;	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
#ifdef CONFIG_VIRT_CPU_ACCOUNTING_GEN	chained_irq_enter(chip, desc);
	seqlock_init(&p->vtime_seqlock);	chained_irq_enter(chip, desc);
	p->vtime_snap = 0;	chained_irq_enter(chip, desc);
	p->vtime_snap_whence = VTIME_SLEEPING;	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
#if defined(SPLIT_RSS_COUNTING)	chained_irq_enter(chip, desc);
	memset(&p->rss_stat, 0, sizeof(p->rss_stat));	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	p->default_timer_slack_ns = current->timer_slack_ns;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	task_io_accounting_init(&p->ioac);	chained_irq_enter(chip, desc);
	acct_clear_integrals(p);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	posix_cpu_timers_init(p);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	do_posix_clock_monotonic_gettime(&p->start_time);	chained_irq_enter(chip, desc);
	p->real_start_time = p->start_time;	chained_irq_enter(chip, desc);
	monotonic_to_bootbased(&p->real_start_time);	chained_irq_enter(chip, desc);
	p->io_context = NULL;	chained_irq_enter(chip, desc);
	p->audit_context = NULL;	chained_irq_enter(chip, desc);
	if (clone_flags & CLONE_THREAD)	chained_irq_enter(chip, desc);
		threadgroup_change_begin(current);	chained_irq_enter(chip, desc);
	cgroup_fork(p);	chained_irq_enter(chip, desc);
#ifdef CONFIG_NUMA	chained_irq_enter(chip, desc);
	p->mempolicy = mpol_dup(p->mempolicy);	chained_irq_enter(chip, desc);
	if (IS_ERR(p->mempolicy)) {	chained_irq_enter(chip, desc);
		retval = PTR_ERR(p->mempolicy);	chained_irq_enter(chip, desc);
		p->mempolicy = NULL;	chained_irq_enter(chip, desc);
		goto bad_fork_cleanup_cgroup;	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	mpol_fix_fork_child_flag(p);	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
#ifdef CONFIG_CPUSETS	chained_irq_enter(chip, desc);
	p->cpuset_mem_spread_rotor = NUMA_NO_NODE;	chained_irq_enter(chip, desc);
	p->cpuset_slab_spread_rotor = NUMA_NO_NODE;	chained_irq_enter(chip, desc);
	seqcount_init(&p->mems_allowed_seq);	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
#ifdef CONFIG_TRACE_IRQFLAGS	chained_irq_enter(chip, desc);
	p->irq_events = 0;	chained_irq_enter(chip, desc);
	p->hardirqs_enabled = 0;	chained_irq_enter(chip, desc);
	p->hardirq_enable_ip = 0;	chained_irq_enter(chip, desc);
	p->hardirq_enable_event = 0;	chained_irq_enter(chip, desc);
	p->hardirq_disable_ip = _THIS_IP_;	chained_irq_enter(chip, desc);
	p->hardirq_disable_event = 0;	chained_irq_enter(chip, desc);
	p->softirqs_enabled = 1;	chained_irq_enter(chip, desc);
	p->softirq_enable_ip = _THIS_IP_;	chained_irq_enter(chip, desc);
	p->softirq_enable_event = 0;	chained_irq_enter(chip, desc);
	p->softirq_disable_ip = 0;	chained_irq_enter(chip, desc);
	p->softirq_disable_event = 0;	chained_irq_enter(chip, desc);
	p->hardirq_context = 0;	chained_irq_enter(chip, desc);
	p->softirq_context = 0;	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
#ifdef CONFIG_LOCKDEP	chained_irq_enter(chip, desc);
	p->lockdep_depth = 0; /* no locks held yet */	chained_irq_enter(chip, desc);
	p->curr_chain_key = 0;	chained_irq_enter(chip, desc);
	p->lockdep_recursion = 0;	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
#ifdef CONFIG_DEBUG_MUTEXES	chained_irq_enter(chip, desc);
	p->blocked_on = NULL; /* not blocked yet */	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
#ifdef CONFIG_MEMCG	chained_irq_enter(chip, desc);
	p->memcg_batch.do_batch = 0;	chained_irq_enter(chip, desc);
	p->memcg_batch.memcg = NULL;	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
#ifdef CONFIG_BCACHE	chained_irq_enter(chip, desc);
	p->sequential_io	= 0;	chained_irq_enter(chip, desc);
	p->sequential_io_avg	= 0;	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/* Perform scheduler related setup. Assign this task to a CPU. */	chained_irq_enter(chip, desc);
	retval = sched_fork(clone_flags, p);	chained_irq_enter(chip, desc);
	if (retval)	chained_irq_enter(chip, desc);
		goto bad_fork_cleanup_policy;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	retval = perf_event_init_task(p);	chained_irq_enter(chip, desc);
	if (retval)	chained_irq_enter(chip, desc);
		goto bad_fork_cleanup_policy;	chained_irq_enter(chip, desc);
	retval = audit_alloc(p);	chained_irq_enter(chip, desc);
	if (retval)	chained_irq_enter(chip, desc);
		goto bad_fork_cleanup_policy;	chained_irq_enter(chip, desc);
	/* copy all the process information */	chained_irq_enter(chip, desc);
	retval = copy_semundo(clone_flags, p);	chained_irq_enter(chip, desc);
	if (retval)	chained_irq_enter(chip, desc);
		goto bad_fork_cleanup_audit;	chained_irq_enter(chip, desc);
	retval = copy_files(clone_flags, p);	chained_irq_enter(chip, desc);
	if (retval)	chained_irq_enter(chip, desc);
		goto bad_fork_cleanup_semundo;	chained_irq_enter(chip, desc);
	retval = copy_fs(clone_flags, p);	chained_irq_enter(chip, desc);
	if (retval)	chained_irq_enter(chip, desc);
		goto bad_fork_cleanup_files;	chained_irq_enter(chip, desc);
	retval = copy_sighand(clone_flags, p);	chained_irq_enter(chip, desc);
	if (retval)	chained_irq_enter(chip, desc);
		goto bad_fork_cleanup_fs;	chained_irq_enter(chip, desc);
	retval = copy_signal(clone_flags, p);	chained_irq_enter(chip, desc);
	if (retval)	chained_irq_enter(chip, desc);
		goto bad_fork_cleanup_sighand;	chained_irq_enter(chip, desc);
	retval = copy_mm(clone_flags, p);	chained_irq_enter(chip, desc);
	if (retval)	chained_irq_enter(chip, desc);
		goto bad_fork_cleanup_signal;	chained_irq_enter(chip, desc);
	retval = copy_namespaces(clone_flags, p);	chained_irq_enter(chip, desc);
	if (retval)	chained_irq_enter(chip, desc);
		goto bad_fork_cleanup_mm;	chained_irq_enter(chip, desc);
	retval = copy_io(clone_flags, p);	chained_irq_enter(chip, desc);
	if (retval)	chained_irq_enter(chip, desc);
		goto bad_fork_cleanup_namespaces;	chained_irq_enter(chip, desc);
	retval = copy_thread(clone_flags, stack_start, stack_size, p);	chained_irq_enter(chip, desc);
	if (retval)	chained_irq_enter(chip, desc);
		goto bad_fork_cleanup_io;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	if (pid != &init_struct_pid) {	chained_irq_enter(chip, desc);
		retval = -ENOMEM;	chained_irq_enter(chip, desc);
		pid = alloc_pid(p->nsproxy->pid_ns_for_children);	chained_irq_enter(chip, desc);
		if (!pid)	chained_irq_enter(chip, desc);
			goto bad_fork_cleanup_io;	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	p->set_child_tid = (clone_flags & CLONE_CHILD_SETTID) ? child_tidptr : NULL;	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * Clear TID on mm_release()?	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	p->clear_child_tid = (clone_flags & CLONE_CHILD_CLEARTID) ? child_tidptr : NULL;	chained_irq_enter(chip, desc);
#ifdef CONFIG_BLOCK	chained_irq_enter(chip, desc);
	p->plug = NULL;	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
#ifdef CONFIG_FUTEX	chained_irq_enter(chip, desc);
	p->robust_list = NULL;	chained_irq_enter(chip, desc);
#ifdef CONFIG_COMPAT	chained_irq_enter(chip, desc);
	p->compat_robust_list = NULL;	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	INIT_LIST_HEAD(&p->pi_state_list);	chained_irq_enter(chip, desc);
	p->pi_state_cache = NULL;	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * sigaltstack should be cleared when sharing the same VM	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	if ((clone_flags & (CLONE_VM|CLONE_VFORK)) == CLONE_VM)	chained_irq_enter(chip, desc);
		p->sas_ss_sp = p->sas_ss_size = 0;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * Syscall tracing and stepping should be turned off in the	chained_irq_enter(chip, desc);
	 * child regardless of CLONE_PTRACE.	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	user_disable_single_step(p);	chained_irq_enter(chip, desc);
	clear_tsk_thread_flag(p, TIF_SYSCALL_TRACE);	chained_irq_enter(chip, desc);
#ifdef TIF_SYSCALL_EMU	chained_irq_enter(chip, desc);
	clear_tsk_thread_flag(p, TIF_SYSCALL_EMU);	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	clear_all_latency_tracing(p);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/* ok, now we should be set up.. */	chained_irq_enter(chip, desc);
	p->pid = pid_nr(pid);	chained_irq_enter(chip, desc);
	if (clone_flags & CLONE_THREAD) {	chained_irq_enter(chip, desc);
		p->exit_signal = -1;	chained_irq_enter(chip, desc);
		p->group_leader = current->group_leader;	chained_irq_enter(chip, desc);
		p->tgid = current->tgid;	chained_irq_enter(chip, desc);
	} else {	chained_irq_enter(chip, desc);
		if (clone_flags & CLONE_PARENT)	chained_irq_enter(chip, desc);
			p->exit_signal = current->group_leader->exit_signal;	chained_irq_enter(chip, desc);
		else	chained_irq_enter(chip, desc);
			p->exit_signal = (clone_flags & CSIGNAL);	chained_irq_enter(chip, desc);
		p->group_leader = p;	chained_irq_enter(chip, desc);
		p->tgid = p->pid;	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	p->nr_dirtied = 0;	chained_irq_enter(chip, desc);
	p->nr_dirtied_pause = 128 >> (PAGE_SHIFT - 10);	chained_irq_enter(chip, desc);
	p->dirty_paused_when = 0;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	p->pdeath_signal = 0;	chained_irq_enter(chip, desc);
	INIT_LIST_HEAD(&p->thread_group);	chained_irq_enter(chip, desc);
	p->task_works = NULL;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * Make it visible to the rest of the system, but dont wake it up yet.	chained_irq_enter(chip, desc);
	 * Need tasklist lock for parent etc handling!	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	write_lock_irq(&tasklist_lock);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/* CLONE_PARENT re-uses the old parent */	chained_irq_enter(chip, desc);
	if (clone_flags & (CLONE_PARENT|CLONE_THREAD)) {	chained_irq_enter(chip, desc);
		p->real_parent = current->real_parent;	chained_irq_enter(chip, desc);
		p->parent_exec_id = current->parent_exec_id;	chained_irq_enter(chip, desc);
	} else {	chained_irq_enter(chip, desc);
		p->real_parent = current;	chained_irq_enter(chip, desc);
		p->parent_exec_id = current->self_exec_id;	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	spin_lock(&current->sighand->siglock);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * Process group and session signals need to be delivered to just the	chained_irq_enter(chip, desc);
	 * parent before the fork or both the parent and the child after the	chained_irq_enter(chip, desc);
	 * fork. Restart if a signal comes in before we add the new process to	chained_irq_enter(chip, desc);
	 * it's process group.	chained_irq_enter(chip, desc);
	 * A fatal signal pending means that current will exit, so the new	chained_irq_enter(chip, desc);
	 * thread can't slip out of an OOM kill (or normal SIGKILL).	chained_irq_enter(chip, desc);
	*/	chained_irq_enter(chip, desc);
	recalc_sigpending();	chained_irq_enter(chip, desc);
	if (signal_pending(current)) {	chained_irq_enter(chip, desc);
		spin_unlock(&current->sighand->siglock);	chained_irq_enter(chip, desc);
		write_unlock_irq(&tasklist_lock);	chained_irq_enter(chip, desc);
		retval = -ERESTARTNOINTR;	chained_irq_enter(chip, desc);
		goto bad_fork_free_pid;	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	if (likely(p->pid)) {	chained_irq_enter(chip, desc);
		ptrace_init_task(p, (clone_flags & CLONE_PTRACE) || trace);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		init_task_pid(p, PIDTYPE_PID, pid);	chained_irq_enter(chip, desc);
		if (thread_group_leader(p)) {	chained_irq_enter(chip, desc);
			init_task_pid(p, PIDTYPE_PGID, task_pgrp(current));	chained_irq_enter(chip, desc);
			init_task_pid(p, PIDTYPE_SID, task_session(current));	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
			if (is_child_reaper(pid)) {	chained_irq_enter(chip, desc);
				ns_of_pid(pid)->child_reaper = p;	chained_irq_enter(chip, desc);
				p->signal->flags |= SIGNAL_UNKILLABLE;	chained_irq_enter(chip, desc);
			}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
			p->signal->leader_pid = pid;	chained_irq_enter(chip, desc);
			p->signal->tty = tty_kref_get(current->signal->tty);	chained_irq_enter(chip, desc);
			list_add_tail(&p->sibling, &p->real_parent->children);	chained_irq_enter(chip, desc);
			list_add_tail_rcu(&p->tasks, &init_task.tasks);	chained_irq_enter(chip, desc);
			attach_pid(p, PIDTYPE_PGID);	chained_irq_enter(chip, desc);
			attach_pid(p, PIDTYPE_SID);	chained_irq_enter(chip, desc);
			__this_cpu_inc(process_counts);	chained_irq_enter(chip, desc);
		} else {	chained_irq_enter(chip, desc);
			current->signal->nr_threads++;	chained_irq_enter(chip, desc);
			atomic_inc(&current->signal->live);	chained_irq_enter(chip, desc);
			atomic_inc(&current->signal->sigcnt);	chained_irq_enter(chip, desc);
			list_add_tail_rcu(&p->thread_group,	chained_irq_enter(chip, desc);
					  &p->group_leader->thread_group);	chained_irq_enter(chip, desc);
			list_add_tail_rcu(&p->thread_node,	chained_irq_enter(chip, desc);
					  &p->signal->thread_head);	chained_irq_enter(chip, desc);
		}	chained_irq_enter(chip, desc);
		attach_pid(p, PIDTYPE_PID);	chained_irq_enter(chip, desc);
		nr_threads++;	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	total_forks++;	chained_irq_enter(chip, desc);
	spin_unlock(&current->sighand->siglock);	chained_irq_enter(chip, desc);
	write_unlock_irq(&tasklist_lock);	chained_irq_enter(chip, desc);
	proc_fork_connector(p);	chained_irq_enter(chip, desc);
	cgroup_post_fork(p);	chained_irq_enter(chip, desc);
	if (clone_flags & CLONE_THREAD)	chained_irq_enter(chip, desc);
		threadgroup_change_end(current);	chained_irq_enter(chip, desc);
	perf_event_fork(p);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	trace_task_newtask(p, clone_flags);	chained_irq_enter(chip, desc);
	uprobe_copy_process(p, clone_flags);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	return p;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
bad_fork_free_pid:	chained_irq_enter(chip, desc);
	if (pid != &init_struct_pid)	chained_irq_enter(chip, desc);
		free_pid(pid);	chained_irq_enter(chip, desc);
bad_fork_cleanup_io:	chained_irq_enter(chip, desc);
	if (p->io_context)	chained_irq_enter(chip, desc);
		exit_io_context(p);	chained_irq_enter(chip, desc);
bad_fork_cleanup_namespaces:	chained_irq_enter(chip, desc);
	exit_task_namespaces(p);	chained_irq_enter(chip, desc);
bad_fork_cleanup_mm:	chained_irq_enter(chip, desc);
	if (p->mm)	chained_irq_enter(chip, desc);
		mmput(p->mm);	chained_irq_enter(chip, desc);
bad_fork_cleanup_signal:	chained_irq_enter(chip, desc);
	if (!(clone_flags & CLONE_THREAD))	chained_irq_enter(chip, desc);
		free_signal_struct(p->signal);	chained_irq_enter(chip, desc);
bad_fork_cleanup_sighand:	chained_irq_enter(chip, desc);
	__cleanup_sighand(p->sighand);	chained_irq_enter(chip, desc);
bad_fork_cleanup_fs:	chained_irq_enter(chip, desc);
	exit_fs(p); /* blocking */	chained_irq_enter(chip, desc);
bad_fork_cleanup_files:	chained_irq_enter(chip, desc);
	exit_files(p); /* blocking */	chained_irq_enter(chip, desc);
bad_fork_cleanup_semundo:	chained_irq_enter(chip, desc);
	exit_sem(p);	chained_irq_enter(chip, desc);
bad_fork_cleanup_audit:	chained_irq_enter(chip, desc);
	audit_free(p);	chained_irq_enter(chip, desc);
bad_fork_cleanup_policy:	chained_irq_enter(chip, desc);
	perf_event_free_task(p);	chained_irq_enter(chip, desc);
#ifdef CONFIG_NUMA	chained_irq_enter(chip, desc);
	mpol_put(p->mempolicy);	chained_irq_enter(chip, desc);
bad_fork_cleanup_cgroup:	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	if (clone_flags & CLONE_THREAD)	chained_irq_enter(chip, desc);
		threadgroup_change_end(current);	chained_irq_enter(chip, desc);
	cgroup_exit(p, 0);	chained_irq_enter(chip, desc);
	delayacct_tsk_free(p);	chained_irq_enter(chip, desc);
	module_put(task_thread_info(p)->exec_domain->module);	chained_irq_enter(chip, desc);
bad_fork_cleanup_count:	chained_irq_enter(chip, desc);
	atomic_dec(&p->cred->user->processes);	chained_irq_enter(chip, desc);
	exit_creds(p);	chained_irq_enter(chip, desc);
bad_fork_free:	chained_irq_enter(chip, desc);
	free_task(p);	chained_irq_enter(chip, desc);
fork_out:	chained_irq_enter(chip, desc);
	return ERR_PTR(retval);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static inline void init_idle_pids(struct pid_link *links)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	enum pid_type type;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	for (type = PIDTYPE_PID; type < PIDTYPE_MAX; ++type) {	chained_irq_enter(chip, desc);
		INIT_HLIST_NODE(&links[type].node); /* not really needed */	chained_irq_enter(chip, desc);
		links[type].pid = &init_struct_pid;	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
struct task_struct *fork_idle(int cpu)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	struct task_struct *task;	chained_irq_enter(chip, desc);
	task = copy_process(CLONE_VM, 0, 0, NULL, &init_struct_pid, 0);	chained_irq_enter(chip, desc);
	if (!IS_ERR(task)) {	chained_irq_enter(chip, desc);
		init_idle_pids(task->pids);	chained_irq_enter(chip, desc);
		init_idle(task, cpu);	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	return task;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/*	chained_irq_enter(chip, desc);
 *  Ok, this is the main fork-routine.	chained_irq_enter(chip, desc);
 *	chained_irq_enter(chip, desc);
 * It copies the process, and if successful kick-starts	chained_irq_enter(chip, desc);
 * it and waits for it to finish using the VM if required.	chained_irq_enter(chip, desc);
 */	chained_irq_enter(chip, desc);
long do_fork(unsigned long clone_flags,	chained_irq_enter(chip, desc);
	      unsigned long stack_start,	chained_irq_enter(chip, desc);
	      unsigned long stack_size,	chained_irq_enter(chip, desc);
	      int __user *parent_tidptr,	chained_irq_enter(chip, desc);
	      int __user *child_tidptr)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	struct task_struct *p;	chained_irq_enter(chip, desc);
	int trace = 0;	chained_irq_enter(chip, desc);
	long nr;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * Determine whether and which event to report to ptracer.  When	chained_irq_enter(chip, desc);
	 * called from kernel_thread or CLONE_UNTRACED is explicitly	chained_irq_enter(chip, desc);
	 * requested, no event is reported; otherwise, report if the event	chained_irq_enter(chip, desc);
	 * for the type of forking is enabled.	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	if (!(clone_flags & CLONE_UNTRACED)) {	chained_irq_enter(chip, desc);
		if (clone_flags & CLONE_VFORK)	chained_irq_enter(chip, desc);
			trace = PTRACE_EVENT_VFORK;	chained_irq_enter(chip, desc);
		else if ((clone_flags & CSIGNAL) != SIGCHLD)	chained_irq_enter(chip, desc);
			trace = PTRACE_EVENT_CLONE;	chained_irq_enter(chip, desc);
		else	chained_irq_enter(chip, desc);
			trace = PTRACE_EVENT_FORK;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		if (likely(!ptrace_event_enabled(current, trace)))	chained_irq_enter(chip, desc);
			trace = 0;	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	p = copy_process(clone_flags, stack_start, stack_size,	chained_irq_enter(chip, desc);
			 child_tidptr, NULL, trace);	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * Do this prior waking up the new thread - the thread pointer	chained_irq_enter(chip, desc);
	 * might get invalid after that point, if the thread exits quickly.	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	if (!IS_ERR(p)) {	chained_irq_enter(chip, desc);
		struct completion vfork;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		trace_sched_process_fork(current, p);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		nr = task_pid_vnr(p);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		if (clone_flags & CLONE_PARENT_SETTID)	chained_irq_enter(chip, desc);
			put_user(nr, parent_tidptr);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		if (clone_flags & CLONE_VFORK) {	chained_irq_enter(chip, desc);
			p->vfork_done = &vfork;	chained_irq_enter(chip, desc);
			init_completion(&vfork);	chained_irq_enter(chip, desc);
			get_task_struct(p);	chained_irq_enter(chip, desc);
		}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		wake_up_new_task(p);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		/* forking complete and child started to run, tell ptracer */	chained_irq_enter(chip, desc);
		if (unlikely(trace))	chained_irq_enter(chip, desc);
			ptrace_event(trace, nr);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		if (clone_flags & CLONE_VFORK) {	chained_irq_enter(chip, desc);
			if (!wait_for_vfork_done(p, &vfork))	chained_irq_enter(chip, desc);
				ptrace_event(PTRACE_EVENT_VFORK_DONE, nr);	chained_irq_enter(chip, desc);
		}	chained_irq_enter(chip, desc);
	} else {	chained_irq_enter(chip, desc);
		nr = PTR_ERR(p);	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	return nr;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/*	chained_irq_enter(chip, desc);
 * Create a kernel thread.	chained_irq_enter(chip, desc);
 */	chained_irq_enter(chip, desc);
pid_t kernel_thread(int (*fn)(void *), void *arg, unsigned long flags)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	return do_fork(flags|CLONE_VM|CLONE_UNTRACED, (unsigned long)fn,	chained_irq_enter(chip, desc);
		(unsigned long)arg, NULL, NULL);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
#ifdef __ARCH_WANT_SYS_FORK	chained_irq_enter(chip, desc);
SYSCALL_DEFINE0(fork)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
#ifdef CONFIG_MMU	chained_irq_enter(chip, desc);
	return do_fork(SIGCHLD, 0, 0, NULL, NULL);	chained_irq_enter(chip, desc);
#else	chained_irq_enter(chip, desc);
	/* can not support in nommu mode */	chained_irq_enter(chip, desc);
	return -EINVAL;	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
#ifdef __ARCH_WANT_SYS_VFORK	chained_irq_enter(chip, desc);
SYSCALL_DEFINE0(vfork)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	return do_fork(CLONE_VFORK | CLONE_VM | SIGCHLD, 0,	chained_irq_enter(chip, desc);
			0, NULL, NULL);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
#ifdef __ARCH_WANT_SYS_CLONE	chained_irq_enter(chip, desc);
#ifdef CONFIG_CLONE_BACKWARDS	chained_irq_enter(chip, desc);
SYSCALL_DEFINE5(clone, unsigned long, clone_flags, unsigned long, newsp,	chained_irq_enter(chip, desc);
		 int __user *, parent_tidptr,	chained_irq_enter(chip, desc);
		 int, tls_val,	chained_irq_enter(chip, desc);
		 int __user *, child_tidptr)	chained_irq_enter(chip, desc);
#elif defined(CONFIG_CLONE_BACKWARDS2)	chained_irq_enter(chip, desc);
SYSCALL_DEFINE5(clone, unsigned long, newsp, unsigned long, clone_flags,	chained_irq_enter(chip, desc);
		 int __user *, parent_tidptr,	chained_irq_enter(chip, desc);
		 int __user *, child_tidptr,	chained_irq_enter(chip, desc);
		 int, tls_val)	chained_irq_enter(chip, desc);
#elif defined(CONFIG_CLONE_BACKWARDS3)	chained_irq_enter(chip, desc);
SYSCALL_DEFINE6(clone, unsigned long, clone_flags, unsigned long, newsp,	chained_irq_enter(chip, desc);
		int, stack_size,	chained_irq_enter(chip, desc);
		int __user *, parent_tidptr,	chained_irq_enter(chip, desc);
		int __user *, child_tidptr,	chained_irq_enter(chip, desc);
		int, tls_val)	chained_irq_enter(chip, desc);
#else	chained_irq_enter(chip, desc);
SYSCALL_DEFINE5(clone, unsigned long, clone_flags, unsigned long, newsp,	chained_irq_enter(chip, desc);
		 int __user *, parent_tidptr,	chained_irq_enter(chip, desc);
		 int __user *, child_tidptr,	chained_irq_enter(chip, desc);
		 int, tls_val)	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	return do_fork(clone_flags, newsp, 0, parent_tidptr, child_tidptr);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
#ifndef ARCH_MIN_MMSTRUCT_ALIGN	chained_irq_enter(chip, desc);
#define ARCH_MIN_MMSTRUCT_ALIGN 0	chained_irq_enter(chip, desc);
#endif	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
static void sighand_ctor(void *data)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	struct sighand_struct *sighand = data;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	spin_lock_init(&sighand->siglock);	chained_irq_enter(chip, desc);
	init_waitqueue_head(&sighand->signalfd_wqh);	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
void __init proc_caches_init(void)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	sighand_cachep = kmem_cache_create("sighand_cache",	chained_irq_enter(chip, desc);
			sizeof(struct sighand_struct), 0,	chained_irq_enter(chip, desc);
			SLAB_HWCACHE_ALIGN|SLAB_PANIC|SLAB_DESTROY_BY_RCU|	chained_irq_enter(chip, desc);
			SLAB_NOTRACK, sighand_ctor);	chained_irq_enter(chip, desc);
	signal_cachep = kmem_cache_create("signal_cache",	chained_irq_enter(chip, desc);
			sizeof(struct signal_struct), 0,	chained_irq_enter(chip, desc);
			SLAB_HWCACHE_ALIGN|SLAB_PANIC|SLAB_NOTRACK, NULL);	chained_irq_enter(chip, desc);
	files_cachep = kmem_cache_create("files_cache",	chained_irq_enter(chip, desc);
			sizeof(struct files_struct), 0,	chained_irq_enter(chip, desc);
			SLAB_HWCACHE_ALIGN|SLAB_PANIC|SLAB_NOTRACK, NULL);	chained_irq_enter(chip, desc);
	fs_cachep = kmem_cache_create("fs_cache",	chained_irq_enter(chip, desc);
			sizeof(struct fs_struct), 0,	chained_irq_enter(chip, desc);
			SLAB_HWCACHE_ALIGN|SLAB_PANIC|SLAB_NOTRACK, NULL);	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * FIXME! The "sizeof(struct mm_struct)" currently includes the	chained_irq_enter(chip, desc);
	 * whole struct cpumask for the OFFSTACK case. We could change	chained_irq_enter(chip, desc);
	 * this to *only* allocate as much of it as required by the	chained_irq_enter(chip, desc);
	 * maximum number of CPU's we can ever have.  The cpumask_allocation	chained_irq_enter(chip, desc);
	 * is at the end of the structure, exactly for that reason.	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	mm_cachep = kmem_cache_create("mm_struct",	chained_irq_enter(chip, desc);
			sizeof(struct mm_struct), ARCH_MIN_MMSTRUCT_ALIGN,	chained_irq_enter(chip, desc);
			SLAB_HWCACHE_ALIGN|SLAB_PANIC|SLAB_NOTRACK, NULL);	chained_irq_enter(chip, desc);
	vm_area_cachep = KMEM_CACHE(vm_area_struct, SLAB_PANIC);	chained_irq_enter(chip, desc);
	mmap_init();	chained_irq_enter(chip, desc);
	nsproxy_cache_init();	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/*	chained_irq_enter(chip, desc);
 * Check constraints on flags passed to the unshare system call.	chained_irq_enter(chip, desc);
 */	chained_irq_enter(chip, desc);
static int check_unshare_flags(unsigned long unshare_flags)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	if (unshare_flags & ~(CLONE_THREAD|CLONE_FS|CLONE_NEWNS|CLONE_SIGHAND|	chained_irq_enter(chip, desc);
				CLONE_VM|CLONE_FILES|CLONE_SYSVSEM|	chained_irq_enter(chip, desc);
				CLONE_NEWUTS|CLONE_NEWIPC|CLONE_NEWNET|	chained_irq_enter(chip, desc);
				CLONE_NEWUSER|CLONE_NEWPID))	chained_irq_enter(chip, desc);
		return -EINVAL;	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * Not implemented, but pretend it works if there is nothing to	chained_irq_enter(chip, desc);
	 * unshare. Note that unsharing CLONE_THREAD or CLONE_SIGHAND	chained_irq_enter(chip, desc);
	 * needs to unshare vm.	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	if (unshare_flags & (CLONE_THREAD | CLONE_SIGHAND | CLONE_VM)) {	chained_irq_enter(chip, desc);
		/* FIXME: get_task_mm() increments ->mm_users */	chained_irq_enter(chip, desc);
		if (atomic_read(&current->mm->mm_users) > 1)	chained_irq_enter(chip, desc);
			return -EINVAL;	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	return 0;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/*	chained_irq_enter(chip, desc);
 * Unshare the filesystem structure if it is being shared	chained_irq_enter(chip, desc);
 */	chained_irq_enter(chip, desc);
static int unshare_fs(unsigned long unshare_flags, struct fs_struct **new_fsp)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	struct fs_struct *fs = current->fs;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	if (!(unshare_flags & CLONE_FS) || !fs)	chained_irq_enter(chip, desc);
		return 0;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/* don't need lock here; in the worst case we'll do useless copy */	chained_irq_enter(chip, desc);
	if (fs->users == 1)	chained_irq_enter(chip, desc);
		return 0;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	*new_fsp = copy_fs_struct(fs);	chained_irq_enter(chip, desc);
	if (!*new_fsp)	chained_irq_enter(chip, desc);
		return -ENOMEM;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	return 0;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/*	chained_irq_enter(chip, desc);
 * Unshare file descriptor table if it is being shared	chained_irq_enter(chip, desc);
 */	chained_irq_enter(chip, desc);
static int unshare_fd(unsigned long unshare_flags, struct files_struct **new_fdp)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	struct files_struct *fd = current->files;	chained_irq_enter(chip, desc);
	int error = 0;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	if ((unshare_flags & CLONE_FILES) &&	chained_irq_enter(chip, desc);
	    (fd && atomic_read(&fd->count) > 1)) {	chained_irq_enter(chip, desc);
		*new_fdp = dup_fd(fd, &error);	chained_irq_enter(chip, desc);
		if (!*new_fdp)	chained_irq_enter(chip, desc);
			return error;	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	return 0;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/*	chained_irq_enter(chip, desc);
 * unshare allows a process to 'unshare' part of the process	chained_irq_enter(chip, desc);
 * context which was originally shared using clone.  copy_*	chained_irq_enter(chip, desc);
 * functions used by do_fork() cannot be used here directly	chained_irq_enter(chip, desc);
 * because they modify an inactive task_struct that is being	chained_irq_enter(chip, desc);
 * constructed. Here we are modifying the current, active,	chained_irq_enter(chip, desc);
 * task_struct.	chained_irq_enter(chip, desc);
 */	chained_irq_enter(chip, desc);
SYSCALL_DEFINE1(unshare, unsigned long, unshare_flags)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	struct fs_struct *fs, *new_fs = NULL;	chained_irq_enter(chip, desc);
	struct files_struct *fd, *new_fd = NULL;	chained_irq_enter(chip, desc);
	struct cred *new_cred = NULL;	chained_irq_enter(chip, desc);
	struct nsproxy *new_nsproxy = NULL;	chained_irq_enter(chip, desc);
	int do_sysvsem = 0;	chained_irq_enter(chip, desc);
	int err;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * If unsharing a user namespace must also unshare the thread.	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	if (unshare_flags & CLONE_NEWUSER)	chained_irq_enter(chip, desc);
		unshare_flags |= CLONE_THREAD | CLONE_FS;	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * If unsharing a thread from a thread group, must also unshare vm.	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	if (unshare_flags & CLONE_THREAD)	chained_irq_enter(chip, desc);
		unshare_flags |= CLONE_VM;	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * If unsharing vm, must also unshare signal handlers.	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	if (unshare_flags & CLONE_VM)	chained_irq_enter(chip, desc);
		unshare_flags |= CLONE_SIGHAND;	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * If unsharing namespace, must also unshare filesystem information.	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	if (unshare_flags & CLONE_NEWNS)	chained_irq_enter(chip, desc);
		unshare_flags |= CLONE_FS;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	err = check_unshare_flags(unshare_flags);	chained_irq_enter(chip, desc);
	if (err)	chained_irq_enter(chip, desc);
		goto bad_unshare_out;	chained_irq_enter(chip, desc);
	/*	chained_irq_enter(chip, desc);
	 * CLONE_NEWIPC must also detach from the undolist: after switching	chained_irq_enter(chip, desc);
	 * to a new ipc namespace, the semaphore arrays from the old	chained_irq_enter(chip, desc);
	 * namespace are unreachable.	chained_irq_enter(chip, desc);
	 */	chained_irq_enter(chip, desc);
	if (unshare_flags & (CLONE_NEWIPC|CLONE_SYSVSEM))	chained_irq_enter(chip, desc);
		do_sysvsem = 1;	chained_irq_enter(chip, desc);
	err = unshare_fs(unshare_flags, &new_fs);	chained_irq_enter(chip, desc);
	if (err)	chained_irq_enter(chip, desc);
		goto bad_unshare_out;	chained_irq_enter(chip, desc);
	err = unshare_fd(unshare_flags, &new_fd);	chained_irq_enter(chip, desc);
	if (err)	chained_irq_enter(chip, desc);
		goto bad_unshare_cleanup_fs;	chained_irq_enter(chip, desc);
	err = unshare_userns(unshare_flags, &new_cred);	chained_irq_enter(chip, desc);
	if (err)	chained_irq_enter(chip, desc);
		goto bad_unshare_cleanup_fd;	chained_irq_enter(chip, desc);
	err = unshare_nsproxy_namespaces(unshare_flags, &new_nsproxy,	chained_irq_enter(chip, desc);
					 new_cred, new_fs);	chained_irq_enter(chip, desc);
	if (err)	chained_irq_enter(chip, desc);
		goto bad_unshare_cleanup_cred;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	if (new_fs || new_fd || do_sysvsem || new_cred || new_nsproxy) {	chained_irq_enter(chip, desc);
		if (do_sysvsem) {	chained_irq_enter(chip, desc);
			/*	chained_irq_enter(chip, desc);
			 * CLONE_SYSVSEM is equivalent to sys_exit().	chained_irq_enter(chip, desc);
			 */	chained_irq_enter(chip, desc);
			exit_sem(current);	chained_irq_enter(chip, desc);
		}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		if (new_nsproxy)	chained_irq_enter(chip, desc);
			switch_task_namespaces(current, new_nsproxy);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		task_lock(current);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		if (new_fs) {	chained_irq_enter(chip, desc);
			fs = current->fs;	chained_irq_enter(chip, desc);
			spin_lock(&fs->lock);	chained_irq_enter(chip, desc);
			current->fs = new_fs;	chained_irq_enter(chip, desc);
			if (--fs->users)	chained_irq_enter(chip, desc);
				new_fs = NULL;	chained_irq_enter(chip, desc);
			else	chained_irq_enter(chip, desc);
				new_fs = fs;	chained_irq_enter(chip, desc);
			spin_unlock(&fs->lock);	chained_irq_enter(chip, desc);
		}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		if (new_fd) {	chained_irq_enter(chip, desc);
			fd = current->files;	chained_irq_enter(chip, desc);
			current->files = new_fd;	chained_irq_enter(chip, desc);
			new_fd = fd;	chained_irq_enter(chip, desc);
		}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		task_unlock(current);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
		if (new_cred) {	chained_irq_enter(chip, desc);
			/* Install the new user namespace */	chained_irq_enter(chip, desc);
			commit_creds(new_cred);	chained_irq_enter(chip, desc);
			new_cred = NULL;	chained_irq_enter(chip, desc);
		}	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
bad_unshare_cleanup_cred:	chained_irq_enter(chip, desc);
	if (new_cred)	chained_irq_enter(chip, desc);
		put_cred(new_cred);	chained_irq_enter(chip, desc);
bad_unshare_cleanup_fd:	chained_irq_enter(chip, desc);
	if (new_fd)	chained_irq_enter(chip, desc);
		put_files_struct(new_fd);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
bad_unshare_cleanup_fs:	chained_irq_enter(chip, desc);
	if (new_fs)	chained_irq_enter(chip, desc);
		free_fs_struct(new_fs);	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
bad_unshare_out:	chained_irq_enter(chip, desc);
	return err;	chained_irq_enter(chip, desc);
}	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
/*	chained_irq_enter(chip, desc);
 *	Helper to unshare the files of the current task.	chained_irq_enter(chip, desc);
 *	We don't want to expose copy_files internals to	chained_irq_enter(chip, desc);
 *	the exec layer of the kernel.	chained_irq_enter(chip, desc);
 */	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
int unshare_files(struct files_struct **displaced)	chained_irq_enter(chip, desc);
{	chained_irq_enter(chip, desc);
	struct task_struct *task = current;	chained_irq_enter(chip, desc);
	struct files_struct *copy = NULL;	chained_irq_enter(chip, desc);
	int error;	chained_irq_enter(chip, desc);
	chained_irq_enter(chip, desc);
	error = unshare_fd(CLONE_FILES, &copy);	chained_irq_enter(chip, desc);
	if (error || !copy) {	chained_irq_enter(chip, desc);
		*displaced = NULL;	chained_irq_enter(chip, desc);
		return error;	chained_irq_enter(chip, desc);
	}	chained_irq_enter(chip, desc);
	*displaced = task->files;	chained_irq_enter(chip, desc);
	task_lock(task);	chained_irq_enter(chip, desc);
	task->files = copy;	chained_irq_enter(chip, desc);
	task_unlock(task);	chained_irq_enter(chip, desc);
	return 0;	chained_irq_enter(chip, desc);
}